# 역질문 자동 응답 기본값 — plans/94 · D-216 (scripts/scenario/clarify.py)
#
# 존 선택·범위 선택 역질문이 오면 러너가 이 순서로 존을 **하나** 고른다(사용자 확정 2026-09-15).
# 목록의 존이 선택지에 없으면 서버가 제시한 첫 선택지를 고른다.
# 시나리오 YAML 의 `auto_answer.selected_db_ids` 가 있으면 그 값이 이긴다.
#
# 김포(gp)가 먼저인 이유: 시나리오 픽스처(cocm-hdkapp01 · "김포 서버로 채워줘" 양식)가 김포 기준이고,
# 존 전체를 고르면 질의당 비용·시간이 약 3배라 60초 타임아웃(504)에 걸리기 쉽다(run 20260914-154940 R2-10).
zone_preference:
  - polestar_cm_gp
