"""스키마 설명 + 유사 단어 생성용 프롬프트 템플릿.

LLM을 사용하여 각 컬럼의 한국어 설명과 유사 단어 목록을 생성한다.
테이블 단위로 배치 처리하여 1회 LLM 호출로 설명 + 유사 단어를 동시 생성한다.

DB 설명 생성 프롬프트도 포함: DB의 테이블 목록과 샘플 데이터를 분석하여
DB가 어떤 데이터를 보유하고 있는지 한국어로 간결하게 설명한다.
"""

# === DB 설명 생성 프롬프트 ===

DB_DESCRIPTION_SYSTEM_PROMPT = """당신은 데이터베이스 전문가입니다.
주어진 DB의 테이블 구성과 샘플 데이터를 분석하여, 이 DB가 어떤 데이터를 관리하고 어떤 용도로 사용되는지
한국어로 간결하게 설명해주세요.

## 규칙
- 1문장, 30~80자 이내로 작성
- "~를 관리하는 ~DB" 형태 권장
- 주요 데이터 도메인(서버, VM, 티켓, 자산 등)을 포함
- JSON이나 코드 블록 없이 순수 텍스트만 출력
- 추가 설명이나 부연 없이 설명 문장만 출력
"""

DB_DESCRIPTION_USER_TEMPLATE = """다음 DB의 설명을 한국어 1문장으로 작성해주세요.

## DB 식별자: {db_id}

## 테이블 ({table_count}개)
{tables_info}

위 정보를 분석하여, 이 DB가 어떤 데이터를 보유하고 어떤 용도인지 한국어 1문장으로 설명하세요.
"""

# 답변 영역 소유 안내 (plans/102 X-T4 · `ROUTER_CAPABILITY_OWNERSHIP_ENABLED`) — 켜졌을 때만
# 위 user 프롬프트 말미에 덧붙인다(off면 프롬프트 바이트 동일). 생성 설명은 라우터 DB 목록의
# 「상세」로 들어가므로, 테이블에 다른 시스템이 정본인 영역과 비슷한 컬럼이 있으면 설명이 그 어휘를
# 되살려 라우팅 경계를 무너뜨린다. 영역 행은 레지스트리에서 렌더해 넣는다
# (사본 금지 · `src/routing/capability_ownership.ownership_guidance_rows`).
DB_DESCRIPTION_OWNERSHIP_TEMPLATE = """
## 답변 영역 소유 (반드시 반영)
이 DB는 **{system_label}** 시스템에 속합니다. 설명에는 이 시스템이 정본인 답변 영역의 어휘를 쓰세요.

이 시스템이 정본인 답변 영역:
{own_rows}

다른 시스템이 정본인 답변 영역 — 테이블에 비슷한 컬럼이 있어도 이 영역의 어휘는 설명에 넣지 마세요:
{other_rows}
"""

SCHEMA_DESCRIPTION_SYSTEM_PROMPT = """당신은 데이터베이스 스키마 전문가입니다.
주어진 테이블의 컬럼 정보와 샘플 데이터를 분석하여, 각 컬럼에 대해:
1. 한국어 설명 (description): 컬럼의 의미를 간결하게 설명
2. 유사 단어 목록 (synonyms): 사용자가 이 컬럼을 지칭할 때 사용할 수 있는 다양한 표현

## 출력 규칙

- description은 한국어로, 15~40자 이내로 작성
- synonyms는 한국어와 영어를 혼합하여 3~8개 작성
- 사용자가 자연어 질의에서 사용할 법한 단어를 포함
- 약어, 별칭, 동의어, 영문 표기를 모두 포함

## 출력 형식

반드시 유효한 JSON만 출력하세요. 추가 설명은 불필요합니다.

```json
{
  "table.column1": {
    "description": "컬럼 설명",
    "synonyms": ["유사단어1", "유사단어2", "유사단어3"]
  },
  "table.column2": {
    "description": "컬럼 설명",
    "synonyms": ["유사단어1", "유사단어2"]
  }
}
```
"""

SCHEMA_DESCRIPTION_USER_TEMPLATE = """다음 테이블의 컬럼에 대해 한국어 설명과 유사 단어를 생성해주세요.

## 테이블: {table_name}

## 컬럼 정보
{columns_info}

## 샘플 데이터
{sample_data}

위 정보를 분석하여, 각 컬럼의 description과 synonyms를 JSON 형식으로 출력하세요.
키 형식은 "{table_name}.컬럼명" 입니다.
"""
