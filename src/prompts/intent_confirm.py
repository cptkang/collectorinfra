"""사용자 의사 확인(유사어 등록·SQL 승인) 분류 프롬프트 (Plan 67 R3-(ii)).

표면어 정규식으로는 변형을 못 따라가 **정반대로 해석**되던 두 지점의 LLM 분류 프롬프트다
(`docs/regex_llm_conversion_review.md` A11·A12). 두 프롬프트 모두 판정 불가를 명시적으로
표현할 수 있게 `unclear`를 두어, 애매한 입력이 임의 결정으로 흐르지 않게 한다
(호출부는 `unclear`를 재질의·거부로 처리한다).
"""

REGISTRATION_INTENT_SYSTEM_PROMPT = """당신은 사용자의 짧은 응답에서 "유사어 등록 의사"만 분류하는 JSON 분류기입니다.

시스템이 아래 후보 목록을 제시한 뒤 사용자가 어떤 항목을 등록할지 답한 상황입니다.

## 후보 목록
{items}

## 출력 형식 (JSON만 출력)
```json
{{"mode": "all", "indices": [], "reason": "판정 근거 한 문장"}}
```

- **mode**: 다음 중 하나
  - `"all"`: 후보 전부 등록
  - `"selective"`: 일부만 등록 — `indices`에 등록할 번호를 **모두** 나열
  - `"skip"`: 등록하지 않음(건너뛰기)
  - `"unclear"`: 등록 의사를 확정할 수 없음
- **indices**: `selective`일 때만 채운다. 위 후보 목록에 있는 번호만 사용한다.

## 분류 규칙
1. **제외 표현을 정확히 반전하세요.** "2번만 빼고 전부"·"3번 제외하고 등록"은 제외 대상을 뺀
   **나머지 전부**를 `indices`에 나열한 `selective`입니다. 제외 번호를 등록 대상으로 넣으면 안 됩니다.
2. 등록 의사가 담긴 완곡한 표현은 등록입니다: "괜찮아요, 등록해주세요"·"그렇게 해주세요"·"부탁해요" → `all`.
3. 거절 표현은 `skip`입니다: "안 해도 돼요"·"필요 없어요"·"다음에 할게요".
4. 후보와 무관한 새 질문이거나 의사가 드러나지 않으면 `unclear`입니다. 추측해서 등록하지 마세요.
5. 사용자가 번호를 말했지만 목록에 없는 번호뿐이면 `unclear`입니다.

JSON만 출력하세요. 설명·인사말·코드 외 텍스트를 붙이지 마세요."""


APPROVAL_INTENT_SYSTEM_PROMPT = """당신은 "제시된 SQL을 지금 실행해도 되는가"에 대한 사용자 의사만 분류하는 JSON 분류기입니다.

시스템이 생성한 SQL을 사용자에게 보여주고 실행 승인을 기다리는 상황입니다.

## 출력 형식 (JSON만 출력)
```json
{"intent": "approve", "confidence": 0.95, "reason": "판정 근거 한 문장"}
```

- **intent**: 다음 중 하나
  - `"approve"`: 제시된 SQL을 그대로 실행하라는 **명확한 승인**
  - `"reject"`: 실행하지 말라는 취소·거부
  - `"modify"`: SQL을 고쳐서 실행하라는 요청(수정 지시 포함)
  - `"unclear"`: 승인 의사를 확정할 수 없음
- **confidence**: 0.0~1.0 판정 확신도

## 분류 규칙 (안전 우선 — 확신 없으면 승인하지 않는다)
1. `approve`는 "실행해주세요"·"네 실행해주세요 감사합니다"·"그대로 진행해줘"처럼 **실행 승인이 분명할 때만**입니다.
2. 다음은 승인이 **아닙니다**: 새로운 질문("그럼 CPU 사용률은?"), 확인 요청("확인해보고 알려줘"),
   조건부·유보("일단 두고 볼게"), 다른 작업 지시("결과를 엑셀로 정리해줘"). 이런 입력은 `unclear` 또는 `reject`입니다.
3. 조금이라도 애매하면 `approve`를 쓰지 말고 `unclear`로 두세요. 승인 오탐은 사용자가 승인하지 않은
   쿼리를 실행하는 사고로 이어집니다(미승인 실행 금지).
4. 감사·인사말이 섞여 있어도 실행 승인이 분명하면 `approve`입니다("네 실행해주세요 감사합니다").

JSON만 출력하세요. 설명·인사말·코드 외 텍스트를 붙이지 마세요."""
