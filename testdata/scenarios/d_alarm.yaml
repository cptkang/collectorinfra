# 자동 생성 초안 (scripts/scenario/import_docs.py) - plans/94 §3.4
#
# expect 는 비어 있고 manual_review 만 들어 있다. 단언으로 옮긴 만큼만 자동 판정
# 대상이 되며, 옮기지 않은 것은 리포트 9절 '수동 검토'에 계속 남는다.
# plans 필드는 군 단위 기본값이다 - 시나리오별로 정확히 좁히는 것이 사람의 일이다.
version: 1
group:
  id: D
  name: "알람 조회"
  latency_target_ms: 10000
  policy_confirmed: false
  source: "docs/29"
scenarios:
  - id: D-01
    plans: [44, 45]
    title: "현재 발생 중인 심각(severity 3) 알람 목록을 조회해줘"
    kind: normal
    env: closed
    profile: baseline
    endpoint: stream
    turns:
      - send:
          query: "현재 발생 중인 심각(severity 3) 알람 목록을 조회해줘"
        expect:
          status: completed
          sql_must_match: ["(?i)cmm_alarm_active", "(?i)alarmseverity\\s*=\\s*3",
                          "(?i)dtime\\s+is\\s+null"]
          manual_review: "9열 표준 알람 뷰인지"
  - id: D-02
    plans: [44, 45]
    title: "경고 이상 알람 이력을 최근 발생 순으로 100건 조회해줘"
    kind: normal
    env: closed
    profile: baseline
    endpoint: stream
    turns:
      - send:
          query: "경고 이상 알람 이력을 최근 발생 순으로 100건 조회해줘"
        expect:
          status: completed
          sql_must_match: ["(?i)\\blimit\\s+100\\b",
                          "(?i)order\\s+by[\\s\\S]*\\bdesc\\b"]
          row_count: {max: 100}
  - id: D-03
    plans: [44, 45]
    title: "2026년 7월에 발생한 심각 알람이 몇 건인지 알려줘"
    kind: normal
    env: closed
    profile: baseline
    endpoint: stream
    turns:
      - send:
          query: "2026년 7월에 발생한 심각 알람이 몇 건인지 알려줘"
        expect:
          status: completed
          sql_must_match: ["(?i)count\\s*\\(", "(?i)202607"]
          row_count: {eq: 1}
  - id: D-04
    plans: [44, 45]
    title: "최근 3개월간 경고 이상 알람이 가장 많이 발생한 상위 10개 서버를 알려줘"
    kind: normal
    env: closed
    profile: baseline
    endpoint: stream
    turns:
      - send:
          query: "최근 3개월간 경고 이상 알람이 가장 많이 발생한 상위 10개 서버를 알려줘"
        expect:
          status: completed
          sql_must_match: ["(?i)group\\s+by", "(?i)order\\s+by[\\s\\S]*\\bdesc\\b",
                          "(?i)\\blimit\\s+10\\b"]
          row_count: {max: 10}
  - id: D-05
    plans: [44, 45]
    title: "이번 달 CPU 임계값 초과 알람 이력 조회"
    kind: normal
    env: closed
    profile: baseline
    endpoint: stream
    turns:
      - send:
          query: "이번 달 CPU 임계값 초과 알람 이력 조회"
        expect:
          status: completed
          sql_must_match: ["(?i)cmm_alarm_def"]
          manual_review: "이번 달 범위가 실행 시점 기준으로 맞는지"
  - id: D-06
    plans: [44, 45]
    title: "담당자 없는 미확인 알람"
    kind: normal
    env: closed
    profile: baseline
    endpoint: stream
    turns:
      - send:
          query: "담당자 없는 미확인 알람"
        expect:
          manual_review: "통보 다단 조인(yd-006 계열) - coverage=outside. 실패 허용 기록"
