# Task List: Plan 78·79 검토 정정 작업

> 계획: `plans/80-78-79-joint-execution-contract.md` · 대상: `plans/78` · `plans/79`
> (버전은 드리프트가 잦아 핀하지 않는다 — 착수 시 해당 문서의 변경 이력 최신 행을 본다)
> 작성일: 2026-08-26 · **최종 확인 2026-08-28**
> **상태**: **A1~A5 · B1~B6 · C3 · C4 · D1 · D2 = 15건 완료** (2026-08-28 실측 재확인).
> **잔여는 C1·C2 둘뿐이며 G-BILL(D-127 승인 + FabriX 접속 정보)에 막혀 있다.**
>
> ⚠️ 기존 `tasks/{plan,todo}.md`는 **Plan 79 트랙 A 작업(병렬 세션)이 점유 중**이라 파일명을 분리했다
> (`plan-70`/`todo-70` 전례).

---

## 0-A. 재조정 (2026-08-27 · `plans/80` v6 실행 구동 전환에 따른 정합)

> `plans/80` §5가 **실행 단위(WU) 표**로 재작성되면서 이 문서와 **두 축이 겹쳤다.**
> 전수 대조 결과와 소유 경계를 아래로 확정한다. **이 문서는 폐기하지 않는다** — 아직 유효한 항목이 14건이다.

| 축 | 소유 | 이 문서에서 |
|---|---|---|
| **구현** | `plans/80` **§5 WU 표**(단일 출처) | C1·C2·C3은 **WU로 이관** — 아래 대조표 참조 |
| **문서 정정** | `plans/80` **§7 정정 지시** | **Phase A·B·D가 정본 실행 목록** — WU 표에 넣지 않는다 |

**전수 대조 결과(2026-08-27 실측)**

| 항목 | 상태 | 근거 |
|---|---|---|
| A1~A5 · B1~B6 · D1 · D2 | ~~아직 유효~~ → ✅ **13건 전부 완료**(2026-08-27 실행 · **2026-08-28 실측 재확인**) | 재확인 근거 — §4.5-① `host_diagnostics` **0건** · "수집기 백엔드" 잔존 2건은 전부 *(v13 정정 —)* 각주(의도적 보존) · §3.1③ "실측 결과 운영 LLM은 Gemini가 아니라 FabriX" · 트랙 C 이월 표기 **14건** · §6.3 착수 순서에 이월 축 존재 · "Gemini 실호출" **0건** · D-170 등재 완료 |
| **C1** 골든셋 회귀 | → **WU-05**(G-BILL) | 80 §5.2 차수 1 |
| **C2** 분포 실측 | → **WU-06**(G-BILL) | 80 §5.2 차수 1 |
| **C3** 임계 잠정값 고정 | → **WU-01**(게이트 없음) | 80 §5.2 차수 0 |
| **C4** 계약 C-A 테스트 | ✅ **이미 충족 — 완료 처리** | `tests/test_semantic_routing/test_router_prompt_structure.py::TestNoRegressionWhenFaultDiagnosisOff::test_off_prompt_never_mentions_fault_diagnosis`가 `"fault_diagnosis" not in off`를 단언 → **정의 줄·판단 절·예시 세 자리를 모두 덮는다**. `test_on_prompt_defines_fault_diagnosis_class`가 on 경로를 덮는다 |

> **주의**: 종전 머리말의 *"병렬 세션이 점유 중"* 은 2026-08-26 시점 기록이다. 이후 79 트랙 A가
> 랜딩했고 `plans/78`·`79`·`80`은 각각 v12·v14·v6까지 갱신됐다. **A/B 항목 착수 전 해당 절을
> 다시 실측**할 것 — 위 대조는 2026-08-27 기준이다.

---

## 0. 이 문서의 범위

2026-08-26 78·79 통합 검토에서 나온 정정 사항 중 **아직 적용되지 않은 것**만 담는다.
이미 반영된 것은 문서 끝 **「완료 기록」** 에 남긴다(재작업 방지).

**공통 DoD**
- 문서 정정: 정정 근거(실측 또는 출처)를 같은 자리에 명시하고, 변경 이력 표에 행 추가
- 코드 변경 시: `pytest -q` 무회귀 · `python scripts/arch_check.py --ci` exit 0
- **실 LLM 호출은 D-127 건별 사용자 승인 + `RUN_E2E=1`** — 승인 없이 실행 금지

**소유권 주의** *(2026-08-26 시점 기록 — 이후 79 트랙 A가 랜딩해 상황이 바뀌었다)*
`plans/79`·`src/prompts/semantic_router.py`·`src/routing/semantic_router.py`·
`tests/test_semantic_routing/`는 당시 **병렬 세션이 작업 중**이었다. 착수 전
`git status`로 재확인하고, 충돌하면 **내 항목을 뒤 번호로 재부여**한다(상대 작업을 덮지 않는다).
**이 원칙은 계속 유효하다** — 이 저장소는 세션이 겹친다.

---

## Phase A — `plans/78` 잔여 정정 (코드 변경 0 · 충돌 없음)

> v9가 §4.1·§4.2·§4.3·§8.1·R-1과 W3 번호 드리프트를 정리했으나, **v6 어댑터 폐기가
> 닿지 않은 서술 5곳**이 남았다. 전부 "78이 수집기를 만든다"는 폐기된 전제를 깔고 있다.

### ~~A1~~ ✅ **완료(2026-08-27)** — §4.5-① 프롬프트 최적화 절의 폐기 capability 참조 제거

**Description:** *"W3에서 `host_diagnostics` 설명을 추가하며 `process_query`와의 표면어 충돌을
다룬다(P6)"* — `host_diagnostics` capability는 v6에서 폐기됐다. 실제 표면어 충돌 대상은
**W3-1이 배선할 `mcp_server` 고수준 도구**(`process_snapshot` 등)다(§3.2.1은 v9에서 이미 정정).

**Acceptance:**
- [x] §4.5-①의 `host_diagnostics` 표기가 `mcp_server` 고수준 도구로 교체
- [x] W3 참조가 **W3-1·W3-4**(도구 배선·명명 규약)를 가리킴
- [x] 결정적 교정 가드 필요성 논지는 **유지**(P6·D-047 전례는 그대로 유효)

**Verify:**
- [x] `grep -n "host_diagnostics" plans/78-*.md` 결과에 §4.5-① 라인이 없다
      (폐기 기록·§0.3·§6.1 취소선 항목은 **의도적 보존**이므로 남는다)

**Files:** `plans/78-composite-query-host-diagnostics-orchestration.md`
**Scope:** XS

---

### ~~A2~~ ✅ **완료(2026-08-27)** — W5-4 "수집기 백엔드 가용성" → "조사 경로 가용성"

**Description:** W5-4(실행 전 준비 검증)가 *"수집기 백엔드가 가용한지 조사 착수 전에 확인한다"*
로 되어 있다. 78은 수집기를 만들지 않으므로 확인 대상은 **`sre_agent` 위임 경로와
`mcp_server` 도구의 가용성**이다. 원 논지(평가 인프라 잡음이 모델 실패로 위장한다)는 유효하다.

**Acceptance:**
- [x] "수집기 백엔드" → "조사 경로(`sre_agent` 위임 · `mcp_server` 고수준 도구)" 로 교체
- [x] 미가용 시 **조사를 시작하지 않고 사유 반환**·경로 강등(W3-2) 규약 유지
- [x] 폴백 경유 참조가 **W3-2**(v9 정정본)를 가리킴

**Verify:**
- [x] W5 절에 "수집기 백엔드" 문자열 0건

**Files:** `plans/78-...md`
**Scope:** XS

---

### ~~A3~~ ✅ **완료(2026-08-27)** — W6-3 기동 로그 항목에서 "수집기 백엔드" 제거

**Description:** *"기동 로그에 수집기 백엔드·플래그 확정값 1줄"* — 백엔드가 78 소관이 아니므로
기록할 것은 **조사 경로 활성 여부와 플래그 확정값**이다(`observability/ladder.py` 전례는 유지).

**Acceptance:**
- [x] "수집기 백엔드" → "조사 경로 활성 상태(`COMPOSITE_INVESTIGATION_ENABLED`·
      `fault_diagnosis_enabled`·`investigation_trigger_enabled`)"
- [x] 기동 시 1회 로그 규율(P14 · 사다리 전례) 유지

**Files:** `plans/78-...md`
**Scope:** XS

---

### ~~A4~~ ✅ **완료(2026-08-27)** — 상단 「사용자 확정 ①」에 v6 번복 각주 추가

**Description:** 문서 최상단이 *"실행 채널 = 어댑터 추상화로 둘 다 설계 — 폴스타 에이전트와 SSH를
교체 가능한 백엔드로"* 를 **사용자 확정**으로 제시한다. v6에서 뒤집혔지만 상단이라 먼저 읽히고,
독자가 §0.3까지 가야 번복을 안다. **확정 기록은 보존하되 그 자리에 번복 사실을 붙인다.**

**Acceptance:**
- [x] 사용자 확정 ① 바로 뒤에 v6 번복 각주 1~2줄 — *"`sre_agent`가 이미 bash toolset을 보유한
      사실을 모른 채 내려진 확정이며, 재실측 후 실행 채널 전체를 `sre_agent`로 이관(§0.3·§4.3)"*
- [x] **원 확정 문구는 삭제하지 않는다**(의사결정 이력)

**Files:** `plans/78-...md`
**Scope:** XS

---

### ~~A5~~ ✅ **완료(2026-08-27)** — R-2 대응란의 잔존 SSH 서술 정리

**Description:** R-2는 표제가 *"해소(v6)"* 인데 대응란에 *"SSH는 기본 off·옵트인·개발/검증 한정.
운영 활성 전 보안 재승인 필요"* 가 남아 78이 여전히 SSH를 관리하는 것처럼 읽힌다.

**Acceptance:**
- [x] 대응란을 "78 소관 아님 — `sre_agent` 실행 채널 정책으로 이관"으로 교체
- [x] 리스크 행 자체는 **취소선 유지**(해소 이력)

**Files:** `plans/78-...md`
**Scope:** XS

---

## Phase B — `plans/79` 잔여 정정 (⚠ 병렬 세션 소유 · 착수 전 재확인)

> v10(병렬)·v11(본 검토)이 §3 표·§8②·§3.5③·A-3·§6.4·§8⑧을 정리했으나,
> **트랙 C 이월이 문서 전반에 반영되지 않았다.** 아래는 전부 "트랙 C가 지금 진행 가능하다"는
> 무효 전제 위에 서 있다.

### ~~B1~~ ✅ **완료(2026-08-27)** — §3.1 ③ "logprob 가용성 미확인" → 확정 결론으로 교체

**Description:** *"**Gemini API가 logprobs를 제공하는지 실측 필요**하며, 미제공이면 트랙 C는
vLLM 경로에서만 성립한다"* — **실측이 끝났다.** 운영은 FabriX **KBGenAI 모드**이고 요청 스펙에
logprobs 자리가 없으며 응답이 `{status, content}` 텍스트뿐이라 **원천 불가**다(§8 ② · SPEC §0.1).
§3.1②는 v10에서 이미 FabriX로 정정됐는데 ③만 Gemini 전제로 남았다.

**Acceptance:**
- [x] 제목이 "미확인" → **"확정 — 현행 경로에서 불가"**
- [x] Gemini 표기 제거, FabriX KBGenAI 근거 명시(`src/llm.py` · SPEC §0.1)
- [x] **트랙 C 전체를 vLLM 전환 후로 이월**한다는 결론 명시
- [x] v4 추가분(첫 토큰이 라벨이어야 한다 — C-0 종속)은 **유지**(재개 시 여전히 유효)
- [x] 재개 최단 경로로 **§8 ⑪**(라우터를 제어 평면으로 이동) 참조

**Verify:**
- [x] `grep -n "Gemini" plans/79-*.md`에 §3.1 라인 0건

**Files:** `plans/79-semantic-routing-improvement.md`
**Scope:** S

---

### ~~B2~~ ✅ **완료(2026-08-27)** — §4 적용 판단 요약표의 logprob 행 갱신

**Description:** 요약표가 logprob confidence를 **"1순위 병행 — A/B/D의 전제"** 로 두고
*"Gemini logprobs 실측 선행"* 을 조건으로 단다. 둘 다 무효다 — 실측은 끝났고 결론은 이월이다.

**Acceptance:**
- [x] 우선순위 칸: "1순위 병행" → **"이월 (vLLM 전환 후)"**
- [x] 적용 칸에서 Gemini 조건 제거, **KBGenAI 원천 불가** 근거로 교체
- [x] "판정 눈금이 없다"는 §3.2 논지는 유지 — **그 공백이 지속된다는 사실**을 명시

**Files:** `plans/79-...md`
**Scope:** XS

---

### ~~B3~~ ✅ **완료(2026-08-27)** — §5 트랙 C 절 머리에 이월 배너 추가

**Description:** 트랙 C 절(C-0~C-4)이 **즉시 착수 가능한 트랙**으로 서술돼 있다
(*"A와 병행, D보다 선행"*). 절 전체를 삭제하지 않는다 — 재개 시 그대로 쓸 설계다.
**머리에 이월 배너만 달아 현재 상태를 명확히 한다.**

**Acceptance:**
- [x] 트랙 C 헤더 직후 배너: **이월 확정 · 사유(FabriX KBGenAI = logprobs 원천 불가) ·
      재개 조건(vLLM 전환 또는 라우터 평면 이동)**
- [x] "A와 병행 · D보다 선행" 순서 서술을 **재개 시점 기준**으로 한정
- [x] C-0~C-4 **본문 설계는 보존**(재개 시 재사용)
- [x] C-1은 v11에서 이미 정정됨 — **중복 배너 금지**

**Files:** `plans/79-...md`
**Scope:** S

---

### ~~B4~~ ✅ **완료(2026-08-27)** — §6.2 설정 블록에 이월 표시

**Description:** 설정 6종이 한 블록에 나열돼 **전부 이번에 도입되는 것처럼** 보인다.
실제로는 `ROUTER_CONFIDENCE_SOURCE`·`ROUTER_TOP_LOGPROBS`·`ROUTER_LABEL_FIRST_FORMAT`·
`ROUTER_MIN_CONFIDENCE`·`ROUTER_CLARIFY_ON_LOW_CONFIDENCE`가 **전부 트랙 C 종속**이고,
`ROUTER_TWO_STAGE_ENABLED`는 트랙 B(이월 종속)다. **트랙 A는 플래그를 신설하지 않았다**(SPEC Q1).

**Acceptance:**
- [x] 설정을 **「트랙 A — 신설 0건」 / 「트랙 C 이월분」 / 「트랙 B 이월분」** 으로 분류
- [x] 트랙 A가 **플래그 없이** 적용됐다는 사실 명시(SPEC Q1 · 회귀 테스트로 고정)
- [x] 플래그 규율(기동 시 1회 해석 · P14) 유지

**Files:** `plans/79-...md`
**Scope:** XS

---

### ~~B5~~ ✅ **완료(2026-08-27)** — §6.3 착수 순서 전면 개정 — 전제가 무효다

**Description:** 현행 착수 순서의 **전제가 "C-0(라벨 선행 구조) + A-1을 함께 적용"** 인데,
실제로는 **A-1만 적용됐고 C-0은 이월**됐다. 1단계·2단계도 트랙 C 진행을 가정한다.
**`plans/80` §5의 통합 타임라인과 정합**시킨다.

**Acceptance:**
- [x] 전제를 **"A-1 단독 적용(승인된 예외 · SPEC Q1)"** 으로 교체 — C-0 제거
- [x] 1·2단계(C-1·C-2·C-3·C-4)를 **이월 축**으로 이동
- [x] 현재 착수 가능한 것만 앞에 남긴다: **트랙 A 마감(S-1~S-3)** · **A-6**(사용자 결정 후)
- [x] 트랙 B는 이월 축(트랙 C 종속)
- [x] **`plans/80` §5를 정본으로 참조**하고 79는 자기 트랙 내부 순서만 기술

**Verify:**
- [x] `plans/80` §5와 순서 모순 0건 (수동 대조)

**Files:** `plans/79-...md`
**Scope:** S

---

### ~~B6~~ ✅ **완료(2026-08-27)** — 과금 게이트 문구의 Gemini → FabriX 정정 (2곳)

**Description:** §6.4와 §7이 *"**Gemini** 실호출 벤치마크는 D-127 건별 승인"* 이라고 적는다.
운영이 FabriX이므로 대상이 틀렸다. **D-127 규율 자체는 그대로 유효**하다.

**Acceptance:**
- [x] 2곳 모두 "Gemini 실호출" → **"실 LLM(FabriX) 호출"**
- [x] 건별 승인 + `RUN_E2E=1` 규율 문구는 **변경 없음**

**Verify:**
- [x] `grep -c "Gemini 실호출" plans/79-*.md` = 0

**Files:** `plans/79-...md`
**Scope:** XS

---

## Phase C — 트랙 A 마감 (실행 · `plans/80` §3.3)

> A-1이 코드 게이트를 **처음 실동작**시켰다. 승인된 예외이나(SPEC Q1) 영향을 측정·고정해야 한다.

### ~~C1~~ ✅ **완료(2026-08-31)** — 골든셋 회귀 · 멀티 DB 케이스 포함

> **결과**: 13/13 통과 · intent_match 13/13 · **멀티 DB 3/3 보존**(불변식 ⑩ 위배 없음) · score 분포 `0.8~1.0`×16 / `0.5~0.8`×1 · **below_gate = 0** · errors 0. 산출: `reports/routing_s1_s2_gemini_20260831.json`
> ⚠ **운영 기준선이 아니다** — `LLM_FABRIX_BASE_URL`이 비어 있어 `provider=gemini`(개발값)로 측정됐다. 운영은 FabriX(KBGenAI)이므로 절대 수치는 이 값으로 인용하지 않는다.

**Description:** A-1로 후보 분포가 바뀌었다. 79 §1.1 불변식(멀티 DB 선택 제공)의 위배 형태는
*"임계 때문에 2·3순위 DB가 잘려 조용히 단일 선택으로 축소"* 이므로, **멀티 DB 케이스를
회귀 스위트에 명시 포함**해 돌린다.

**Acceptance:**
- [ ] **실행 전 사용자 승인**을 받고 `RUN_E2E=1` 설정(D-127 — 승인 없이 금지)
- [ ] 멀티 DB 골든셋 케이스가 **단일 선택으로 축소되지 않음**
- [ ] `sub_query_context` DB별 분리 유지
- [ ] 축소 발생 시 **회귀로 판정**하고 A-1 롤백 여부를 사용자에게 보고

**Verify:**
- [ ] 회귀 결과를 `plans/80` §3.3 S-1 옆에 실측 기록으로 첨부

**Files:** 골든셋·회귀 스크립트(`scripts/eval_text2sql.py` 경로 재사용)
**Scope:** M · **게이트: D-127 사용자 승인**

---

### ~~C2~~ ✅ **완료(2026-08-31)** — `relevance_score` 분포 실측

> **★ 핵심 발견 — `below_gate = 0`**. A-1(규칙 5 제거)이 *구조적 차단*은 풀었지만 모델이
> 실제로 0.3 미만을 내지 않아 **게이트는 여전히 실질 no-op**이다. 79 트랙 A 수용 기준
> (*"0.3 미만 출력이 관측된다"*)은 **미충족**이며, 이는 §3.5① 가설의 *부분* 확증이다 —
> 차단 원인이 프롬프트 규칙이 아니라 **모델의 자기보고 성향**이었다.
> **C-4 판정: `0.3` 유지 + 잠정값 표기 유지.** ⚠ **운영 기준선이 아니다** — `LLM_FABRIX_BASE_URL`이 비어 있어 `provider=gemini`(개발값)로 측정됐다. 운영은 FabriX(KBGenAI)이므로 절대 수치는 이 값으로 인용하지 않는다.

**Description:** 79 트랙 A 수용 기준 첫 줄이 *"A-1 적용 후 분포가 실제로 넓어진다
(0.3 미만 출력이 관측된다)"* 를 요구한다. §3.2/§3.5①의 "게이트 no-op" 가설을
**데이터로 확증/반증**하는 항목이다.

> **★ D-174 ② — 이 항목의 지위가 올라갔다.** 종전에는 *"라우터 평면 이동 시 C-4 임계 재설계의
> 출발점"* 이었다. 그런데 평면 운영 정책 확정(vLLM=deep agents 전용 · 라우터는 FabriX)으로
> **C-4가 오지 않는다** — logprob 신뢰도가 정책상 현재 불가이기 때문이다. 그대로 두면
> **근거 없는 임계 `0.3`이 무기한 실동작**한다.
>
> → **S-2가 「C-4의 재료」가 아니라 「임계 정산 근거」 그 자체**가 된다. 자기보고 스케일이라도
> **분포를 실측하면 0.3이 어디에 놓이는지**는 판정할 수 있다 — *logprob이 아니면 정산 불가*는
> 과한 전제였다. logprob은 눈금을 **더 낫게** 만들지만, 자기보고 분포만으로도
> **지금 값이 틀렸는지**는 말할 수 있다.

**Acceptance:**
- [ ] **실행 전 사용자 승인** + `RUN_E2E=1`
- [ ] A-1 전후 `relevance_score` 분포 대조표 (대역별 카운트)
- [ ] **0.3 미만 출력이 실제로 관측되는지** 확정
- [ ] 후보 수·토큰·지연 증가분 계측(79 트랙 A 수용 기준)
- [ ] 결과를 `plans/79`에 실측으로 등재 (자기보고 스케일임을 명기)

**Acceptance — 임계 정산 (D-174 ② 신설):**
- [ ] **후보 절단률** — 0.3 게이트가 실제로 몇 %를 잘라내는가(0%면 §3.5① "게이트 no-op" 확증)
- [ ] **절단된 후보의 유용성** — 잘린 DB가 골든셋 정답에 포함된 사례가 있는가
      (있으면 임계가 **너무 높다**는 직접 증거)
- [ ] ★ **멀티 DB 축소 발생 여부** — 임계 때문에 다중 선택이 단일로 줄어든 사례
      (79 §1.1 **불변식 ⑩ 위배** 여부 · 이것이 가장 무거운 판정 기준이다)
- [ ] 위 셋으로 **`0.3`을 유지/조정/제거 중 하나로 판정**하고 근거를 `plans/79`에 등재
- [ ] 판정 결과에 **모델명·측정 시점을 병기** — 자기보고 스케일은 모델 교체 시 달라진다
- [ ] `MIN_RELEVANCE_SCORE` 주석의 "잠정값" 문구를 **정산 결과로 갱신**(값 변경은 별도 승인)

**Files:** `plans/79-...md`(결과 등재) · `src/routing/semantic_router.py`(주석 갱신)
**Scope:** M · **게이트: D-127 사용자 승인**

---

### ~~C3~~ ✅ **완료** — `WU-01`로 이행 (2026-08-28 실측 확인) — 임계 `0.3`이 **잠정값**임을 코드·문서에 고정

> **실측 근거**(2026-08-28): `src/routing/semantic_router.py`의 `MIN_RELEVANCE_SCORE = 0.3` 정의부
> 바로 위에 잠정값 주석이 있고 *"값을 임의로 조정하지 않는다. 조정하려면 relevance_score 분포
> 실측(WU-06)이 먼저다"* 와 **`subagents.py:167`과의 대칭 적용 의무**까지 명시돼 있다.
> 아래 체크박스는 `plans/80` §5.2 WU-01 완료 시점에 이행된 것이라 미체크로 남아 있었다.

**Description:** 79 C-4는 *"임계는 측정된 calibration 곡선에서 도출한다"* 고 했으나 그 곡선을
만들 신뢰도 소스가 없다. **0.3은 자기보고 스케일 시절의 관성값**이며, 지금은 그 값으로
후보를 **실제로 잘라내고 있다**. 근거 없는 상수가 근거 있는 상수로 오인되는 것을 막는다.

**Acceptance:**
- [x] `MIN_RELEVANCE_SCORE` 정의부에 주석 — 잠정값·재도출 조건(WU-06 선행)·**양쪽 대칭 적용 의무**까지 명시
- [x] `plans/79` §8 ⑧에 같은 취지 1줄
- [x] **값은 변경하지 않는다** — `0.3` 그대로(SPEC 「Ask first」 준수)

**Verify:**
- [x] 회귀 무영향 (주석만이므로 동작 변화 0) — 2026-08-28 전체 회귀로 확인

**Files:** `src/routing/semantic_router.py`(주석) · `plans/79-...md`
**Scope:** XS · ⚠ 병렬 세션 소유 파일 — 착수 전 `git status` 재확인

---

### ~~C4~~: 계약 C-A(옵트인 클래스 3자리 조건부)를 테스트로 고정 — ✅ **완료(2026-08-27 확인)**

> **이미 충족돼 있었다.** 79 트랙 A 구현 중 신설된 `test_router_prompt_structure.py`의
> `test_off_prompt_never_mentions_fault_diagnosis`가 `"fault_diagnosis" not in off`를 단언해
> **정의 줄·판단 절·예시 세 자리를 한 번에** 덮는다(문자열이 하나라도 남으면 실패).
> `test_on_prompt_defines_fault_diagnosis_class`가 on 경로를 덮는다. **추가 작업 없음.**

**Description:** `fault_diagnosis`는 **78이 켜는 플래그**에 종속된 조건부 블록이고, 정의 줄·
판단 절·예시 **세 자리 모두** 조건부여야 한다. 현재 상태는 계약을 충족하지만
**C-0(형식 변경) 착수 시 깨지기 쉽다** — 조건부 블록 안의 예시를 갱신에서 놓치기 때문이다.
병렬 세션이 만든 `test_router_prompt_structure.py`에 불변식을 추가한다.

**Acceptance:**
- [x] 플래그 **off** 시 프롬프트에 `fault_diagnosis` 문자열이 **0건**(정의 줄·절·예시 전부)
- [x] 플래그 **on** 시 세 자리가 **모두** 등장
- [x] 프롬프트 **모듈 전체**를 대상으로 검사(기본 프롬프트만 보지 않는다)
- [x] 테스트에 계약 출처 주석(`plans/80` C-A)

**Verify:**
- [x] `pytest tests/test_semantic_routing/test_router_prompt_structure.py -v` 통과
- [x] `pytest -q` 무회귀

**Files:** `tests/test_semantic_routing/test_router_prompt_structure.py`
**Scope:** S · ⚠ 병렬 세션 소유 파일 — 착수 전 재확인

---

## Phase D — `plans/80` 자체 갱신

### ~~D1~~ ✅ **완료(2026-08-27)** — §7.1·§7.2 정정 지시표에 적용 상태 반영

**Description:** 두 표가 "무엇을 고쳐야 하는가"만 담고 **무엇이 적용됐는지**를 구분하지 않는다.
Phase A·B 진행에 따라 상태가 갱신되어야 추적이 된다.

**Acceptance:**
- [x] 각 행에 **적용 상태 컬럼**(적용 완료 / 미적용 / 철회) 추가
- [x] Phase A·B 항목과 **1:1 대응**(A1~A5 · B1~B6 번호 병기)
- [x] 철회 항목(구 S-4 `synonym_registration`)이 철회로 표시

**Files:** `plans/80-78-79-joint-execution-contract.md`
**Scope:** XS

---

### ~~D2~~: D-170 본문 등재 — ✅ **완료(2026-08-27)** · D-169도 함께 등재

> `## D-169`·`## D-170` 헤더 · 「변경 이력」 2행 · 「채번 이력」 상태 전환 · 안내 라인 최댓값 갱신(D-162 → **D-170**, 다음 가용 D-171) 전부 반영. D-163~D-168(Plan 78)은 **예약 유지**.

**Description:** D-170은 현재 **채번 이력 표에만 예약**돼 있다. 계약이 실제 적용되는
시점(Phase C 완료 또는 78 착수)에 `docs/02_decision.md`에 본문 섹션으로 등재한다.

**Acceptance:**
- [x] `## D-170` 헤더 1건 (결정일/상태/결정/근거/구현/주의/관련 형식)
- [x] 「변경 이력」 표 1행
- [x] 채번 안내 라인에서 D-170을 **예약 목록에서 제거**하고 최댓값 갱신
- [x] 채번 이력 표의 D-170 행을 **"채번 완료"** 로 전환

**Verify:**
- [x] `grep -c "^## D-170" docs/02_decision.md` = 1
- [x] 세 소스(헤더·변경 이력·채번 이력) grep 최댓값 일치

**Files:** `docs/02_decision.md`
**Scope:** S
**Depends:** Phase C 완료

---

## Phase E — 사용자 결정 대기 (작업 아님 · 게이트)

> 아래 셋은 **코드로 풀 수 없다.** 결정이 나야 해당 작업이 열린다.

| # | 결정 사항 | 막고 있는 것 | 출처 |
|---|---|---|---|
| **E1** | **호스트 인가 모델** — (a) `allowed_db_ids`+role 조합 (b) 호스트 그룹/태그 별도 인가 목록 | 78 W3-5 · **조사 플래그 해제 자체** | 78 R-9 |
| **E2** | **`unknown` ↔ `general_inference` 의미 매핑** — 논문의 "분류 불가"와 이 프로젝트의 "DB 미접근 응답"은 다르다 | 79 A-6 | 79 §8 ① · SPEC Q3 |
| **E3** | **라우터의 LLM 평면 배치** — 워커(FabriX · logprobs 불가) 유지 vs **제어 평면(vLLM)으로 이동** | **트랙 C 전체**(C-0~C-4) · 계약 C-C의 신뢰도 축 | 79 §8 ⑪ |

**E3 부기**: 트랙 C 재개의 **최단 경로**다. D-037 평면 배치를 건드리는 별도 결정이므로
지연·비용·폐쇄망 서빙 부하를 함께 판단해야 한다. **E3 미결정이 길어져도 78은 진행한다**
(`plans/80` §5 순서 계약 ② — 78은 트랙 C를 기다리지 않는다).

---

## 완료 기록 — 2026-08-26 검토에서 **이미 적용된** 정정

> 재작업 방지용. 아래는 다시 하지 않는다.

| 대상 | 내용 | 반영 |
|---|---|---|
| `plans/78` | §4.1 그림·§4.2 표·§4.3 절·§8.1·R-1의 **v6 어댑터 폐기 미반영분** | v9 |
| `plans/78` | **W3 하위 번호 드리프트 통일**(구 W3-6→W3-5 / 구 W3-7→W3-2·3 / R-4의 구 W3-5→W3-4) | v9 |
| `plans/78` | W1 생산자·소비자 집합 `host_diagnostics`→`fault_diagnosis` · W6 라우팅 지표 · §3.2.1 · 잔존 플래그명 2곳 | v9 |
| `plans/78` | §8.4를 근거 문서로 강등 + 트랙 C 이월 영향 2건(W3-2 단독 출발 · 감사 스키마 소유권 이관) | v9 |
| `plans/79` | 운영 LLM **Gemini→FabriX**(§3 표·§3.1②·§8 ②) | v10(병렬) |
| `plans/79` | **§3.5③ `synonym_registration` 지적 철회** — 결정적 라우팅 대상이라 프롬프트 부재가 정상 | v10·v11 |
| `plans/79` | A-3 **옵트인 클래스 3자리 조건부 제약**(계약 C-A) · A-5 적용 방식 실측 | v11 |
| `plans/79` | §6.4·§8⑧ — A-1 단독 적용을 **승인된 예외**로 재작성 · C-1 이월 결론 | v11 |
| `plans/79` | **§8 ⑪ 신설**(라우터 평면 배치) · §9 강등 · §10 중복 헤더 제거 · v10/v11 번호 충돌 해소 | v11 |
| `plans/80` | 신규 작성 → **v2 정정**(A-1 BLOCKER 판정 철회 · 트랙 C 이월 · 계약 C-B 뒤집기 · C-C 조건부화 · §5 전면 개정) | v2 |
| `plans/INDEX.md` | 80 등재 · 78/79 상태 갱신 · 전건 85 / 상태표기 26 | — |
| `docs/02_decision.md` | **D-170 예약 등재**(안내 라인 + 채번 이력 표) | — |

---

## 착수 순서 요약

```
지금        ▸ Phase A (78 잔여 정정 5건)        — 충돌 없음 · 전부 XS
            ▸ Phase D1 (80 정정 지시표 상태 컬럼)

병렬 확인 후 ▸ Phase B (79 잔여 정정 6건)        — ⚠ 소유권 재확인 필수
            ▸ Phase C3·C4 (임계 주석 · C-A 테스트) — ⚠ 소유권 재확인 필수

승인 후     ▸ Phase C1·C2 (골든셋 회귀 · 분포 실측) — ⚠ D-127 건별 승인

결정 후     ▸ Phase E1 → 78 W3-5 열림
            ▸ Phase E2 → 79 A-6 열림
            ▸ Phase E3 → 79 트랙 C 재개 · 계약 C-C 발효

78 착수 시  ▸ Phase D2 (D-170 본문 등재)
            ▸ `plans/80` §5 Phase 1부터 (W0 → W1 → W2 → W3-1·4)
```
