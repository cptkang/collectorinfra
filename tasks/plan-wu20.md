# Plan: WU-20 — 미들웨어 OS 근사 식별 (78 W7-1)

> Spec: `SPEC-middleware-os-identification.md` · 상위: `plans/80` §5.2 자유 축
> **게이트 없음** — 실 LLM 0 · 실 호스트 접근 0 · 신규 의존 0

## 구성요소와 의존

```
① config/middleware_signatures.yaml   선언 규칙(데이터)
        │
        ▼
② src/domain/middleware.py            결정적 매처 + 결과 모델
        │
        ▼
③ docs/24_middleware_profile_spec.md  sre_agent 요구 명세 + 선행 실측 문서화
```

**순차로 간다.** ②는 ①의 스키마를 알아야 하고, ③은 ②가 실제로 무엇을 요구하는지 확정된 뒤에
써야 정확하다(요구 명세가 구현보다 앞서면 추측이 섞인다).

## 구현 순서 (TDD)

| # | 작업 | 검증 |
|---|---|---|
| 1 | 판정 계약 테스트 작성 (S1~S5) — **현행에서 실패** | `pytest tests/test_middleware/ -v` |
| 2 | 선언 파일 스키마 + 기본 규칙 세트 | 로딩·스키마 검증 통과 |
| 3 | 결정적 매처 구현 | S1·S2·S4 통과 |
| 4 | 하드코딩 0 · 선언적 확장 실증 | **S3·S5** 통과 |
| 5 | 요구 명세 문서 + 선행 실측 2항 | S6·S7 |
| 6 | 전체 회귀 + `arch_check --ci` | S8·S9 |

## 리스크

| # | 리스크 | 완화 |
|---|---|---|
| R1 | 규칙이 코드로 새어 들어간다 | **S3를 테스트로 강제** — 소스에 미들웨어명 문자열 0건을 grep 단언 |
| R2 | 기본 규칙 세트가 조직 실제와 다르다 | **S5(선언적 확장)** 가 성립하면 규칙 교체가 데이터 변경으로 끝난다 — 착수를 막지 않는다 |
| R3 | `ps` 출력 형식이 배포판마다 다르다 | cmdline **부분 문자열·정규식** 매칭으로 두고 컬럼 위치에 의존하지 않는다 |
| R4 | 오탐(예: `grep tomcat`이 tomcat으로 잡힘) | **제외 패턴**을 규칙에 두고 테스트로 고정 |
| R5 | `sre_agent` 경계 침범 | **import 0 단언**(D-118) + `arch_check --ci` |
