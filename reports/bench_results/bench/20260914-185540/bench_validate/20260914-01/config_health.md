# 설정 건강 보고서 — 20260914-01

> 생성 2026-09-14T17:52:09 · 대상 **335필드** · plans/93 트랙 T(L1~L4 · 실 LLM 미호출)

## 1. 즉시 조치

없음 — 고아 키·가림 키·기동 실패가 모두 0이다.

## 2. 카탈로그 정합 (L1)

| 유형 | 건수 | 뜻 |
|---|---:|---|
| orphan | 0 | 파일에만 있고 코드가 읽지 않는다 |
| missing_example | 0 | 코드에는 있는데 `.env.example`이 알려주지 않는다 |
| undocumented | 69 | 설명이 없다(웹UI 도움말·주석의 원천) |
| encenv_shadow_candidate | 9 | `.encenv`가 관리 — `.env` 수정이 무효일 수 있다 |

## 3. 주입 실효성 (L3) — "바꿨는데 왜 안 바뀌지"

가림 0건 — 주입한 값이 전부 그대로 읽혔다.

## 4. 기동 안전성 (L2)

시도 0건 · 거부 0건

## 5. 소비 실증 (L4)

변함 0 · 불변 0 · 판정 보류 0

> **불변 = 삭제해도 된다가 아니다.** 이 층이 보는 것은 설정 객체의 변화이고,
> 그 값을 읽는 코드가 있는지는 별개다(§3.5.4).

## 6. 등급 초안

| 등급 | 뜻 | 건수 |
|---|---|---:|
| A | 환경 결속 — 설치마다 정해야 한다 | 31 |
| B | 운영 레버 — 권고값과 함께 제공 | 144 |
| C | 내부 상수급 — 기본값 사용 | 160 |

> 최종 심판은 신규 설치 시나리오다(§6.8.1) — 빈 `.env` + A등급만으로 기동·질의가 되는가.

## 7. 커버리지 요약

- L1 정합: **335건 전수**
- L2 기동: 0건
- L3 주입: 0건
- L4 소비: 0건
- L5 성능: **0건** — 실 LLM이 필요한 스위프는 별도 실행이다

상세는 `coverage_ledger.md`(전건 한 줄씩).

> **실행 모드**: --quick(L1·L3)
> **대상**: 148건 / 전체 335건
