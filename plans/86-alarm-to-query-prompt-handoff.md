# 86. 알람 → 질의 프롬프트 인계 — 카드에서 클릭, 질의응답에서 조회

> **작성일**: 2026-08-31
> **성격**: 구현 계획(implementation-ready) · **상태: 구현 완료(2026-08-31 · D-192 등재)** —
> SDD 산출물 `CAPABILITY-MAP-86.md` · `SPEC-alarm-prompt-builder.md` · `SPEC-alarm-prompt-ui.md` ·
> `SPEC-alarm-prompt-llm-suggest.md` · `tasks/plan-86.md` · `tasks/todo-86.md`.
> 신규 테스트 25건 · `tests/test_api` 288 passed · arch/overfit 0 · 실 LLM 호출 0.
> **잔여**: T7(답변 가능성 실측 — 과금 승인 필요) · 실 브라우저 렌더 확인
> **요청 취지(사용자 지시 원문)**: *"이벤트 알람에서 나타난 내용을 바로 프롬프트로 조회할 수 있는
> 기능을 추가하라. 예를 들어 `[심각] fgtsidd0 -- Memory사용률 (메모리)` 라고 알림이 보여졌을 때
> 'fgisidd0'를 클릭하면 해당 이벤트 내용을 파악하여 `fgisidd0 서버의 1달 평균 메모리 사용률과
> 증가율을 보여줘` 라는 프롬프트를 생성하여 '질의응답' 페이지에서 조회를 자동으로 할 수 있는
> 기능을 추가한다. 프롬프트는 '권고 조치'나 '패턴 분석'에서 해당 이벤트를 분석할 수 있는
> 프롬프트를 추천하여 프롬프트를 생성하도록 해야 한다."*
> **관련 결정**: D-182(알람 뷰 — 이 기능이 얹히는 토대) · **D-183**(질의 이력 — *"재사용은 입력창에
> 채우기만, 전송하지 않는다"* — **본 요청과 정면으로 긴장한다, §5.1**) · D-190(알람 레벨 필터·찾아보기) ·
> D-176 후속2(급증 SQL 결정적 조립 — 생성 프롬프트의 답변 가능성을 좌우, §4.3) · D-089(DB별 특화 로직 격리) ·
> D-129(설정 SSOT)
> **신규 결정 예약**: **D-192**(알람 → 질의 프롬프트 인계) — `docs/02_decision.md` 「채번 이력」 표에
> 예약 등재 완료(2026-08-31). 계획서에만 적은 예약은 효력이 없다(D-161 부기).
> ※ 채번 실측 2026-08-31 — `## D-` 헤더 최댓값 **191** · 「변경 이력」 표 최댓값 **191** ·
> 「채번 이력」 표 확인(D-105·115·134·158·163~168·176 미등재 예약, 재사용 금지) → **192**.
> ⚠ 최초 작성 시 191로 잡았으나, **같은 날 병렬 작업(설정 설명 패널)이 D-191을 본문 등재**해
> 등재 직전 재실측으로 192로 옮겼다 — 채번은 반드시 **쓰기 직전**에 다시 확인한다(D-161).
> **★ 사용자 확정(2026-08-31)**: G-1=**서버명 + 하단 추천 칩**(둘 다) · G-2=**확인 후 전송**(안 C —
> §3.3의 권고는 B였으나 사용자가 C를 택했다. 그 결과 **D-183과 충돌하지 않는다**) ·
> G-3=**검증된 형태만 추천**(1+2 병행) · G-4=**Phase 1 + 2**(LLM 추천 포함).
> §3.3·§5.1·§5.2의 "자동 전송" 서술은 **확정 전 검토 기록**이며 현행 결정이 아니다.
> **실측 기준**: 아래 모든 `file:line`·값은 2026-08-31 현 브랜치(`multiintent`)에서 직접 확인했다.
> **갱신 이력**: 2026-08-31 작성·구현 완료 · **2026-09-03 본문을 구현 결과로 갱신**
> (§3.3·§3.4 확정 결과 · §5.1·§5.2 개정 · §6 실행 태스크 재편 · §7 확정 결과 · §8 검증 결과 · §10 산출물 신설).
> 계획 시점 서술은 지우지 않고 **검토 기록**으로 표시해 남긴다 — 왜 그 선택이 아니었는지가
> 나중에 다시 물어질 지점이다.

---

## 0. 요약 — 이 계획이 실제로 푸는 문제

요청은 "클릭하면 프롬프트가 생성되어 자동 조회된다" 한 줄이지만, 실측하면 **난이도가 서로 다른 세
조각**으로 갈라진다.

| 조각 | 난이도 | 근거 |
|---|---|---|
| **① 카드 → 질의응답 인계(배선)** | 낮음 | 필요한 것이 전부 이미 있다 — `setActiveView("chat")`(`app.js:2758`) · `promptEl` · `handleSend()`(`app.js:655`). 알람 카드의 closure 바인딩 패턴도 ack·피드백이 선례다(`bindAlarmFeedback`, `app.js:3211`) |
| **② 프롬프트 생성(무엇을 물을 것인가)** | **중간** | 재료는 SSE payload에 이미 전부 실려 있다(§4.1). 문제는 *생성 방식* — 결정적 템플릿이냐 LLM 추천이냐 |
| **③ 생성한 질의를 파이프라인이 실제로 답하는가** | **높음 — 이 계획의 진짜 리스크** | 사용자 예시의 *"1달 평균 메모리 사용률"* 은 답한다(골드셋에 있다). **"증가율"은 현재 결정적 경로가 없다** — 급증 조립기가 **파일시스템 전용·월 단위·절대 임계 명시 필수**다(§4.3) |

**따라서 이 계획의 중심은 UI가 아니라 ③이다.** 클릭 한 번에 파이프라인이 못 푸는 질의를 자동
전송하면, 사용자는 "기능이 됐다"가 아니라 **"에이전트가 내 알람에 답을 못 한다"**를 경험한다.
그래서 §3의 설계는 **추천 프롬프트를 "파이프라인이 답할 수 있다고 확인된 형태"로 제한**하는 데
대부분을 쓴다.

> **★ 구현 후 확인(2026-08-31)**: 이 판단은 맞았다. ①배선은 예상대로 하루 안에 끝났고
> (기존 함수 재사용) ②난점은 전부 ③에 몰렸다 — 축 매핑표의 문구 하나하나를 골드셋과 대조하는
> 작업이 실제 시간의 대부분이었다. **`server.Network` 축은 대조에 실패해 비워 둔 채로 남겼다**
> (T7 대기). 다만 예상하지 못한 비용이 두 군데 있었다: 신규 설정 키 하나가 설정 카탈로그
> 3계약을 동시에 깨뜨린 것(§6)과, `escapeHtml`이 따옴표를 이스케이프하지 않아 칩 구현 방식을
> 바꾼 것(§6).

---

## 1. 요청 해석 — 갈라지는 지점 2곳

### 1.1 "클릭"의 대상이 무엇인가

사용자 문장에는 두 개가 함께 들어 있다.

- (a) *"'fgisidd0'를 **클릭**하면"* → **카드 헤더의 서버명**을 누르는 것
- (b) *"'권고 조치'나 '패턴 분석'에서 … 프롬프트를 **추천**하여"* → 분석 결과를 근거로 한 **추천 목록**

(a)만 만들면 "서버명 클릭 = 고정 템플릿 1개"가 되어 (b)의 *추천*이 사라지고, (b)만 만들면 예시로 든
서버명 클릭이 안 된다. **둘은 배타적이지 않다** — (a)는 진입, (b)는 선택지다. 본 계획은 둘 다
넣되 역할을 나눈다(§3.2, G-1로 확정 요청).

### 1.2 "자동으로 조회" — 어디까지가 자동인가

**D-183이 정면으로 반대 방향을 확정해 두었다**:

> *"목록 클릭은 입력창에 **채우기만** 한다 — 전송하지 않는다. 옛 질의가 지금도 유효하다는 보장이
> 없고(존 개명·서버 폐기), **오전송은 되돌릴 수 없다**."* (`docs/02_decision.md` D-183)

본 요청은 명시적으로 *"조회를 자동으로"* 를 요구하므로 **기존 결정과 충돌한다**. 다만 두 상황의
성질은 다르다 — 아래 표가 §5.1의 근거다.

| | D-183(질의 이력 재사용) | 본 요청(알람 인계) |
|---|---|---|
| 질의의 나이 | 며칠~몇 주 전 | **방금 도착한 알람** |
| 대상 유효성 | 존 개명·서버 폐기로 무효화 가능 | 알람이 방금 왔다 = **그 서버는 지금 살아 있다** |
| 사용자 의도 | "이런 걸 물었었지" 회고 | **"이 알람을 지금 파악하겠다"** |
| 문구의 출처 | 사용자가 과거에 쓴 문장 | **시스템이 방금 생성한 문장** |

마지막 행이 중요하다 — **시스템이 만든 문장을 사용자가 읽지도 않고 전송**하는 것이 자동 조회다.
그래서 §3.3은 "자동 전송하되 **보내기 전에 무엇을 보내는지 보이고 되돌릴 수 있게** 한다"로 설계했다.

> **★ 확정(G-2, 2026-08-31)**: 사용자는 한 걸음 더 보수적인 쪽을 택했다 — **보여주고 확인을 받은
> 뒤에 보낸다**(안 C). 위 표의 분석은 유효하지만 결론은 바뀌었고, 그 덕에 D-183과의 긴장 자체가
> 사라졌다(§5.1). 이 표를 남겨 두는 이유는 **알람 경로에만 확인 바가 붙는 근거**가 여기 있기
> 때문이다 — "문구의 출처가 사용자가 아니다"라는 마지막 행.

---

## 2. 현재 상태 실측

### 2.1 알람 카드가 실제로 들고 있는 필드

SSE 티어 payload 정본 — `noise_gate/application/nodes/alarm_notifier.py:586~617` (`_tier_sse_payload`),
incident open payload는 `:707~750`에서 같은 필드 집합에 식별자만 더한다.

| 분류 | 필드 | 프롬프트 생성에서의 쓰임 |
|---|---|---|
| 대상 식별 | `hostname` · `server_name` · `server_identity{name,ip_address,os_type,zone_label,…}` · `db_id` · `ip_address` | **질의의 주어**. 등록명 우선(`server_identity.name` → `server_name` → `hostname`)은 카드 헤더와 같은 규칙(`app.js:3021`) |
| 알람 정체 | `alarm_name`(자유 한국어) · **`resource_type`** · `resource_name` · `severity_label` · `alarm_time` · `alarm_status` | **자원 축 판정의 키는 `resource_type`**(§2.2) |
| 분석 결과 | `summary` · `probable_cause` · **`recommended_action`** · **`pattern_type`** · `is_routine` · **`pattern_analysis`** · `pre_classification` | 사용자가 지목한 추천 근거 2종이 여기 있다 |

**⚠ 경로별 비대칭 1건**: `history_stats`(패턴 근거 표)와 `process_snapshot`은 **SSE 티어 payload에
없다**. 프론트는 `data.history_stats`를 렌더하고 있고(`app.js:3117`), 이 값은 API 경로
(`src/api/routes/alarm.py:304`·`:624`)에서만 채워진다. **추천 로직이 `history_stats`에 의존하면
경로에 따라 조용히 빈 추천이 나온다** — §3.1은 이 필드에 의존하지 않는다.

### 2.2 자원 축 판정 키 — `resource_type`이 유일하게 구조화돼 있다

실측 분포(`noise_gate/tests/` 전수 grep):

```
server.Cpus(33)  server.Server(15)  server.Disks(13)  server.LogMonitor(10)
server.Memory(2)  server.Network(1)  server.FileSystems(spike_sql.py)  network.NMSNode(2)
management.MonitorGroup(1)  platform.server(2)  host.cpu(2)
```

`alarm_name`은 자유 문자열이다 — `"CPU 임계 초과"` · `"CPU 사용률"` · `"디스크 사용률"` ·
`"메모리 사용률"` · `"로그 패턴 감지"` · `"내부Cloud ○○○님의 Cloud PC 사양변경 승인바랍니다"`.
**단독 분류 키로 쓰면 안 된다**(마지막 사례가 이유다). `resource_type` 1차 · `alarm_name` 보조.

사용자 예시 `[심각] fgtsidd0 -- Memory사용률 (메모리)`의 `(메모리)`는 `resource_name`,
`Memory사용률`은 `alarm_name`, 축 판정은 `resource_type == "server.Memory"`가 한다.

### 2.3 질의응답 쪽 인계 지점

| 필요 | 존재 | 위치 |
|---|---|---|
| 탭 전환 | `setActiveView("chat")` | `app.js:2758` |
| 입력창 주입 | `promptEl.value` + `autoResizeTextarea()` | `reuseHistoryQuery`가 선례 — `app.js:2837` |
| 전송 | `handleSend()` — 버튼·Enter·웰컴 힌트·파일 업로드의 **단일 진입점** | `app.js:655`(D-183이 확인) |
| 카드 내 버튼 바인딩 | closure 캡처 · 인라인 `onclick` 미사용 | `bindAlarmFeedback`(`app.js:3211`) · `bindIncidentAck`(D-049) |

**신규로 만들 서버측 API는 없다.** 결정적 생성만 쓰면 이 기능은 전부 프론트에서 끝난다(§3.1).

### 2.4 기존에 없는 것

- 질의 추천·후속 질의 제안 기능: **0건**(`app.js` 전수 grep — 신규다)
- 알람 카드의 액션: `확인`(ack, D-049) · `유효/노이즈`(피드백, D-177) **둘뿐**

---

## 3. 설계

### 3.1 M1 — 프롬프트 생성기 (결정적 템플릿 우선)

**결정적 조립을 기본으로 한다.** 근거는 취향이 아니라 CLAUDE.md의 명문 원칙이다 —
*"LLM 분류·매핑 출력에 정합성을 의존하지 말 것. 스키마·조인이 고정된 쿼리는 코드가 직접 조립"*.
여기서는 SQL이 아니라 **질의문**을 조립하지만 실패 양식이 같다: LLM이 만든 추천 프롬프트는
파이프라인이 못 푸는 형태로 흘러가도 **아무도 그것을 검증하지 않는다**.

**축 매핑표**(`resource_type` → 추천 질의군). 각 항목은 §4.3의 "답할 수 있는가" 판정을 통과한
것만 넣는다.

| 축 (`resource_type`) | 추천 프롬프트 (초안) | 파이프라인 근거 |
|---|---|---|
| `server.Cpus` | `{서버} 서버의 지난 1개월 평균·최대 CPU 사용률을 보여줘` | 골드셋 `gp.yaml:214`·`:345` |
| `server.Memory` | `{서버} 서버의 지난 1개월 평균·최대 메모리 사용률을 보여줘` | 골드셋 `gp.yaml:214` |
| `server.Disks` / `server.FileSystems` | `{서버} 서버의 파일시스템별 사용률을 보여줘` / **급증형**: `{서버} 서버에서 지난달 대비 사용률이 10%p 이상 상승하고 80% 이상인 파일시스템을 보여줘` | 급증은 `spike_sql.py`가 결정적 조립 — **절대 임계·차분을 문구에 넣어야 진입한다**(§4.3) |
| `server.Network` | `{서버} 서버의 지난 1개월 네트워크 트래픽 통계를 보여줘` | `cmm_metric_stat_m` 계열 — **검증 필요(§6 T2)** |
| `server.LogMonitor` | `{서버} 서버의 최근 3개월 알람 발생 이력을 보여줘` | 골드셋 `gp.yaml:400` 인접 형태 |
| `server.Server`(가용성) | `{서버} 서버의 가용성 상태와 IP를 조회해줘` | 골드셋 `gp.yaml:177` **동일 형태** |
| 미매핑 / `management.*` · `platform.*` | **추천을 만들지 않는다**(칩 미표시) | 근거 없는 추천보다 없는 편이 낫다 |

공통 추가 축(자원과 무관하게 항상 1개):
- `{서버} 서버의 사양과 OS 정보를 조회해줘` — 골드셋 `gp.yaml:131` 동일 형태. **대상을 모를 때의
  안전한 첫 질의**다.

**`{서버}` 치환 규칙**: 카드 헤더와 동일하게 `server_identity.name` → `server_name` → `hostname`.
헤더가 보여준 이름과 프롬프트의 이름이 다르면 사용자는 다른 서버를 조회한 것으로 읽는다.

**⚠ 사용자 예시 문구를 그대로 쓰지 않는 이유**: *"1달 평균 메모리 사용률과 **증가율**"* 에서
증가율은 §4.3이 보이듯 **메모리에 대해서는 결정적 경로가 없다**. 예시를 문자 그대로 구현하면
첫 클릭부터 LLM 폴백으로 새는 질의가 나간다. G-3에서 확정을 요청한다.

**모듈 위치**: `src/static/js/app.js` 내 신규 함수군(`buildAlarmPrompts(data)`)으로 시작한다.
매핑표에 `server.Memory` 같은 **폴스타 리터럴**이 들어가는데, 이는 `overfit_check`의 공용 계층
스캔(`PUBLIC_LAYER_DIRS` = `src/utils`·`src/nodes`·`src/prompts`·…)에 걸리는 종류다. 다만 게이트는
`rglob("*.py")`만 훑으므로 **JS는 대상이 아니다**. 향후 서버측으로 옮긴다면 `src/db_adapters/polestar/`
(스캔 제외 목록) 또는 `config/` YAML에 두어야 한다(D-089 · §5.3).

### 3.2 M2 — 카드 UI (진입 2종)

**(a) 헤더 요소 클릭 — 사용자 예시 경로.** 카드 헤더의 서버명(`.alarm-server`)을 클릭 가능한
요소로 만든다. 클릭하면 **그 서버 축의 추천 목록을 펼친다**(즉시 전송하지 않는다 — §3.3).

- 접근성: `<span>`이 아니라 `<button class="alarm-prompt-trigger">`로 감싼다(키보드 도달·`aria-label`).
- 시각: 밑줄 점선 + hover 강조. **링크처럼 보이되 링크가 아님**을 유지(페이지 이동이 아니다).

**(b) 추천 프롬프트 칩 — 사용자가 지목한 "권고 조치·패턴 분석 기반" 경로.** 카드 하단
(`.alarm-feedback-section` **위**)에 `.alarm-prompt-section`을 둔다.

```
이 알람을 조회해 볼까요?
[ 1개월 메모리 사용률 ]  [ 서버 사양·OS ]  [ 최근 알람 이력 ]     ← 최대 3개
```

- **최대 3개**. 그 이상은 고르는 비용이 조회 비용을 넘는다.
- 칩 라벨은 **짧은 요지**, `title`(툴팁)에 **전송될 프롬프트 전문**을 넣는다 — 무엇이 나갈지
  보이지 않는 자동 전송은 만들지 않는다.
- `pattern_type`이 `"급증"`이면 급증형 추천을 **1순위**로 올린다(패턴 분석 반영).
- `is_routine === true`(일상적 반복)면 칩을 **접어 둔다** — 노이즈 캔슬링이 "일상"이라 판정한 알람에
  조회를 권하는 것은 게이트의 판단을 UI가 뒤집는 것이다.

### 3.3 M3 — 인계 정책 (D-183과의 조정)

> **⚠ 확정됨(2026-08-31 · G-2) → 안 C(확인 후 전송).** 아래 3안 비교와 "권고는 B"는
> **확정 전 검토 기록**이며 현행 결정이 아니다. 구현은 C를 따랐고, 그 결과 **D-183과
> 충돌하지 않는다**(§5.1 개정 참조). 검토를 지우지 않고 남기는 이유는 왜 B가 아니라
> C인지가 나중에 다시 물어질 지점이기 때문이다.

**(검토 기록) 3단 중 하나를 G-2로 확정 요청했다.**

| 안 | 동작 | 장점 | 단점 |
|---|---|---|---|
| **A. 채우기만** (D-183 그대로) | 탭 전환 + 입력창 채움 + 포커스. 사용자가 Enter | 기존 결정과 무모순 · 오전송 0 | 사용자 요구(*"자동으로 조회"*)를 **충족하지 못함** |
| **B. 자동 전송 + 되돌리기** *(권고)* | 탭 전환 + 채움 + **즉시 `handleSend()`**. 전송 직후 스트림 상단에 `이 질의는 알람 카드에서 자동 생성되었습니다 [중단]` 배너 | 요구 충족 · `stopStreaming()`이 이미 있다(`app.js:657`) | 과금 경로가 클릭 1회로 열린다 |
| **C. 확인 후 전송** | 채운 뒤 입력창 위에 `[이대로 조회] [수정]` | 오전송 0 + 요구 근사 | 클릭이 2회 — "자동"이 아니다 |

**권고는 B**다. 근거: ①`handleSend`는 이미 진행 중 재클릭 시 `stopStreaming()`으로 중단된다 —
**되돌릴 수 없다는 D-183의 전제가 이 경로에서는 성립하지 않는다** ②알람은 방금 도착했으므로
대상 유효성 문제(§1.2)가 없다 ③프롬프트는 §3.1의 **폐쇄된 템플릿 집합**에서만 나오므로 "무엇이
나갈지 모른다"가 아니다.

**B를 택할 경우 D-183을 개정하지 않는다** — 그 결정은 *질의 이력 재사용*에 대한 것이고, 본
경로는 별개 진입점이다.

**★ 확정 결과 — 안 C로 구현**(2026-08-31):

| 단계 | 동작 | 구현 |
|---|---|---|
| 1 | 칩/트리거 클릭 → 질의응답 탭 전환 + 입력창 채움 + 확인 바 표시 | `stageAlarmPrompt(text)` |
| 2 | 확인 바가 **전송될 전문을 그대로 보여준다** (`알람에서 생성` 배지 + 문장) | `showPromptConfirm(text)` · `#promptConfirm` |
| 3 | `[이대로 조회]` → `handleSend()` **1회** / `[수정]` → 바만 닫고 내용·포커스 유지 | `#promptConfirmRun` · `#promptConfirmEdit` |
| 4 | 진행 중(`isProcessing`)이면 조회 버튼 비활성 — 연타 = 중복 과금 | `showPromptConfirm` 내 `disabled` |
| 5 | 어느 경로로 보내든 확인 바는 `handleSend` 진입에서 닫힌다 | `hidePromptConfirm()` |

**자동 전송 경로는 만들지 않았다.** 계획 단계의 `runAlarmPrompt`(즉시 전송) 명칭도 쓰지 않는다 —
실제 함수는 `stageAlarmPrompt`(올려두기)이며, 이름 자체가 "전송하지 않는다"를 말한다.

### 3.4 M4 — LLM 추천 보강 (옵트인 · 후순위)

결정적 매핑이 커버하지 못하는 축(`management.*`·`platform.*`·미매핑 `alarm_name`)에 한해,
`recommended_action`·`pattern_analysis`를 근거로 LLM이 **질의 1개**를 제안한다.

- **기본 off**(`ALARM_PROMPT_LLM_SUGGEST_ENABLED=false`) — 신규 플래그 기본 off = 현행 비트 동일
  (`plans/80` §5.4-③).
- 켜져도 **자동 전송 금지**(안 A로 강등) — LLM 생성 문구는 폐쇄 집합이 아니다.
- 서버 라우트 신설이 필요하므로 **본 계획의 Phase 2**로 분리한다. Phase 1은 프론트 전용.

**★ 구현 결과**(2026-08-31 · G-4에서 Phase 1+2 동시 착수로 확정):

- `POST /api/v1/alarm/suggest-prompt` — 요청/응답 형식은 `CAPABILITY-MAP-86.md`의
  `AlarmPromptSuggestion`과 동일(`source: "llm"`으로 결정적 추천과 구별).
- 설정 키는 `NOISE_ALARM_PROMPT_LLM_SUGGEST_ENABLED`(**기본 `false`**) —
  계획 단계 가명 `ALARM_PROMPT_LLM_SUGGEST_ENABLED`가 아니라 `NoiseGateConfig` 접두를 따랐다.
- **호출 조건 3중 축소**: ①`capabilities.prompt_suggest_enabled`가 false면 요청 자체를 보내지 않는다
  (알람마다 503을 때리지 않기 위해 capabilities에 필드를 노출했다) ②결정적 추천이 **0건일 때만**
  ③확인 후 전송(자동 전송 경로가 애초에 없으므로 "안 A로 강등"이라는 계획 문구는 무의미해졌다).
- **실패는 전부 `200 + suggestion: null`** — 호출 실패·파싱 실패·`text` 누락·빈 질의 ·
  **대상 서버명 누락**. 추천이 없다고 알람 카드가 깨지면 안 된다.
- 존 RBAC은 피드백·ack와 같은 규약(`_assert_zone_access`) — 다른 존 알람 내용을 LLM에 실어
  보내지 못하게 한다.
- 뒤늦게 도착한 추천은 `insertAlarmPromptSection`이 피드백 섹션 **위**에 끼워 넣는다.
  **헤더 서버명은 버튼으로 바뀌지 않는다** — 트리거는 렌더 시점에 확정되며 결정적 추천의 진입이다.

---

## 4. 답변 가능성 실측 — 이 계획의 핵심 근거

### 4.1 재료는 충분한가 → **충분하다**

§2.1의 payload에 대상·자원·분석이 모두 있다. `history_stats` 비대칭(§2.1 ⚠)만 피하면 된다.

### 4.2 "1달 평균 사용률" → **답한다**

골드셋에 동일 형태가 있다.
- `testdata/text2sql_gold/gp.yaml:214` — *"전체 서버들의 CPU, 메모리 용량 및 사용률(평균·최대)
  리스트를 조회해줘. 사용률은 지난달 1개월 통계 기준으로."*
- `:255` — *"지난 3개월간 전체 서버별 월간 CPU, 메모리, 파일시스템, 디스크 IO 성능 통계"*
- `:345` — *"김포 폴스타 전체 서버를 통틀은 지난달 평균 CPU 사용률"*

서버 1대로 좁히는 형태(`:131`·`:196`)도 골드셋에 있다. **조합이 새로울 뿐 두 축 모두 검증돼 있다.**

### 4.3 "증가율" → **조건부다. 사용자 예시대로는 안 된다**

급증 조립기 실측(`src/db_adapters/polestar/spike_sql.py`):

| 제약 | 값 | 출처 |
|---|---|---|
| 대상 자원 | **파일시스템 전용** | `_FILESYSTEM_TYPE = "server.FileSystems"` (`:35`) |
| 기간 단위 | **월만** | `_METRIC_TABLE = "cmm_metric_stat_m"` (`:34`) — *"주 단위는 보존기간 미확인"* |
| 진입 조건 | 급증 어휘 **AND 절대 임계 명시** | `query_generator.py:590` — 절대 임계 없으면 *"급증 조립 미진입"* 로그 후 **LLM 경로로 폴백** |
| 게이트 | `spike_condition_enabled` | `query_generator.py:566` |

즉 *"메모리 **증가율**"* 은 ①자원 축이 파일시스템이 아니고 ②임계 문구도 없어 **결정적 경로에
진입하지 못하고 LLM 폴백**된다. 폴백이 곧 실패는 아니지만, **추천 기능의 첫 인상을 비결정 경로에
맡기는 설계**가 된다.

**대응 3안**(G-3):
1. **(권고)** 메모리·CPU 추천에서 "증가율"을 빼고 `평균·최대 사용률`로 한다 — 검증된 형태만 추천
2. 파일시스템 축에서만 급증형 추천을 낸다(문구에 임계·차분을 넣어 결정적 진입 보장) — **1과 병행 가능**
3. `spike_sql`을 메모리·CPU로 확장한다 — **별건 작업**(`_FILESYSTEM_TYPE` 일반화 + 골드셋 추가).
   본 계획의 범위 밖이며, 하려면 별도 계획서가 필요하다

### 4.4 전제 검증이 필요한 것 (실행 태스크 **T7** — `tasks/todo-86.md`, 미착수)

- 네트워크 축 질의(`server.Network`)가 실제로 답하는지 — 골드셋에 없다
- 로그 축(`server.LogMonitor`) 알람의 자연스러운 후속 질의가 무엇인지 — 알람 이력 조회로 대체 가능한지

---

## 5. 결정과 긴장

### 5.1 D-183과의 관계 — **긴장은 G-2 확정으로 해소됐다** (2026-08-31 개정)

계획 단계에서는 이 절이 "자동 전송을 위한 경계 추가"였다. **G-2가 확인 후 전송으로 확정되면서
경계를 새로 그을 필요가 없어졌다** — 두 경로 모두 "사용자 행동 없이는 전송하지 않는다"를
지키기 때문이다. D-183은 개정하지 않았고, 예외 조항도 만들지 않았다.

남은 것은 **두 경로가 서로 다른 이유로 다른 UI를 쓴다는 사실**이고, 이건 구현에 못 박았다:

| | `reuseHistoryQuery` (D-183) | `stageAlarmPrompt` (D-192) |
|---|---|---|
| 문구의 출처 | 사용자가 과거에 쓴 문장 | **시스템이 방금 생성한 문장** |
| 채운 뒤 | 포커스만 | **확인 바**(전문 표시 + `[이대로 조회]`) |
| 전송 | 사용자가 Enter | 사용자가 확인 버튼 |

확인 바가 알람 경로에만 붙는 이유는 **사용자가 쓰지 않은 문장이기 때문**이다 — 자기가 쓴
질의는 입력창에서 읽으면 되지만, 시스템이 만든 문장은 보내기 전에 보여야 한다.

**두 함수는 이름으로 구분하고 각 주석이 서로를 참조한다** — 나중에 한쪽을 보고 다른 쪽을
"일관성 있게" 고치는 회귀를 막기 위해서다. 테스트가 `reuseHistoryQuery`에 `handleSend`·
`showPromptConfirm` 호출이 **없음**을 고정한다(`test_history_reuse_still_does_not_send`).

### 5.2 과금 경로가 열린다

클릭 = LLM 파이프라인 실행이다. 운영 실측(`.env`)은 `LLM_PROVIDER=gemini`이므로 **실제 과금**이다
(D-127). **G-2가 C로 확정되면서 "클릭 1회로 과금"은 성립하지 않게 됐다** — 확인 버튼이 한 단계
더 있다. 실제로 구현한 방어:

| 방어 | 구현 |
|---|---|
| 확인 없이는 전송 없음 | 확인 바(§3.3 확정 결과) — 계획의 "중단 배너"는 **불필요해져 만들지 않았다** |
| 칩 3개 상한 | `ALARM_PROMPT_MAX = 3` |
| 일상 알람은 권하지 않음 | `is_routine === true` → 섹션 접힘 |
| 연타 방지 | 진행 중이면 조회 버튼 `disabled`(계획의 "칩 비활성"보다 상류에서 막는다) |
| LLM 추천 과금 | 기본 off · capabilities 게이트 · **결정적 추천 0건일 때만** 호출 |

### 5.3 리터럴 위치

`server.Memory` 등 폴스타 자원 타입이 매핑표에 들어간다. Phase 1(프론트 JS)은 `overfit_check`
스캔 대상이 아니다(`rglob("*.py")`). **Phase 2에서 서버로 옮길 때는** `src/db_adapters/polestar/`
(EXCLUDE) 또는 `config/` YAML이어야 한다 — `src/utils`·`src/nodes`에 두면 게이트가 막는다.

### 5.4 알려진 한계 (구현 후에도 남는 것)

- 추천은 **폐쇄 집합**이다 — 매핑 밖 알람은 추천이 없다(의도된 동작, §3.1)
- 존/DB 스코프: 프롬프트에 `db_id`를 넣지 않는다. 서버명만으로 라우팅이 되는지는 **T7에서 확인**하고,
  안 되면 존 표면어(`config/db_registry.yaml`의 위치 표면어)를 문구에 넣는다
- 알람 뷰는 세션 한정이므로(D-182 ①) 새로고침 후에는 추천도 사라진다 — 기능 성질상 정상

---

## 6. 태스크 분해 — **실행 결과** (2026-08-31)

> **계획 단계의 T1~T9 분해는 실행 시 T1~T7로 재편됐다.** SDD 워크플로를 태우면서 모듈 경계가
> `CAPABILITY-MAP-86.md`의 3모듈로 정리됐고, 태스크도 그 경계를 따랐다. **실행 정본은
> `tasks/todo-86.md`**이며 아래 표가 그것과 같은 번호를 쓴다. (계획 T1=G 확정은 착수 조건이라
> 태스크에서 빠졌고, 계획 T2=답변 가능성 검증은 승인이 필요해 **맨 뒤 T7로 이동**했다.)

전 태스크 공통 게이트: `node --check` · `pytest` · `arch_check --ci` · `overfit_check --ci`.

| # | 모듈 | 태스크 | 상태 |
|---|---|---|---|
| **T1** | `alarm-prompt-builder` | `buildAlarmPrompts` + 축 매핑표 + `alarmTargetName` | ✅ 완료 — 실행 검증 27 시나리오 |
| **T2** | `alarm-prompt-ui` | 카드 마크업(칩·헤더 트리거) + CSS | ✅ 완료 |
| **T3** | `alarm-prompt-ui` | 인계 배선 — **확인 후 전송** | ✅ 완료 — 실행 검증 22 시나리오 |
| **T4** | `alarm-prompt-llm-suggest` | 라우트 + 플래그 + 프롬프트 (기본 off) | ✅ 완료 — 11 테스트 |
| **T5** | `alarm-prompt-llm-suggest` | 프론트 연동 — 결정적 0건일 때만 호출 | ✅ 완료 |
| **T6** | — | 문서(D-192 등재 · INDEX · `.env.example` · 실수 이력) | ✅ 완료 |
| **T7** | — | **답변 가능성 실측** — 축별 추천 1건씩 실 파이프라인 구동 | ⏸ **미착수 — 과금 승인 대기**(D-127) |

**계획에 없었는데 실행에서 나온 작업 2건**(둘 다 완료):

- **설정 카탈로그 3계약 복구** — 신규 키 하나가 ①`SECTION_BY_KEY` 구획 분류 ②카탈로그 필드 수
  단언(309→310) ③`config/settings_help/` 큐레이션 커버리지를 동시에 깨뜨려 4건이 실패했다.
  **자기 델타만** 메웠다(공유 생성 자산 전면 재생성 금지).
- **`escapeHtml` 한계 회피** — 프롬프트 전문을 `data-*` 속성에 넣으려다, `escapeHtml`이
  **따옴표를 이스케이프하지 않음**(`div.textContent → innerHTML`)을 발견했다. 전문에는 서버명
  (외부 데이터)이 들어가므로 속성에 담으면 마크업이 깨진다 → 칩을 `createElement` +
  `textContent`/`title` + closure 캡처로 만들었다.

## 7. 사용자 확정 결과 (G-1~G-4 · 2026-08-31)

| ID | 질문 | 계획서 권고 | **사용자 확정** | 결과 |
|---|---|---|---|---|
| **G-1** | 클릭 진입 위치 | (c) 둘 다 | **(c) 서버명 + 하단 추천 칩** | 권고와 동일 |
| **G-2** | 자동 전송 수위 | B 자동 전송+중단 | **C 확인 후 전송** | ★**권고와 다름** — D-183과의 충돌이 해소되는 방향(§5.1) |
| **G-3** | "증가율" 처리 | 1+2 병행 | **1+2 병행**(검증된 형태만) | 권고와 동일 |
| **G-4** | 구현 범위 | 결정적 우선 · LLM은 옵트인 | **Phase 1 + 2**(LLM 추천 포함) | 범위 확대 — 서버 라우트까지 이번에 구현 |

**G-2가 이 계획의 결과물을 가장 크게 바꿨다.** 권고(B)대로 갔다면 D-192에 "자동 전송은 시스템
생성 프롬프트에 한한다"는 예외 조항을 새로 만들어야 했다. C를 택하면서 **예외 없이** D-183의
정신("사용자 행동 없이 전송하지 않는다")을 지키게 됐다.

## 8. 검증 결과 (2026-08-31)

| 항목 | 결과 |
|---|---|
| 신규 테스트 | **25건** — UI 14(`test_ui_alarm_view.py`) · 라우트 11(`test_alarm_prompt_suggest.py`) |
| `tests/test_api` | **288 passed** |
| 전체 스위트 | `30 failed / 5576 passed / 30 skipped / 16 errors` — **신규 실패 0** |
| 실행 검증 | 생성기 27 · 인계 22 시나리오 전건 통과 · **D-190 필터 회귀 0** |
| 게이트 | `node --check` · CSS 중괄호 균형 · `arch_check --ci` exit 0 · `overfit_check --ci` 신규 유입 0 |
| 실 LLM 호출 | **0건**(mock — D-127) |

**신규 실패 0의 근거**: 격리 worktree(`git worktree add <dir> HEAD`) 기준선과 실패 집합을
차분했다. 차분에서 현재 트리에만 있던 3건(`test_llm_gemini` 2 · `test_plan31_field_mapping_fix` 1)은
**비교 아티팩트**였다 — worktree에는 `.env`/`.encenv`가 없어(gitignore) 통과한 것으로, 기준선에
두 파일을 연결하자 **같은 3건이 그대로 실패**했다. 환경 의존 기존 실패다.

**실행 검증 방식**: 브라우저 확장·playwright MCP가 **모두 미연결**이라 D-190에서 확립한 방식을
재사용했다 — `app.js`의 대상 함수를 **원문 그대로 잘라내 최소 DOM 스텁 위에서 실행**한다.
복제한 로직이 아니라 배포될 코드를 돌리므로 구현과 검증이 갈라지지 않는다.

**미완 2건**:
- **실 브라우저 렌더** — 라이트/다크·모바일(414px) 칩 줄바꿈을 눈으로 확인하지 못했다.
  확장 연결이 복구되면 확인한다.
- **T7 답변 가능성 실측** — 과금 경로라 건별 승인이 필요하다(D-127). 이것이 끝나야
  `server.Network` 축의 편입 여부가 정해진다.

## 9. 범위 밖 (명시적 제외)

- `spike_sql`의 메모리·CPU 확장(§4.3-3) — 별건 계획 필요. **T7 실측 결과에 따라 우선순위가
  올라갈 수 있다**: 급증형 추천이 파일시스템에만 있다는 제약이 운영에서 불편으로 드러나면
  이 확장이 답이다.
- 알람 이력 저장/조회 API(D-182 ① 세션 한정 목록의 해소)
- 추천 프롬프트의 학습·개인화(피드백 루프 D-177과의 연계)
- 다중 서버 알람의 일괄 조회

---

## 10. 구현 산출물 (2026-08-31)

### 코드

| 파일 | 내용 |
|---|---|
| `src/static/js/app.js` | `ALARM_PROMPT_AXES`·`ALARM_PROMPT_COMMON`·`ALARM_PROMPT_MAX` · `alarmTargetName` · `buildAlarmPrompts` · `renderAlarmPromptSection` · `bindAlarmPrompts` · `stageAlarmPrompt` · `showPromptConfirm`/`hidePromptConfirm` · `requestLlmPrompt` · `insertAlarmPromptSection` · `renderAlarmIdentityHeader`에 `promptable` 인자 |
| `src/static/index.html` | 확인 바(`#promptConfirm`) · 자산 캐시 `app.js?v=8` · `style.css?v=13`(D-187) |
| `src/static/css/style.css` | `.alarm-prompt-*` · `button.alarm-prompt-trigger` · `.prompt-confirm-*` |
| `src/api/routes/alarm.py` | `POST /alarm/suggest-prompt` + 요청/응답 모델 3종 · capabilities에 `prompt_suggest_enabled` |
| `src/config.py` | `NoiseGateConfig.alarm_prompt_llm_suggest_enabled`(기본 `False`) |
| `noise_gate/prompts/alarm_prompt_suggest.py` | 시스템 프롬프트 + 사용자 템플릿(조회 가능 범위 명시) |
| `src/api/settings_catalog.py` | `SECTION_BY_KEY`에 신규 키 등재("운영자 피드백" 구획) |
| `.env.example` · `config/settings_help/noise_gate_notify.yaml` | 신규 키 문서화 · 설정 패널 도움말 |

### 테스트

| 파일 | 건수 |
|---|---|
| `tests/test_api/test_ui_alarm_view.py` | +14 (기존 28 → 42) |
| `tests/test_api/test_alarm_prompt_suggest.py` | 11 (신규 파일) |
| `tests/test_api/test_alarm_capabilities.py` | 키 집합 단언 갱신(자기 델타) |
| `tests/test_api/test_settings_catalog.py` | 필드 수 309→310(자기 델타) |

### SDD 산출물

`CAPABILITY-MAP-86.md` · `SPEC-alarm-prompt-builder.md` · `SPEC-alarm-prompt-ui.md` ·
`SPEC-alarm-prompt-llm-suggest.md` · `tasks/plan-86.md` · `tasks/todo-86.md`

> **스펙 정정 1건**: `SPEC-alarm-prompt-builder.md` 성공 기준 2와 3이 모순이었다 — "미매핑 축은
> 빈 배열"과 "축과 무관하게 공통 추천 1건 추가"가 동시에 성립할 수 없다. 미매핑 축에 공통 1건이
> 붙으면 **"결정적 추천 0건"이 성립하지 않아 LLM 보강 경로가 영영 열리지 않는다.** 구현 중
> 발견해 "**매핑된 축에만** 공통 추가"로 정정했다.

### 결정 기록

**D-192**(알람 → 질의 프롬프트 인계 — 추천은 검증된 형태만, 전송은 확인 뒤에) 본문 등재 완료.
`docs/18_known_mistakes.md`에 실수 1건 기록(인가 테스트 픽스처 키를 실측 없이 추정 → 7건 무더기 403).
