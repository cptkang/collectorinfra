# 93. 벤치마크 기반 환경변수 최적화·간소화 — 측정으로 노브를 줄인다

> **★ 완결(시점 문서) · 2026-09-21** — [`plans/109`](./109-WIP-config-simplification-consolidated.md)(config 간소화 통합 계획)로 잔여를 이관하고 종료했다(INDEX 「이관 조항」 D-208 2차 적용 · 파일명 상태 태그 해제). 잔여(L5 재측정 · 하네스 결함 · 실험 설계 확장 · R0~R4 롤아웃 · v12 A.7 알려진 제약)와 **현행 실행 가이드(v12 ①·② 전문)**는 109 「▶ 실행 가이드」·§3이 소유한다. 이 문서는 설계 근거·시점 기록으로 보존한다 — 아래 상태 표기는 **이관 직전 원문**이다.
>
> **작성일**: 2026-09-11
> **성격**: 구현 계획 · **상태: 트랙 T·A~C 구현 완료(2026-09-11 · D-211) — 잔여는 L5 실 스위프 실행뿐** · 파일명 `-WIP`
> **게이트**: G-1~G-13은 사용자 지시(*"구현을 진행하라"*)에 따라 **권고안으로 확정**하고 진행했다(`plans/90` 선례).
> **v12(2026-09-21)**: 사용자 지시 *"수정된 93번의 단계적인 실행 방법에 대한 가이드를 계획서에 업데이트하라"* — **「실행 가이드 ①·②」를 W-1~W-4(D-219·D-237·D-238) 이후 하네스 기준 0~8단계로 재작성**했다. 단계는 새 명령이 아니라 **같은 명령을 점점 크게 돌리며 단계마다 다음으로 넘어가도 되는지 판정하는 기준**을 둔 것이다 — run 20260914 가 그 기준 없이 93.4시간을 돌고 폐기됐다. 핵심 관문은 4단계(`--mode run --scale smoke`)이고 **차단 0 · 멈춤 주의 0** 이 통과 조건이다. 무과금 명령 5종(`--preflight`·`--show-env`·`--quick`·기본 실행·`--sweep --mode dry`/`mock --scale smoke`)은 로컬에서 실제로 돌려 출력을 떴고, `--mode run` 은 돌리지 않고 run 20260914 산출물에서 떴다. 옵션은 `build_parser()` 와 전수 대조했다. **실측이 뒤집은 종전 가이드 서술**: ①`--preflight --sweep-ready`·`--compare`·`--out` 옵션은 없다 ②`scripts.bench.validate`·`report`·`runner` 모듈 CLI 는 없다(`axes` 만 있다) ③preflight 는 DB·LLM 도달을 보지 않는다 ④`--sweep` 산출은 `results/scenario/<run_id>/` 이고 `SUMMARY.md` 는 트랙 T 전용이다 ⑤**`--sweep` 은 재개되지 않는다** ⑥smoke 는 30분이 아니라 **약 13~17시간(추정)** — 축만 줄고 워크로드는 전량이다 ⑦`--quick` 은 10초가 아니라 91초, 기본 실행은 1.5~3시간이 아니라 287초 ⑧§3.3 의 S1·S3·S4 는 구현되지 않았다(S0·S2 뿐) ⑨`--propose` 는 스위프 결과를 읽지 않는다 — 축 최적값이 반영된 제안은 `--sweep` 산출물에만 있다. 부수 발견: 트랙 T 기본 실행의 즉시 조치 6건은 L2 표본값 결함에 의한 **허위**다(A.3 2단계). **A.1~A.9 기호와 §0~§13 번호는 유지**했다. 코드 결함은 고치지 않고 A.7 「알려진 제약」에 적었다. **설계 절은 다시 쓰지 않고 틀린 명령·옵션 표기만 부기로 고쳤다**: §0.4-1 `--axes-review`(없음 → `scripts.bench.axes --explain`/`--write`) · §0.4-3 재개(스위프는 현재 미동작) · §3.3 `--repeat 3`(구현 기본값 1) · §4.4 `RUN_BENCH=1`(구현은 `--yes`) · §6.8.1-4 `--mode run` 추가(기본값 mock) · §6.8.2 `--compare`(없음). A.5 제목은 §9 의 참조("A.5 내부 명령")가 끊기지 않게 종전 이름을 병기했다.
> **v11(2026-09-15)**: **인증이 켜진 서버는 전용 벤치 계정으로만 잰다**(`plans/94` G-3 · 사용자 확정). v8 에서 넣은
> "크레덴셜이 없으면 모든 arm 에 `AUTH_ENABLED=false` 동일 주입"을 **철회**한다 — 인증 미들웨어와 사용자별 DB 범위가
> 빠져 운영과 다른 경로를 잰다. 이제 계정이 없으면 서버를 띄우기 전에 멈추고 계정 주는 법을 출력한다(인증이 꺼진
> 서버는 그대로 진행). v8 을 쓸 때 94 의 게이트 표(G-3)와 대조하지 않은 것이 원인이다.
> **v11.1(2026-09-21)**: **워크로드 건수는 산문이 아니라 동적 계산이 정본이다.** 아래 v9·v10 항에 적힌
> `107건`·`87/98건(89%)`·`유사어 32건` 은 **2026-09-14 시점의 실측이고 지금은 낡았다** —
> 2026-09-21 실측은 `closed` **103건 · 판정 가능 92/103(89%)** 이다(그룹별 D 6→8 · F 8→9 · K 10→3 —
> `testdata/scenarios/d_alarm.yaml` 의 D-07·D-08 신설 포함, 둘 다 `auto_answer: false`).
> `sandbox` 31건은 드리프트가 아니다 — 그룹 L 카탈로그 **32건** 중 SYN-F-05(운영 절차 `action`)를 뺀 값이라
> 본문의 `유사어 32건` 표기는 **카탈로그 기준으로 여전히 맞다**.
> **v9 항의 수치를 고쳐 쓰지 않는다** — 그건 그때의 실측 기록이다. 대신 원칙을 못 박는다:
> 정본은 `sweep.workload_summary()`·`sweep.judgeable_count()` 의 동적 계산이고 **산문은 파생**이다.
> 같은 드리프트가 v9→v10(`89/100` → `87/98`)에서 이미 한 번 났고 또 났다 —
> `scripts/bench/*`·`tests/test_scripts/*` 의 산문에서 건수를 **제거**했다(D-238 기준 ⑧).
> **잔여(기록만 · 구현 안 함)** — `plans/97` §11.5: **G-1** 판정 계약 변경(타임아웃·역질문 차단을 기능 합격률
> 분모에서 뺄 것인가 — 사용자 게이트) · **G-2** teardown 오염 `invalid` 표시(CU-B3 · `plans/108` 등재분 참조) ·
> **G-3** 최적화 표면 사각지대(`AppConfig` 밖 코드 상수는 축이 되지 않는다 — `axes.py` 독스트링 「한계 ②」).
> **v10(2026-09-14)**: **벤치 서버가 운영 상태를 건드리던 것과 kill 시 고아 서버를 막았다.** ①폐쇄망 스위프 서버 로그
> 62개 전부에서 in-process 알람 워커가 운영 스트림 `alarm:raw` 를 **운영과 같은 consumer group** 으로 소비하고 있었다 —
> 러너가 모든 프로파일에 `ALARM_ENABLED=false` 를 주입하고 에코로 확인한다(이 플래그의 사용처 3곳이 전부 질의 경로
> 밖임을 전수 확인). ②부모를 SIGTERM 으로 멈추면 `finally` 가 돌지 않아 프로파일 서버가 고아로 남았다(모의 서버 7개
> 실측) — SIGTERM 을 SystemExit 으로 바꿔 정리 경로를 보장한다. ③v9 의 판정 가능 집계 **89/100 을 87/98 로 정정**한다 —
> teardown 미지원으로 매 arm 에서 건너뛰는 A-05·A-10 을 실행 대상으로 셌다.
> **v9(2026-09-14)**: **정확도를 잴 수 있게 만들었다(§4.3.0-1).** 정상군 107건 중 기계 단언이
> **1건**뿐이라 정확도 쌍이 늘 0쌍이었다 — 산문 기대값을 단언으로 옮겨 **87/98건(89%)** 으로(v10 정정)
> 올렸다. 막고 있던 셋을 먼저 고쳤다: ①`intent` 는 응답에 없어 대조하면 전건 거짓 불합격
> ②모의 실행이 canned 응답을 판정 ③턴마다 엔드포인트를 못 바꿔 폼필 HITL 8건이 표현 불가.
> 부수로 F-01 의 `limit 100000` 오기를 정정했다(실제 상향값 10,000) — 단언과 모의 응답이
> **서로만 맞고 시스템과 어긋나** 모의에서만 통과하던 자기참조였다. H군 17건이 같은 샘플
> 파일 하나를 올리던 것도 양식별 실 픽스처로 교체했다.
> **v8(2026-09-14)**: **첫 실 스위프가 전건 실패 — 원인 3건을 고쳤다.** 62 arm · 1984턴을 돌렸는데
> `/query/stream` 요청이 **전량 HTTP 401**이었고 `executed_sql`은 1984행 전부 `null`이었다. ①러너가
> **인증을 한 번도 하지 않았다** — `login()`은 호출부 0곳, 본문 키도 서버 계약(`user_id`)과 어긋났다.
> 프로파일마다 로그인하도록 고치고(JWT 시크릿이 기동마다 난수라 런 1회 1토큰은 62개 중 1개만 맞는다),
> 크레덴셜이 없으면 모든 arm에 `AUTH_ENABLED=false`를 **동일 주입**하고 그 사실을 첫 화면에 적는다
> ②94의 `verify_profile`이 **확인 못 한 것을 통과로 셌다** — 유효성을 사유 문자열 접두사로 판정해
> "설정 에코 미확인"이 목록 밖이라 62개 프로파일이 전부 `valid=true`였다. 상태 플래그 판정으로 바꿨다
> ③**사고가 정상 얼굴의 리포트로 나갔다** — 판정 전 **건전성 관문**(§4.3.2)과 **2순위 판정 신호**
> (완주율·SQL 생성률 · §4.3.1)를 넣었다. 정상군은 기계 단언이 107건 중 1건뿐이라 정확도 쌍이 늘 0쌍이었다.
> ④**워크로드 환경 기본값이 틀려 실 질의를 한 건도 재지 않았다** — `env="sandbox"` 로 유사어 32건(그룹 L)만
> 돌았다. 실 질의 워크로드는 `closed` 쪽 그룹 A~K **107건**이다. 기본을 `closed` 로 바꾸고 활성 DB로
> 자동 판정하며, **워크로드 구성을 실행 첫 화면에 적는다**(§4.3.0)
> ⑤**선언만 있고 대입이 없는 지표 3종** — `llm_calls`·`tokens`·`retries` 가 3968행 전부 `null` 이었고
> `llm_calls.max` 예산 단언은 영영 발화하지 않으면서 `pass` 로 집계됐다. LLM 호출 수·토큰 수는 `done`
> 페이로드에 없어 **구조적 측정 불가**임을 명시하고(추정치 금지), 재시도는 `node_start` 회귀 재진입으로,
> 비용은 노드 실행 수로 **실측**한다. 측정치 없는 예산 단언은 `pass` 가 아니라 `manual` 이다
> ⑥94 자체 CLI(`python -m scripts.scenario`)에도 같은 크레덴셜 배선을 넣었다 — 직접 실행해도 같은 401 을 맞았다.
> **v7(2026-09-11)**: **트랙 T 구현 완료 — 실측이 계획을 고친 것 5건**(`CAPABILITY-MAP-93.md`·`SPEC-bench-config-validation.md`·
> `tasks/plan-93.md`·`todo-93.md`). ①**F1 후 후보는 40~60이 아니라 148**이다(§3.1 추정 정정) — 40~60으로 좁히는 것은 F2의 몫이다
> ②**L2·L3 전수는 40~80분이 아니라 3~5분**이다(경량 모드 실측 · §3.5.2 표 정정) ③`auth.jwt_secret`이 기동마다 새로 생성돼
> **설정 지문을 매번 바꾼다** — 비결정 필드 자동 탐지를 넣지 않으면 L4가 전 항목을 "변함"으로 오판한다 ④env 접두와 그룹명이 다른
> 실사례(`API_PORT`→`server.port`)가 있어 **경로는 카탈로그의 `field_name`으로 만든다**(문자열 깎기 금지) ⑤축은 **1차(동작 모드)와
> 2차(상한·예산)로 갈라야 한다** — 타임아웃류를 1차에 두면 47개가 되고, 타임아웃을 줄이는 것은 빨라지는 것이 아니라 실패가 느는 것이다.
> **첫 실행 결과**: 고아 키 0 · 가림 0 · **`.env.example` 누락 103** · 설명 부재 46 · 1차 축 28개.
> **v6(2026-09-11)**: 사용자 지시 *"실행가이드는 문서 제일 위쪽에 배치하라"* 반영 — **실행 가이드 두 절을 헤더 직후로 이동**.
> 문서를 여는 사람 대부분이 실행자이므로 「실행 가이드 ① 퀵(한 화면)」·「② 상세」가 §0(설계 요약)보다 앞에 온다. 설계 절의 번호는
> **그대로 유지**했다(§0~§13 · 본문 상호 참조 60여 곳이 깨지지 않게) — 실행 가이드는 번호 없는 머리 절이다. 하위 절 기호 `A.1`~`A.9`도
> 보존했다(기존 참조 유지). 첫머리에 **독자 분기 안내**를 넣어 실행자와 검토자가 각자 볼 곳을 한 줄로 고른다.
> **v5(2026-09-11)**: 사용자 확정 2건 — ① *"내부망에서 밴치마크 테스트를 진행할 예정이라 과금 승인은 필요 없다"* →
> **G-7 해소**. 승인 게이트를 삭제하는 대신 **프로바이더 기반 자동 분기**로 바꿨다: 내부망(`LLM_PROVIDER=fabrix`)이면 승인·옵트인 없이 실행,
> **외부 프로바이더(`gemini` 등)면 D-127 승인 게이트가 그대로 산다**(개발망 스모크가 무승인으로 새지 않게 — §4.4). 표의 「과금」 열은
> 「**실 LLM**」으로 바꿨다(내부망에서도 시간·서버 부하는 실재한다). ② *"사용자 퀵 가이드를 작성하라"* → **「실행 가이드 ① 퀵」** 신설
> (한 화면 · 구현 시 `docs/30_bench_quick_guide.md`로 추출 — Wave B5).
> **v4(2026-09-11)**: **`plans/94` 신설에 따른 경계 정리와 실측 정정**(§1.5 신설) — 94 §8이 제기한 실측을 검증해 수용했다:
> `docs/29` **113건** + `docs/synonym_test_cases.md` **32건 = 145건**이 이미 문서로 존재한다(실측 2026-09-11 ·
> `grep -cE '^\| *[A-K]-[0-9]+'`). **§0.1의 선행 P(골드셋 26→80건 확장)는 94 카탈로그 소비로 대체**되며, §2-②의 검정력 전망도
> 상향된다(80건 = 10%p 검출 → 145건 = 5~7%p 검출). Wave B1은 *생성*에서 *소비·선별*로 성격이 바뀐다. 첫 집계에서 104건으로 센 것은
> 표 파이프 뒤 공백을 가정한 grep 누락이었다(9건 미검출 · `docs/18` 등재 유형 재발).
> **v3(2026-09-11)**: 사용자 추가 지시 *"작성된 레포트를 어떻게 분석하여 설정값 간소화를 진행할 것인지에 대한 계획도 업데이트하라"* 반영 —
> **§6.5 리포트 → 처분 결정 파이프라인**(입력 신호 6종 → 10순위 결정 절차 · **0단계는 간소화가 아니라 결함 수정**) ·
> **§6.6 위험도 순 5단계 롤아웃 R0~R4**(결함수정 → 문서 → 노출 → 기본값 → 삭제 · **체감 간소화의 대부분은 무위험 구간 R1·R2에서 나온다** · 실험설계 S0~S4와 기호를 구분한다) ·
> **§6.7 회귀 방어**(R3의 `migration_pin.env` 선(先)고정 · 비트 동일성 가드 4종 재사용 · R4 테스트 연쇄) ·
> **§6.8 정량 목표와 완료 판정**(운영자가 보는 수를 줄인다 · **신규 설치 시나리오가 등급 배정의 최종 심판**). 부수 정정: 카탈로그 필드 334→**335**(테스트 단언 실측)
> **v2(2026-09-11)**: 사용자 추가 지시 2건 반영 — ① *"개발자가 벤치마크 테스트 코드를 실행하는 방법을 가이드하고"* → **「실행 가이드 ② 상세」** 신설 ·
> ② *"전체적인 설정 파일을 테스트하고 보고서를 만들 수 있는 계획"* → **트랙 T 전수 설정 검증(§3.5)** + **설정 건강 보고서·전건 커버리지 장부(§5.4)** 신설 ·
> ③ *"개발자의 역할은 단지 벤치마크 테스트를 실행시키는 것 뿐이다"* → **§0.4 자동화 책임 경계** 신설, 축 선별·해석·재개를 전부 코드로 넘기고
> 개발자 표면을 **단일 명령 하나**로 축소(「실행 가이드 ② 상세」). v1에서 *"사람이 고치는 유일한 파일"* 이던 `axes.yaml`은 **자동 생성물**로 성격이 바뀌었다
> **요청 취지(사용자 지시 원문)**: *"현재 환경변수가 너무 많아 설정하기 너무 어렵다. 이 설정 변수들의 조합에 따라 성능도 달라 질수 있다. 따라서 내부망에서 별도의 밴치마크 테스트를 수행하여 최적의 환경변수를 정의하고 기존의 환경변수를 줄여 설정옵션을 간소화 시켜야 한다. 이 요건에 맞게 밴치마크 테스트 코드와 결과 레포트 생성 코드를 작성하고 결과를 읽어 환경변수 최적화를 수행하는 코드를 작성하기 위한 계획파일을 생성하라."*
> **요청된 산출물 3종**: ① 벤치마크 테스트 코드 ② 결과 리포트 생성 코드 ③ 결과를 읽어 환경변수 최적화를 수행하는 코드
> **⚠ 해석 정정(§0.1)**: 산출물은 3종이지만, 그 앞에 **측정 가능성을 만드는 선행 과제 1건**(워크로드 확장)이 있다. 현 골드셋 26건으로는
> 두 설정의 정확도 차이를 통계적으로 구분할 수 없다(§2-②). 이 선행이 없으면 ①②③을 다 만들어도 리포트가 *"차이 없음(판정 불가)"*만 낸다.
> **상위/선행 계획**: `plans/70`(코드베이스 규모·경로 부채 — 본 계획은 그 P1 플래그 정리에 **측정 근거**를 공급) ·
> `plans/68`(설정 웹UI — 카탈로그 SSOT 재사용) · `plans/61`(평가 하네스 E1 — `scripts/eval_text2sql.py` A/B 인프라 재사용) ·
> `plans/85` §11·F1(플래그 43개 감사표) · `docs/29_query_performance_test_plan.md`(질의 성능·기능 테스트 스위트 — **워크로드 원천**) ·
> `docs/flag_audit.md`(플래그 전수 감사표 — **정적 판정의 정본**) · `docs/21_orchestration_ladder.md`(사다리 단 = 최상위 축)
> **관련 결정**: **D-161**(폐기 전 4항 실측 의무 — 환경변수 삭제는 폐기 제안이다 · 신규 플래그 만료일) ·
> **D-162**(플래그 판정 규칙 5조 + *"신규 `enable_*` 플래그 추가 0"*) · **D-127**(과금 외부 API 건별 승인 — **내부망 FabriX 경로는 사용자 확정으로 면제**, 외부 프로바이더는 유지 · §4.4 · v5) ·
> **D-129**(설정 카탈로그 SSOT = pydantic 인트로스펙션) · **D-135**(`apply_mode` 3분류 — 즉시 7/리로드 78/재시작 148) ·
> **D-194**(FabriX 하이퍼파라미터 프로파일 — 온도는 **축이지 노브가 아니다**) · **D-035**(결정적=판단·LLM=서술) ·
> `plans/80` §5.4-③(신규 플래그 기본 off = 현행과 비트 동일)
> **신규 결정 예약**: **D-211**(§12). `docs/02_decision.md` 「채번 이력」 표에 등재(2026-09-11).
> ※ 채번 실측 2026-09-11 — `## D-` 헤더 최댓값 **209** · 「변경 이력」 표 최댓값 **209** · 「채번 이력」 표 예약(D-105·115·134·158·163~168·176·195·**210**=`plans/92`) 대조 → **D-211**.
> **실측 기준**: 아래 `file:line`·수치는 2026-09-11 현 브랜치(`multiintent`, HEAD `9e73cc9` + 미커밋 작업 트리)에서 직접 확인했다.

---

> **이 문서를 처음 여는 사람에게**
>
> **벤치마크를 *돌리러* 왔다면 바로 아래 두 절만 보면 된다.** 「실행 가이드 ① 퀵」이 한 화면이고,
> 「② 상세」가 참조본이다. 그 뒤 §0부터는 *왜 이렇게 만드는가*를 다루는 설계 문서라 실행에는 필요 없다.
>
> **설계를 검토하러 왔다면** 실행 가이드 두 절을 건너뛰고 §0(요약)부터 읽으면 된다.

---

## 실행 가이드 ① 퀵 (한 화면) *(v5 신설 · v6에서 문서 앞으로 이동 · **v12에서 단계별로 재작성**)*

> 사용자 지시: *"사용자 퀵 가이드를 작성하라."* · *"수정된 93번의 단계적인 실행 방법에 대한 가이드를 계획서에 업데이트하라."*(v12)
>
> 「② 상세」가 **참조본**이고 이 절은 그 요약이다 — 둘이 어긋나면 ②를 따른다.
> 아래 출력 예시는 전부 **실제 출력에서 떴다**(무과금 명령은 2026-09-21 로컬 실측 · `--mode run` 은
> run `20260914-185540` 의 `sweep_console.log` 와 그 원시 로그에 현재 코드를 적용한 결과).

---

### 벤치마크 퀵 가이드

**무엇인가** — 설정 파일이 멀쩡한지 전수로 검사하고, 설정을 바꿔가며 **축마다 어느 값이 최적인지** 잰다.
**내가 할 일** — 아래 단계를 **순서대로** 돌리고, 단계마다 「통과 기준」을 본 뒤 다음으로 간다. 끝나면 폴더를 전달한다.

> **왜 단계인가.** run `20260914-185540` 은 62 arm · 6,567턴 · **93.4시간**을 돌고 **통째로 폐기**됐다 —
> 역질문 56%·SQL 관측 0건 상태였는데 그걸 멈출 기준이 없었다. 단계는 새 명령이 아니다.
> **같은 명령을 점점 크게 돌리되, 다음으로 넘어가도 되는지 매번 판정한다.**

| 단계 | 명령 | 걸리는 시간 | 통과 기준(한 줄) |
|---:|---|---|---|
| **0** | 사전 결정·코드 준비 (명령 없음 — 아래 확인 4건) | — | 사다리 단 결정 · 벤치 계정 · **깨끗한 원격 커밋** · 하네스 수정 포함 |
| **1** | `python -m scripts.bench --preflight` | 약 2초 | 마지막 줄 `→ 실행 가능합니다.` |
| **2** | `python -m scripts.bench --quick` → `python -m scripts.bench` | 91초 → 약 5분 | `즉시 조치` 가 알려진 L2 허위 6건(A.3 2단계) 뿐 |
| **3** | `python -m scripts.bench --sweep --mode mock --scale smoke` | 약 6분 | 건전성 블록에 `판정을 내지 않습니다` 없음 · 산출 4종 생성 (**exit 1 은 정상**) |
| **4** ★ | `python -m scripts.bench --sweep --mode run --scale smoke` | **약 13~17시간(추정)** | **차단 0 · 멈춤 주의 0** (아래) |
| **5** | `python -m scripts.bench --sweep --mode run --scale full` | **약 93~98시간** | 4단계와 같은 기준 |
| **6** | 결과 읽기 (명령 없음) | — | 건전성 → 축 최적 레벨 → arm 판정 순서 |
| **7** | 처분 제안 확인 (`--sweep` 이 이미 만든다) | — | `recommended.env.diff` 는 **제안일 뿐** |
| **8** | 산출 폴더 전달 | — | 체크포인트 DB 제외 |

**4단계가 핵심 관문이다 — 여기서 아래 중 하나라도 보이면 5단계로 가지 않는다.**

| 화면에 나온 것 | 뜻 | 할 일 |
|---|---|---|
| `판정을 내지 않습니다 — …SQL 이 관측된 턴이 0건이다` | **차단**. 파이프라인이 SQL 에 못 닿았거나 하네스가 SQL 을 못 읽었다 | 멈춤. `raw.jsonl` 을 전달 |
| `판정을 내지 않습니다 — …전부 무효다` · `…오류다` · `유효한 프로파일이 0개다` | **차단** | 멈춤. `INVALID` 사유를 전달 |
| `주의: 그래프 진입률 NN%` (80% 미만) | 턴 상당수가 노드를 하나도 안 밟았다 | **멈춤** |
| `주의: 역질문 종료율 NN%` (30% 이상) | 역질문에서 멈춘 턴이 많다 — **이 런이 폐기된 이유** | **멈춤** |
| `주의: 무효 턴 N건(NN%)` | 러너 인증 실패 등 측정 불성립 | **5% 넘으면 멈춤**(D-219 ③) |
| `주의: 기준선 arm 이 N개 중 M번째로 실행됐다` | 기준선이 맨 앞이 아니다 — 커밋에 R-6 수정이 없다 | **멈춤**(0단계 커밋 확인) |
| `판정표를 무효로 표시했습니다 — … 불일치 쌍 합 0건` | 설정을 바꿔도 결과가 한 건도 안 바뀌었다 | **멈춤** — `full` 은 축이 늘 뿐 워크로드는 같다 |
| `주의: 자동응답 N건(NN%)` | 하네스가 역질문에 대신 답했다 | **멈춤 사유 아님** — 그 턴의 라우팅·매핑 판정은 해석에서 뺀다(6단계) |

> **현재 기능 합격률에는 타임아웃·역질문 차단 턴이 포함된다** — 「단언 미평가」 턴을 분모에서 빼는 규칙(`plans/97` §11.5 G-1)은
> 사용자 승인 대기다. 합격률 숫자만 보고 품질을 판단하지 말 것.

**인증이 켜진 서버(`AUTH_ENABLED=true`)는 전용 벤치 계정이 있어야 돈다**(`plans/94` G-3 · D-215). 계정이 없으면 스위프는
서버를 띄우기 전에 멈추고 계정 주는 법을 출력한다 — 인증을 끄고 재지 않는다. 계정은 `--user <ID> --password <PW>` 또는
OS 환경변수 `BENCH_USER_ID`·`BENCH_USER_PASSWORD`(**`.env`·`.encenv`에 적으면 읽히지 않는다**). 운영자 크레덴셜은
설정에서 자동으로 읽는다(`--admin-user`/`--admin-password` 로 덮을 수 있다). 계정 준비는 `docs/03_setup_guide.md` §3.5.

**내부망(`fabrix`)·로컬(`mlx`)이면 승인을 묻지 않는다.** 외부 프로바이더(`gemini` 등)면 `--mode run` 이 시작하지 않고 멈춘다 —
여는 방법은 코드상 **`--yes` 하나**다(`__main__.py:335-338` · §4.4 의 `RUN_BENCH=1` 은 구현되지 않았다).
**외부 프로바이더(gemini 등)는 과금이다. `--yes` 는 사용자가 이번 실행을 승인했을 때만 붙인다(D-127).**

#### 맥북 로컬 MLX로 돌릴 때 *(2026-09-17 추가 · D-222 · `plans/100`)*

- 준비·기동은 `docs/03_setup_guide.md` §7.2(`scripts/mlx_server.sh`). mlx는 내부망과 같이 승인 없이 실 실행된다.
- `--sweep --mode run`은 arm 서버를 띄우기 전에 **MLX 서버 도달·1토큰 생성**을 확인하고, 실패하면 멈춘다(exit 2).
- **로직 확인용으로만 쓴다.** arm 1개 × 시나리오 1건이 27B에서 약 10분(583초·618초 실측)이다. 규모를 줄이는 CLI 옵션은 없다.
- **지연 비교를 결론으로 쓰지 않는다.** MLX 프롬프트 캐시가 arm 사이에 남아 실행 순서가 지연에 섞인다. 성능 결론은 내부망 결과로만 낸다.
- 로컬 `ACTIVE_DB_IDS` 에 `itam` 이 있으면 워크로드 환경이 `closed` 로 자동 판정된다(`itam` 은 샌드박스지만
  `sweep.SANDBOX_DB_IDS` 가 `polestar` 만 샌드박스로 본다 — 2026-09-21 실측). 그러면 운영 DB 전제의 A~K 시나리오가
  샌드박스 데이터 위에서 돌아 **데이터 의존 판정은 참고용**이다. 그룹 L 만 돌리려면 `--env sandbox` 를 명시한다.

---

### 치트시트

```bash
python -m scripts.bench --preflight                        # 1  준비 확인 (2초)
python -m scripts.bench --show-env                         #    지금 어떤 프로바이더·활성 DB 로 도는지
python -m scripts.bench --quick                            # 2  L1·L3 정합 (91초)
python -m scripts.bench                                    # 2  L1~L4 전층 (약 5분 · LLM 0)
python -m scripts.bench --sweep --mode dry                 #    arm 전개만 (1초 · smoke 9 arm)
python -m scripts.bench --sweep --mode mock --scale smoke  # 3  무과금 배관 (6분 · exit 1 정상)
python -m scripts.bench --sweep --mode run  --scale smoke  # 4  ★ 핵심 관문 (13~17시간 추정)
python -m scripts.bench --sweep --mode run  --scale full   # 5  본 실행 (93~98시간)
python -m scripts.bench.axes --explain                    #    축이 왜 뽑혔는지(보조)
```

---

## 실행 가이드 ② 상세 — 개발자 실행 참조본 *(v2 신설 · v6에서 문서 앞으로 이동 · **v12에서 단계별로 재작성**)*

> 사용자 지시: *"개발자가 벤치마크 테스트 코드를 실행하는 방법을 가이드하고"* ·
> *"개발자의 역할은 단지 벤치마크 테스트를 실행시키는 것 뿐이다."* ·
> *"수정된 93번의 단계적인 실행 방법에 대한 가이드를 계획서에 업데이트하라."*(v12)
>
> **이 절만 읽으면 실행할 수 있어야 한다.** 뒤따르는 §0~§13은 설계 근거다.
> 명령·옵션은 전부 `scripts/bench/__main__.py` `build_parser()` 와 대조했다 — 여기 없는 옵션은 존재하지 않는다.

### A.1 30초 요약

```bash
cd /path/to/collectorinfra && source .venv/bin/activate

python -m scripts.bench --preflight                        # 1단계
python -m scripts.bench --quick && python -m scripts.bench # 2단계 — 설정 검증 (LLM 0)
python -m scripts.bench --sweep --mode mock --scale smoke  # 3단계 — 배관 (무과금)
python -m scripts.bench --sweep --mode run  --scale smoke  # 4단계 — ★ 관문. 통과해야 5단계
python -m scripts.bench --sweep --mode run  --scale full   # 5단계 — 본 실행
```

- **단계를 건너뛰지 않는다.** 특히 4단계의 「멈춤」 신호(①퀵 표)가 하나라도 보이면 5단계를 돌리지 않는다.
- **스위프는 중단되면 처음부터 다시 돈다** — `--sweep` 에는 재개가 없다(2026-09-21 실측 · A.7). 긴 실행은 백그라운드로 띄우고 세션을 끊지 않는다.
- YAML을 열거나 고칠 일은 없다. 축은 코드가 고르고, 판정표가 결론 문장을 쓴다.

### A.2 0단계 — 사전 결정과 코드 준비 (명령 없음 · 스위프 전 1회)

| # | 확인 | 방법 | 이게 없으면 |
|---|---|---|---|
| 1 | **사다리 단** — 어느 경로를 잴 것인가 | 서버 `.env` 의 `ENABLE_*` 3종 · 기동 후 `report.md` §1 「사다리 단」 열 | 스위프는 **서버 `.env` 가 확정하는 단을 그대로 잰다**. run 20260914 는 62/62 arm 이 **2단 `intent_orchestration`** 이었는데 **기준 경로는 3단 `semantic_router`** 다(D-225). 어느 단으로 잴지는 **사용자 결정 대기**(3단 운영 전환 = `plans/102` L-5 · 3단 기능 동등성 = `plans/103` P5 선행) |
| 2 | **벤치 계정**(인증 on 서버만) | 계정 ID·비밀번호 확보 | 서버를 띄우기 전에 멈춘다(D-215) |
| 3 | **깨끗한 원격 커밋** | `git status --porcelain` 이 비어 있고 `git branch -r --contains HEAD` 에 원격 브랜치가 나온다 | **재현 불가**. run 20260914 는 `meta.commit=82e411e…` 가 이 저장소에 없고 `dirty=true` 라 어떤 처분의 근거도 되지 못했다(D-161 ①) |
| 4 | **하네스 수정이 들어간 커밋** | `git merge-base --is-ancestor 9bc94f0 HEAD` (역질문 자동응답) · `grep -c "def compare_levels" scripts/bench/compare.py` ≥ 1 (레벨 간 비교·대조군 · D-237) · `grep -c "그 순서대로 돈다" scripts/scenario/runner.py` ≥ 1 (기준선 맨 앞 실행 · D-238) | run 20260914 와 같은 사고가 재발한다. **※ 2026-09-21 현재 D-219·D-237·D-238 수정은 작업 트리에만 있고 커밋되지 않았다 — 커밋은 사용자 판단 대기** |

### A.3 1~5단계 — 절차와 통과 기준

#### 1단계 — `--preflight` (약 2초 · LLM 0)

```
$ python -m scripts.bench --preflight          # 2026-09-21 로컬 실측
[..]   config        읽는 중...
[OK]   venv          python 3.12.11
[OK]   config        343 필드 로드 · 에코 343 경로
[OK]   provider      내부망/로컬 프로바이더(워커 mlx, 오케스트레이터 mlx) — 승인 없이 진행합니다
[INFO] 비결정 필드    1건 자동 제외 (auth.jwt_secret)
[WARN] DB2 드라이버   ibm-db 미설치 — 은행존(b0) 대상 항목은 건너뜁니다
[OK]   디스크        여유 54GB
[INFO] 이전 실행     20260917-02 미완 — 다음 실행에서 이어집니다
→ 실행 가능합니다.  python -m scripts.bench
```

- **통과**: 마지막 줄 `→ 실행 가능합니다.` (exit 0). `[WARN] ibm-db` 는 은행존(b0)을 안 쓰면 그대로 진행.
- `[FAIL] config` 이면 멈춘다 — `.env` 값 형식 문제다.
- **preflight 가 보지 않는 것**: DB 도달·LLM 도달·사다리 단. 그건 3·4단계 산출(`report.md` §1)에서 확인한다.
- `이전 실행 … 미완 — 다음 실행에서 이어집니다` 는 **사실이 아니다** — 트랙 T 는 매번 새 폴더를 만든다(재개 없음). 무시해도 된다.
- 프로바이더만 빠르게 보려면 `--show-env`:

```
$ python -m scripts.bench --show-env           # 2026-09-21 로컬 실측
프로바이더   : 워커 mlx / 오케스트레이터 mlx
승인 정책    : 불요 — 내부망/로컬 프로바이더(워커 mlx, 오케스트레이터 mlx) — 승인 없이 진행합니다
활성 DB      : polestar,itam
DB 백엔드    : dbhub
카탈로그     : 343 필드
민감 경로    : 14 건(값은 해시로만 기록)
```

#### 2단계 — 트랙 T 설정 검증 (LLM 0 · 성능 스위프와 무관하게 유효)

```
$ python -m scripts.bench --quick              # 2026-09-21 로컬 실측 · 91초
[1/5] L1 카탈로그 정합 — 343필드
      고아 0 · 누락 0 · 설명부재 69
[2/5] L2·L4 건너뜀 (--quick)
[3/5] L3 주입 실효성 — 156건
      가림 0건
[5/5] 리포트 생성

# 20260921-01 요약
대상       : 설정 343필드 · 트랙 T(L1~L4) · 실 LLM 호출 0
즉시 조치  : 0건
정합       : 누락 0 · 설명부재 69 · encenv 관리 9
…
소요 91초 · 산출 폴더: …/results/bench/20260921-01
```

- **통과**: 요약의 `즉시 조치  : 0건`. 0이 아니면 `config_health.md` 1절(즉시 조치)을 전달하고 스위프 전에 고친다 —
  깨진 설정을 안고 스위프하면 "줄였더니 안 된다"가 된다(§6.5.2 0단계).
- `--quick` 통과 후 **기본 실행**(`python -m scripts.bench`, L1~L4 전층)을 한 번 돈다. 2026-09-21 로컬 실측 **287초(4분 47초)** — 종전 가이드의 「1.5~3시간」이 아니다:

```
$ python -m scripts.bench                      # 2026-09-21 로컬 실측 · 287초
[1/5] L1 카탈로그 정합 — 343필드
      고아 0 · 누락 0 · 설명부재 69
[2/5] L2 기동 안전성 — 156건
      거부 6건
[3/5] L3 주입 실효성 — 156건
      가림 0건
[4/5] L4 소비 실증 — 156건
      변함 131 · 불변 6 · 목록 낡음 0
[5/5] 리포트 생성

# 20260921-02 요약
즉시 조치  : 6건 (고아 0 · 가림 0 · 기동실패 6)
…
```

  **이 6건은 허위다 — 설정 결함이 아니라 L2 표본값 결함이다.** `validate._sample_values`(`validate.py:80-91`)가
  `tristate`·`json_list`·JSON 문자열 타입을 몰라 `x` 를 넣고, 설정이 그것을 **올바르게 거부**한 것이다:
  `API_CORS_ORIGINS` · `ENABLE_INTENT_ORCHESTRATION` · `ENABLE_SEMANTIC_ROUTING` · `LLM_FABRIX_ANSWER_LLM_CONFIG` ·
  `LLM_FABRIX_LLM_CONFIG` · `SECURITY_SENSITIVE_COLUMNS`(전부 `기동 실패 <KEY>=x`). **이 6건 외의 즉시 조치가 있으면 멈춘다.**
  (run 20260914 의 설정 검증 트랙은 `--quick` 이라 L2 를 돌지 않아 이 6건이 나타나지 않았다.)
- CI 용은 `--quick --ci`(즉시 조치가 있으면 exit 1).

#### 3단계 — `--sweep --mode mock --scale smoke` (무과금 배관 · 약 6분)

모의 서버로 **스위프 전 경로**(arm 전개 → 설정 스냅샷 → 서버 기동 → 판정 → 94 리포트 → 처분 제안)가 도는지만 본다.
LLM·DB 를 호출하지 않는다. 서버 포트는 OS가 빈 포트를 준다(`server.py` 가 `bind(("127.0.0.1", 0))`) — 기존 서비스와 충돌하지 않는다.

```
$ python -m scripts.bench --sweep --mode mock --scale smoke     # 2026-09-21 로컬 실측 · 6분 10초
축 스위프 — arm 9개 (기준선 포함) · 규모 smoke · 모드 mock
  환경: closed — 실 관측 DB 활성(itam) — closed
  워크로드: 시나리오 103건 — 그룹 A(12), B(12), C(13), D(8), E(7), F(9), G(4), H(17), I(8), J(10), K(3)
            정확도 판정 가능 92/103건(89%) — 나머지는 완주율·지연만 잰다
  규모: 시나리오 103건 × arm 9 × 반복 1 = 927회 실행
  예상: 약 0.1시간 (기동 9회 + 턴 927회 × 0.2초 · mock 기준)
  접속: 모의 서버 — 인증 없음
  격리: ALARM_ENABLED=false 를 모든 arm에 동일 주입 — …
  설정 스냅샷: arm 8개 실효값 확인 · 대조군(기준선과 동일 설정) 4개
     대조군 S2-COMPOSITE_AVAILABILITY_PRECHECK_ENABLED-true — 주입 {…: 'true'} 이 기준선 실효값과 같다
     …
  94 리포트: report.md, summary.json

건전성 — 유효 프로파일 9/9 · 턴 1161건 · 판정 {'manual': 1134, 'pass': 27}
  워크로드 도달 — SQL 관측 98% · 그래프 진입 98% · 역질문 종료 2%
  주의: 자동응답 9건(1%) — 하네스가 역질문에 대신 답했다(…)

노이즈 바닥(대조군 4개 실측) — 정확도 미측정 · 지연 ±2ms
…(arm 표 · 축 표)…
산출: …/results/scenario/20260921-135452
  설정 스냅샷: config_snapshot.json
  처분 제안: d161_evidence.md, knob_disposition.md, migration_pin.env, recommended.env.diff

판정표를 무효로 표시했습니다 — arm 8개 전부 「판정 불가」 · 불일치 쌍 합 0건.
```

- **통과**: ①`판정을 내지 않습니다` 없음 ②`설정 스냅샷: arm N개 실효값 확인` ③`94 리포트: report.md, summary.json`
  ④`처분 제안:` 4종 ⑤끝난 뒤 남은 파이썬 프로세스 없음.
- **exit 1 과 `판정표를 무효로 표시했습니다` 는 mock 의 정상 결과다** — 모의 응답은 arm 마다 같아 불일치 쌍이 늘 0이다.
  **3단계 통과를 종료 코드로 판정하지 않는다**(A.7 · 코드 결함으로 보고).
- mock 의 `SQL 관측 98%`·`그래프 진입 98%` 는 모의 응답의 값이다 — 제품 품질의 근거가 아니다.

#### 4단계 — `--sweep --mode run --scale smoke` ★ 핵심 관문

- **smoke 는 축만 줄인다(4개 → arm 9개). 워크로드는 전 카탈로그(103건)다.** 그래서 30분이 아니다:
  코드 예고값은 `기동 9회 + 턴 927회 × 20초` ≈ 5.2시간이지만, run 20260914 실측 속도(턴당 약 51초 · arm 당 약 90분)로는
  **약 13~17시간(추정)** 이다. 워크로드를 줄이는 벤치 옵션은 없다(A.7).
- **통과 기준**: ①퀵의 「멈춤」 표 — **차단 0 · 멈춤 주의 0**. 하나라도 보이면 5단계로 가지 않고 이 폴더를 전달한다.
- 건전성 블록 읽는 법 — run 20260914 원시 로그에 **현재 코드**를 적용한 결과(실제 값):

```
건전성 — 유효 프로파일 62/62 · 턴 6567건 · 판정 {'error': 52, 'fail': 4268, 'manual': 1630, 'pass': 617}
  워크로드 도달 — SQL 관측 0% · 그래프 진입 66% · 역질문 종료 56%
  주의: 그래프 진입률 66% — 턴 2235건이 노드를 하나도 밟지 않았다(pre-gate 역질문·요청 거부). …
  주의: 역질문 종료율 56% — 워크로드가 역질문에서 멈췄다면 축이 아니라 역질문 응답 경로를 재고 있다
  주의: 기준선 arm 이 62개 중 62번째로 실행됐다 — 쌍체 지연 비교가 실행 시각과 교란된다. …

판정을 내지 않습니다 — 턴 6567건 중 SQL 이 관측된 턴이 0건이다 — 파이프라인이 SQL 에 닿지 못했거나
하네스가 실행 SQL 을 적재하지 못했다. 어느 쪽이든 SQL 생성률은 신호가 아니라 상수다
```

  그 런이 실제로 찍은 것은 첫 줄과 `오류 표지 314건: status=error http=422` 뿐이었다(`sweep_console.log:13-14`) —
  **오류율 0.8% 라 종전 관문을 통과했고 93시간을 돌았다.** 지금 코드는 위처럼 **차단**하고 주의 3줄을 낸다.
- **검정력을 올리는 것은 `--scale full` 이 아니라 `--repeat` 다.** 축 비교는 (시나리오, 반복) 쌍으로 하므로
  full 은 축을 늘릴 뿐 축당 표본은 smoke 와 같다. smoke 에서 불일치 쌍이 적으면 full 에서도 적다.

#### 5단계 — `--sweep --mode run --scale full` (본 실행)

```bash
nohup python -m scripts.bench --sweep --mode run --scale full > results/bench/nohup.log 2>&1 &
tail -f results/bench/nohup.log
```

- 규모: 현재 1차 축 **29개 → arm 65개**(2026-09-21 `build_arms()` 실측). run 20260914 는 28축·62 arm·6,567턴이었다.
- 소요: run 20260914 실측 **93.4시간**(2026-09-14 18:55 → 09-18 16:18 · arm 당 약 90분 · 턴당 약 51초).
  65 arm 이면 **약 98시간(추정)**. 화면의 `예상:` 줄은 턴당 20초로 계산해 **약 2.5배 짧게** 나온다(그 런 예고 37.4시간 · A.7).
- **현재 스위프는 중단되면 이어지지 않는다. 다시 실행하면 새 폴더에서 처음부터 돈다**(A.7 — 코드 결함 · 사용자 결정 대기).
  그래서 **5단계 전에 4단계(smoke)로 끊김 요인을 먼저 없앤다** — 인증 만료·기동 실패·디스크·서버 재시작 같은 것이 smoke 에서 드러난다.
- 실무 대책: ①`nohup … &` 로 띄워 터미널이 닫혀도 돌게 한다 ②원격 접속이면 `tmux`/`screen` 안에서 띄운다
  ③서버 재부팅·배포 창을 피하는 실행 창을 먼저 합의한다(A.8 6번) ④디스크 여유를 run 20260914 기준(체크포인트 14GB 포함)보다 넉넉히 잡는다.
- 통과 기준은 4단계와 같다.

### A.4 명령이 내부에서 하는 일 (참고 · 개발자가 실행하지 않는다)

```
--sweep
 1. 축 자동 선별   카탈로그(D-129) → F1 → F2 → 1차 축 (smoke 는 앞 4개)
 2. arm 전개       기준선 + 축마다 레벨별 arm(OFAT). 기준선이 맨 앞에 선다(D-238)
 3. 설정 스냅샷    서버를 띄우기 전에 arm 마다 자식 파이썬으로 실효 설정을 읽는다 → 대조군 식별(D-237)
 4. 실행           94 러너 — arm 마다 서버 기동·설정 에코·로그인·시나리오 실행·원시 적재·종료
 5. 94 리포트      report.md · summary.json (D-238)
 6. 건전성 관문    차단이면 여기서 exit 1 — 판정표를 만들지 않는다(D-219)
 7. 판정           노이즈 바닥(대조군 실측) → arm↔기준선 판정 → 축별 레벨↔레벨 판정(D-237)
 8. 후단 관문      전 arm 판정 불가면 판정표에 「무효」를 박고 exit 1
 9. 처분 제안      축 최적 레벨 → §6.5.2 처분 → proposals/ 4종(D-238)
```

- **실제로 도는 실험 설계는 S0(기준선)·S2(OFAT)뿐이다.** §3.3 의 S1(노이즈 반복)·S3(교호작용)·S4(확인)는 구현되지 않았다 —
  노이즈 바닥은 S1 대신 **대조군 arm**(주입값 = 기준선 실효값)으로 잰다(D-237).
- 트랙 T(`python -m scripts.bench`)는 스위프와 **별개 명령**이다 — `--sweep` 은 트랙 T 를 돌리지 않는다.

### A.5 보조 명령 (종전 「내부 명령」 · 개발·디버깅 시에만)

```bash
python -m scripts.bench.axes --explain      # 각 키가 왜 축으로 뽑혔는지/빠졌는지 한 줄씩
python -m scripts.bench.axes --all-tiers    # 2차 축(상한·예산)까지
python -m scripts.bench --sweep --mode dry  # arm 전개만 (1초)
python -m scripts.scenario --run --only A-02,B-01   # 94 러너로 시나리오 몇 건만 실 실행(기준선 프로파일) — plans/99 E-2 방식
```

- 이것 말고 모듈별 CLI 는 없다. `scripts.bench.validate`·`scripts.bench.report` 에는 `__main__` 이 없고
  `scripts.bench.runner` 는 파일이 없다(2026-09-21 실측 · v11 이전 가이드에 적혀 있던 것은 존재하지 않았다).
- 마지막 줄은 **4단계 전에 수 분짜리 표적 확인**이 필요할 때 쓴다 — `results/scenario/<run_id>/report.md` §1 의
  「사다리 단」과 §2 기능 판정에서 역질문·오류로 끝나는 턴이 많은지 본다. 벤치의 건전성 관문은 이 명령에 붙지 않는다.

### A.6 6~8단계 — 결과 읽기 · 처분 제안 · 전달

#### 산출 폴더는 둘이다 (2026-09-21 실측)

| 명령 | 폴더 | 내용 |
|---|---|---|
| `python -m scripts.bench` (트랙 T) · `--propose` | `results/bench/<YYYYMMDD-NN>/` | `SUMMARY.md` · `config_health.md` · `coverage_ledger.md` · (`--propose` 는 `proposals/`) |
| `--sweep` | **`results/scenario/<YYYYMMDD-HHMMSS>/`** | 아래 트리 |

`--sweep` 은 94 러너에 실행을 위임하고 그 러너가 경로를 정한다(`scripts/scenario/runner.py` `RESULTS_ROOT`).
**`SUMMARY.md` 는 트랙 T 전용이다** — 스위프 폴더에는 없다.

```
results/scenario/20260921-135452/        ← 3단계 mock 실측 트리
├── axis_verdicts.md       ★ 판정표 — 6단계에서 읽는다
├── config_snapshot.json     arm 별 실효 설정·대조군 목록(D-237)
├── proposals/               ★ 처분 제안 4종 — 7단계
│   ├── recommended.env.diff
│   ├── knob_disposition.md
│   ├── d161_evidence.md
│   └── migration_pin.env
├── raw.jsonl                원시 관측(판정의 유일한 입력)
├── run.json                 커밋·dirty·프로바이더·arm 별 기동 결과(run 모드는 실효 설정 전체 포함)
├── report.md · summary.json 94 리포트(94 분석기 입력)
├── logs/server-<arm>.log    arm 별 서버 로그
├── artifacts/               폼필 산출 문서
└── checkpoints-<arm>.db     ※ run 모드에만 생긴다 — 전달하지 않는다(8단계)
```

#### 6단계 — 결과 읽기 순서

1. **건전성 블록**(콘솔 · `nohup.log`) — 차단·주의부터. 4단계 기준을 다시 적용한다.
2. **`axis_verdicts.md` 「축 최적 레벨」 표** — *"이 축의 최적 레벨은 무엇인가"*. 판정 3종:
   `최적 레벨`(그 값이 유의하게 낫다) · `레벨 간 차이 없음`(**재서 같다**) · `판정 불가`(**측정이 안 됐다**).
   **「불일치 쌍이 전부 0건」이면 「차이 없음」이 아니다** — 레벨을 바꿔도 결과가 한 건도 안 바뀐 것은 워크로드가 축에
   닿지 않았다는 뜻일 수 있다.
3. **「arm 판정 — 기준선 대비」 표** — 근거다. `(대조군)` 표기가 붙은 arm 은 **주입값이 기준선 실효값과 같다** —
   그 줄의 델타는 축 효과가 아니라 **노이즈 바닥**이다. 머리말의 `실측 노이즈 바닥 — 지연 ±Nms` 를 넘지 못하는 지연 차이는 효과로 읽지 않는다.
   (run 20260914 는 설정이 같은 대조군 3개가 **-2.1초·+2.1초·+5.8초**를 냈다.)
4. **자동응답 주의가 있었다면** 그 턴의 라우팅·매핑·거부 판정은 하네스가 정한 값 위에서 내려진 것이다.
5. **현재 기능 합격률에는 타임아웃·역질문 차단 턴이 포함된다**(G-1 승인 대기).

#### 7단계 — 처분 제안 (`--sweep` 이 이미 만들었다)

- **`--sweep` 이 끝나면 `proposals/` 4종이 이미 있다** — 축 최적 레벨을 §6.5.2 처분 절차에 넣은 결과다(D-238).
- **대조군 레벨이 이기면 `기각`(현행 유지)** 이다 — 현행이 최적이라는 뜻이라 기본값을 바꾸자는 제안이 아니다.
  이 구분이 없으면 현행 유지가 기본값 변경 제안으로 둔갑한다.
- `recommended.env.diff` 는 권고가 있는 축만 키를 쓰고, 없으면 사유만 주석으로 남긴다(`# **권고 변경 0건.**`).
- **어떤 파일도 자동 반영하지 않는다.** 반영은 §6.6 R0~R4 순서로 사람이 한다 — R3(기본값 변경) 전에 `migration_pin.env` 를 먼저 배포한다(§6.7.1).
- 따로 `python -m scripts.bench --propose` 를 치면 **스위프 결과를 읽지 않는다** — 트랙 T 신호만으로 처분을 내고
  `recommended.env.diff` 는 만들지 않는다(`results/bench/<id>/proposals/` 3종). 스위프 뒤에는 칠 필요 없다(A.7).

#### 8단계 — 전달

- **전달할 것**: `results/scenario/<run_id>/` 폴더 — **단 `checkpoints-*.db` 는 빼고**(run 20260914 는 14GB 였고 판정 코드가
  읽지 않는다 · `plans/97` §1). 트랙 T 를 돌렸다면 `results/bench/<id>/` 도.
- **크기**: run 20260914 축소본이 **56MB** 였다(`plans/97` §5 목표 ≤50MB 와의 차이는 미정).
- **반출 스크립트는 없다** — `scripts/bench/triage.py`(`plans/97` §6 · R-8)는 미구현이다. 지금은 손으로 묶는다:

```bash
cd results/scenario && tar czf <run_id>-triage.tar.gz --exclude='checkpoints-*.db' <run_id>/
```

- **주의**: `raw.jsonl` 에는 질의문과 생성 SQL 이 들어간다. 폐쇄망 산출물을 내보낼 때는 기존 반출 절차를 따른다.

### A.7 트러블슈팅 · 알려진 제약

| 증상 | 원인 | 조치 |
|---|---|---|
| `판정을 내지 않습니다 — …SQL 이 관측된 턴이 0건이다` | 파이프라인이 SQL 에 못 닿았거나 커밋에 `executed_sqls` 판독(D-219)이 없다 | 0단계 4번 커밋 확인 → `raw.jsonl` 전달 |
| `판정표를 무효로 표시했습니다` (run 모드) | 설정을 바꿔도 결과가 안 바뀌었다 | 불일치 쌍 합이 0이면 워크로드 문제 — 5단계로 가지 않는다 |
| `판정표를 무효로 표시했습니다` (**mock**) | **정상** — 모의 응답은 arm 간 동일 | 3단계 통과 기준은 종료 코드가 아니라 A.3 3단계 ①~⑤ |
| `주의: 기준선 arm 이 … M번째` | 커밋에 실행 순서 수정(D-238)이 없다 | 0단계 4번 |
| 전 arm `INVALID` · `조용한 강등` | 오케스트레이터 서빙·`deepagents` 설치 | `report.md` §1 사유 · `plans/94` 「▶ 실행 가이드」 ⑪ |
| `접속: 인증이 켜진 서버…인데 벤치 계정이 없습니다` | 계정 미지정 | `--user`/`--password` 또는 `BENCH_USER_*` |
| `※ 운영자 크레덴셜을 찾지 못했습니다` | `ADMIN_USERNAME`/`ADMIN_PASSWORD` 미설정 | 설정 에코가 실패해 arm 이 전부 INVALID — `docs/03` §3.5 |
| 어떤 키를 바꿔도 결과가 같다 | OS env·`.encenv` 가 `.env` 를 덮음 | `config_health.md` 가림 목록(L3) — 그 키는 축에서 자동 제외 |
| `※ 카탈로그에 없는 축 N건은 처분 대상에서 뺐습니다` | 그 사이 키가 삭제·개명됐다 | 정상 — 이름이 출력된다 |
| `.env` 가 수정돼 있다 | **버그** — 이 도구는 설정 파일을 쓰지 않는다(V5) | 즉시 중단하고 보고 |

**알려진 제약(2026-09-21 실측 · 코드 미수정 · 보고됨)**

- **스위프 재개 없음(가장 중요)** — 두 겹이다. ①`_unfinished_run()`(`__main__.py:115-121`)은 `results/bench/` 에서 `SUMMARY.md` 가 없는 폴더를 찾는데 스위프는 `results/scenario/` 에 쓰고 `SUMMARY.md` 를 만들지 않아 **스위프 런을 애초에 찾지 못한다** ②찾더라도 `sweep.run_arms()` 가 러너에 `resume_from` 을 넘기지 않는다(94 러너에는 있다 — `runner.py:114`·`122-124`). 그래서 §0.4 3항 *"같은 명령을 다시 치면 이어진다"* 는 **스위프에 대해 거짓**이다.
- **워크로드를 줄이는 옵션 없음** — `--scale smoke` 는 축 수만 줄인다. `sweep.run_arms(groups=…)` 는 있지만 CLI 에 없다.
- **소요 예고가 약 2.5배 짧다** — 턴당 20초로 계산한다(실측 약 51초).
- **mock 스위프가 늘 exit 1** — 후단 관문이 모드를 구분하지 않는다.
- **`--propose` 가 스위프 결과를 읽지 않는다** — 축 최적값이 반영된 제안은 `--sweep` 산출물에만 있다.
- **preflight 의 「다음 실행에서 이어집니다」 문구가 사실과 다르다** — 트랙 T 는 매번 새 폴더다.
- **로컬 `itam` 이 실 관측 DB 로 판정된다** — `sweep.SANDBOX_DB_IDS` 가 `polestar` 만 샌드박스로 본다.
- **트랙 T L2 가 허위 「기동 실패」 6건을 낸다** — `validate._sample_values` 가 `tristate`·`json_list`·JSON 문자열에 `x` 를 넣는다.

### A.8 폐쇄망 실행 체크리스트 (4·5단계 전)

별도 점검 옵션은 없다(v11 이전의 `--preflight --sweep-ready` 는 존재하지 않는다). 아래를 순서대로 확인한다.

| # | 항목 | 확인 |
|---|---|---|
| 1 | 0단계 4건(사다리 단 · 벤치 계정 · 깨끗한 원격 커밋 · 하네스 수정 포함) | A.2 |
| 2 | `.env` 가 운영 실제값(`LLM_PROVIDER=fabrix`) · 활성 DB | `--show-env` |
| 3 | preflight 통과 | `--preflight` |
| 4 | 트랙 T 즉시 조치가 알려진 L2 허위 6건뿐 | 2단계 |
| 5 | mock 배관 통과 | 3단계 |
| 6 | 실행 창(운영 부하 시간대 회피) 합의 — smoke 도 반나절을 넘긴다 | **사람이 확인**(G-13) |
| 7 | ~~과금 승인~~ | **불요** — 내부망 면제(§4.4) |

### A.9 소요 시간 기준 (2026-09-21 실측 · run 20260914 실측)

| 단계 | 명령 | 규모 | 소요 | 근거 |
|---|---|---|---|---|
| 1 | `--preflight` | — | **2초** | 2026-09-21 로컬 실측 |
| 2 | `--quick` | L1·L3 · 343필드(L3 156건) | **91초**(D-211 기록은 약 80초) | 2026-09-21 로컬 실측 |
| 2 | 기본(트랙 T 전층) | L1~L4 · L2~L4 156건 | **287초(4분 47초)** · 즉시 조치 6건은 L2 허위(A.3 2단계) | 2026-09-21 로컬 실측 |
| 3 | `--sweep --mode mock --scale smoke` | 9 arm · 1,161턴 | **6분 10초** | 2026-09-21 로컬 실측 |
| 4 | `--sweep --mode run --scale smoke` | 9 arm · 약 1,161턴 | **약 13~17시간(추정)** | run 20260914 의 턴당 속도로 환산 |
| 5 | `--sweep --mode run --scale full` | 65 arm | **약 98시간(추정)** · run 20260914 **62 arm 93.4시간(실측)** | `sweep_console.log` · 서버 로그 시각 |

**본 실행은 4일을 넘긴다고 보고 잡는다.** 재개가 없으므로(A.7) 세션이 끊기지 않는 환경(`nohup`)에서 돌린다.

---


## 0. 요약 — 이 계획이 실제로 푸는 문제

### 0.1 해석 정정 — 요청은 3종, 실체는 "선행 1 + 트랙 4"

사용자 지시를 그대로 읽으면 만들 것은 세 개다. 그런데 실측하면 **③이 ②에 의존하고, ②가 ①에 의존하고,
①이 성립하려면 워크로드가 지금보다 커야 한다**. 순서를 뒤집으면 전부 헛돈다.

| # | 이름 | 한 줄 | 산출 코드 | 없으면 생기는 일 |
|---|---|---|---|---|
| **선행 P** | **워크로드 확보** *(v4 정정)* | ~~골드셋 26건 → 80건 신규 작성~~ → **`plans/94` 시나리오 카탈로그(145건)를 소비**하고 축 비교에 쓸 정상 군만 선별 | `testdata/scenarios/`(94 소유) | 리포트가 *"차이 없음"*만 낸다 — 노브당 유의 차이를 검출할 검정력이 없다(§2-②) |
| **트랙 A** | **벤치마크 러너** | 설정 조합(=arm)을 **subprocess로 격리 기동**해 워크로드를 돌리고 원시 측정치를 JSONL로 적재 | `scripts/bench/runner.py` 외 | — |
| **트랙 B** | **리포트 생성기** | 원시 JSONL → 축별 효과·신뢰구간·Pareto·회귀표(Markdown + HTML) | `scripts/bench/report.py` | 숫자는 쌓이는데 읽을 수 없다 |
| **트랙 C** | **최적화 제안기** | 리포트를 읽어 **권고 프로파일**과 **노브 처분 제안**(존치/상수화/삭제)을 생성 — **자동 적용은 하지 않는다** | `scripts/bench/optimize.py` | 사람이 231개 노브를 손으로 판정 |
| **트랙 T** *(v2 신설)* | **전수 설정 검증** | **335필드 전건**을 L1~L4로 검증(카탈로그 정합·기동 안전성·주입 실효성·소비 실증). **성능 스윕이 닿지 않는 나머지를 여기서 덮는다** | `scripts/bench/validate.py` | *"전체 설정 파일을 테스트"* 가 축 12~18개로 축소돼 **317개가 미검증으로 남는다** |

**권고 착수 순서: T → P → A → B → C.** 트랙 T가 맨 앞인 이유는 두 가지다 — **LLM을 한 번도 부르지 않아 지금 시작할 수 있고**,
L3(주입 실효성)이 실패하는 키를 먼저 걸러내지 않으면 그 키를 축으로 삼은 스윕이 통째로 무효가 된다(§2-③·R3).

**개발자가 이 순서를 외울 필요는 없다.** 단일 명령이 순서·의존·재개를 전부 안다(§0.4·「실행 가이드 ② 상세」).

### 0.2 이 계획의 한 문장

**노브를 줄이는 근거를 "정적 감사"(참조 수·수명)에서 "측정"(정확도·지연·비용)으로 한 축 넓히고,
그 측정을 재현 가능한 코드로 고정한다.** 삭제 자체는 본 계획의 산출물이 아니라 `plans/70` P1의 입력이다.

### 0.3 ★ 실측이 바꾼 것 3건

1. **정적 감사표는 이미 있다 — 없는 것은 측정이다.** `docs/flag_audit.md`가 플래그 **43개**를 전수 판정했고
   (기한부 5·상수화 3·존치 35), 판정 규칙 5조는 D-162 §6으로 고정돼 있다. 그런데 **다섯 규칙 중 성능을 보는
   것이 하나도 없다** — 참조 수·`.env` 실제값·생성일만 본다. 본 계획은 **규칙 6조(측정 근거)** 를 추가하는 것이지
   감사표를 다시 만드는 것이 아니다.
2. **A/B 비교 인프라가 이미 있다.** `scripts/eval_text2sql.py:75`의 `AB_AXES` 5축(`synonym_fuzzy`·`value_retrieval`·
   `candidate_count`·`selection`·`semantic_compose`)과 `FLAG_ENV` 매핑(`:65-74`), `build_ab_report`(`:1038`),
   `_run_meta`(`:1194` — 커밋·dirty·벽시계 provenance)가 **2팔 비교까지는 이미 동작한다**. 트랙 A는 신규 하네스가
   아니라 **이것을 N팔로 일반화하고 subprocess 격리를 씌우는 것**이다. 지표도 이미 있다(`aggregate:984` — `ex_rate`·
   `avg_latency_ms`·`total_tokens`·`avg_llm_calls`·`avg_retries`).
3. **"간소화"의 값싼 답은 삭제가 아니라 프로파일이다.** 환경변수 151개(`.env` 실측) 중 운영자가 실제로 만지는 것은
   소수인데, 어느 것이 소수인지가 문서화돼 있지 않다. 삭제는 D-161 4항 실측을 노브마다 요구하는 비싼 경로다.
   **검증된 프리셋 몇 개 + 나머지는 "건드리지 마시오" 등급**이 같은 체감 효과를 훨씬 싸게 낸다(§6.3, G-6).

### 0.4 자동화 책임 경계 — 개발자가 하는 일은 "실행"뿐 *(v2 · 사용자 지시)*

> 사용자 지시: *"개발자의 역할은 단지 벤치마크 테스트를 실행시키는 것 뿐이다."*

이 한 줄이 설계 제약이다. 개발자가 축을 고르거나, 명령을 순서대로 여러 개 치거나, 통계를 해석하거나,
중단된 스윕을 손으로 복구하는 일이 **하나라도 남아 있으면 이 계획은 요건을 못 맞춘 것이다.**

| 주체 | 하는 일 | 하지 않는 일 |
|---|---|---|
| **개발자(실행자)** | ①내부망 접속·`.env` 준비 확인 ②**명령 하나 실행** ③끝나면 산출 폴더 전달 *(v5: 승인 단계 삭제 — 내부망은 프롬프트 없이 진행)* | 축 선별 · YAML 편집 · 단계별 명령 나열 · 통계 해석 · 실패 arm 복구 · 재개 판단 |
| **코드(자동)** | 환경 사전점검 · 축 자동 선별 · arm 전개 · 격리 기동·검증 · 실패 격리 · **자동 재개** · 통계·검정 · 리포트 · 제안서 생성 · 결론 문장 작성 | `.env`·`config.py` 수정(V5) · 폐기 제안 자동 반영 |
| **의사결정자(설계자·운영자)** | 제안서 검토 후 **반영 여부 결정** · 게이트 G-1~G-13 확정 | 벤치마크 실행 |

**개발자 표면의 전부**(자세히는 문서 앞 「실행 가이드 ② 상세」):

```bash
python -m scripts.bench            # 이것 하나. 점검 → 계획 제시 → 실행 → 리포트 → 제안서
```

이 요건이 v1 설계에서 바꾼 것 4가지:

1. **`axes.yaml`은 "사람이 고치는 파일"에서 "자동 생성물"로 바뀐다**(§3.2·§7.1). 카탈로그 인트로스펙션과
   영향 경로 규칙으로 코드가 만들고, 사람 검토는 **선택**이다(`--axes-review`로 열람·동결 가능 — *v12: 이 옵션은 없다. 구현은 `python -m scripts.bench.axes --explain`(열람)·`--write`(동결)*).
2. **단계별 명령을 나열하지 않는다.** 트랙 T·P·A·B·C는 내부 단계이지 개발자가 아는 이름이 아니다.
   단일 명령이 의존 순서를 알고, 앞 단계 산출물이 있으면 건너뛴다.
3. **중단·재개가 개발자 판단을 요구하지 않는다.** 같은 명령을 다시 치면 이어서 간다(`--resume` 불요 ·
   기본 동작) *(스위프는 현재 미동작 · v12 실측 — 다시 치면 새 폴더에서 처음부터 돈다 · 「실행 가이드 ②」 A.7)*. 폐쇄망 며칠짜리 스윕이 전제이므로 이것이 편의가 아니라 요건이다(R2).
4. **리포트가 결론을 낸다**(§5.5). "축 X2는 p=0.03"이 아니라 *"후보 수 3이 기준선보다 정확도 +9%p,
   지연 +2.1s. 권고: 채택"* 까지 코드가 쓴다. 개발자가 통계를 읽고 판단하지 않는다.

**경계의 반대쪽도 명시한다** — 자동화하지 **않는** 것은 셋뿐이고, 전부 근거가 있다.

| 자동화 제외 | 이유 |
|---|---|
| `.env`·`config.py` 실제 수정 | D-161 폐기 절차 우회 · `.env`는 git 미추적이라 복구 근거 소실(§6.1·V5) |
| **외부 프로바이더**(`gemini` 등) 실행 | D-127. 내부망은 면제지만 외부 경로는 승인이 산다 — 코드가 `LLM_PROVIDER`로 자동 판정한다(§4.4 · v5) |
| 게이트 G-1~G-13 확정 | 설계 판단이지 실행이 아니다 |

---

## 1. 실측 현황 (2026-09-11)

### 1.1 노브 규모 — "231개"의 내역

| 대상 | 수 | 근거 |
|---|---:|---|
| `AppConfig` nested 그룹 | **23** | `grep -c "^class .*Config" src/config.py` = 24 (AppConfig 자신 제외) |
| `.env` 실제 등재 키 | **151** | `grep -cE "^[A-Z][A-Z0-9_]*=" .env` |
| `.env.example` 등재 키 | **216** | 동일 명령 |
| 설정 카탈로그 필드(D-129 SSOT) | **335** | `tests/test_api/test_settings_catalog.py:205` 단언(실측 2026-09-11). 같은 파일 `:204` 카탈로그 그룹 **24**(nested 23 + top-level `general` 18필드) · `:599` `UNCONSUMED_KEYS` **20**. ※`plans/91` 기록의 334는 `plans/88` R-E +1 이전 값 |
| `bool` 필드 | **110** | `grep -cE "^\s+[a-z_]+:\s*bool" src/config.py` |
| `int`/`float` 필드 | **121** | `grep -cE "^\s+[a-z_]+:\s*(int|float)\b" src/config.py` |
| `enable_*`/`use_*`/`*_enabled` 플래그 | **43** | `docs/flag_audit.md` (pydantic introspection) |
| tri-state(`bool \| None`) | **2** | `config.py:1226` `enable_semantic_routing` · `:1237` `enable_intent_orchestration` |

**조합 공간은 최소 2^110 이다.** 전수 탐색은 논의 대상이 아니다(§2-①).

### 1.2 재사용할 선행 자산 (신규 작성 금지 목록)

| 자산 | 위치 | 본 계획에서의 역할 |
|---|---|---|
| 플래그 전수 감사표 | `docs/flag_audit.md` (43개 · 판정 규칙 5조) | **축 후보의 1차 필터** — "존치 35" 중에서만 축을 고른다 |
| 설정 카탈로그 | `src/api/settings_catalog.py` (1080 LOC · `field_index()`:582) | 노브 전수 열거·타입·enum·`apply_mode`·`UNCONSUMED_KEYS`(:350)의 SSOT |
| EX 평가 하네스 | `scripts/eval_text2sql.py` (1440 LOC) | 워크로드 실행기·지표 집계(`aggregate`:984)·provenance(`_run_meta`:1194) |
| 라우팅 평가 하네스 | `scripts/eval_routing.py` (415 LOC) | 라우팅 정확도 축 |
| 성능·기능 테스트 스위트 | `docs/29_query_performance_test_plan.md` (A~K군) | **지연 워크로드의 원천** — 이미 목표치(단순≤10s·복합≤30s·문서≤60s)가 군별로 정의돼 있다 |
| 사다리 판정 | `src/observability/ladder.py:131` `record_ladder_resolution` | arm이 **의도한 단으로 실제 기동했는지** 검증(조용한 강등 차단) |
| 단계 트레이스 | `src/observability/trace_collector.py:220` `elapsed_ms_for` | 노드별 지연 분해(어느 노드가 느려졌는지) |
| 골드셋 | `testdata/text2sql_gold/{gp,yd,b0}.yaml`(15·6·5) · `testdata/routing_gold/routing.yaml`(13) | EX 판정의 기존 기반 |
| **시나리오 카탈로그** *(v4)* | `plans/94` 산출 `testdata/scenarios/` — 원천은 `docs/29`(**113건**) + `docs/synonym_test_cases.md`(**32건**) = **145건** | **선행 P의 실체**. 93은 소비만 한다(§1.5) |

### 1.3 운영 실제값(`.env`, 2026-09-11) — 기준선 arm의 정의

```
LLM_PROVIDER=gemini · ORCHESTRATOR_PROVIDER=gemini · DB_BACKEND=dbhub · ACTIVE_DB_IDS=polestar
SCHEMA_CACHE_ENABLED=true · 사다리 1·2·3단 플래그 true(=1단 deep_agent 확정)
COMPOSITE_* 순차 의존 7종 전부 true (D-203 · 코드 기본값도 on)
NOISE_* 대부분 false (13종 중 4종만 true)
```

**기준선(arm `baseline`)은 코드 기본값이 아니라 이 값이다**(D-161 ① — 코드 기본값으로 판단 금지).

### 1.4 내부망(폐쇄망) 전제

- 운영 LLM은 **FabriX**(KBGenAI)다. `LLM_PROVIDER=gemini`는 개발망 값이며, 폐쇄망 벤치마크는 `fabrix`로 돌아야
  측정값이 운영과 같은 의미를 갖는다(G-1).
- FabriX 호출 1건의 벽시계 총상한은 `LLM_FABRIX_TOTAL_TIMEOUT=300`(`config.py:43`, D-198). 벤치마크의 arm당
  타임아웃은 이보다 **작게** 잡아야 한 arm이 스위프 전체를 잡아먹지 않는다.
- 온도·top_k·top_p는 `LLM_FABRIX_LLM_CONFIG` / `LLM_FABRIX_ANSWER_LLM_CONFIG` JSON 문자열이다(D-194).
  **이것은 최적화 대상 축이지, 줄일 노브가 아니다** — 삭제 후보에서 제외한다.

### 1.5 `plans/94`와의 경계 *(v4 신설 · 중복 방지)*

같은 날 병행 작성된 `plans/94`(기능·성능 시나리오 자동 실행 하네스)와 **워크로드를 공유하고 질문이 다르다.**
94 §8이 이 경계를 먼저 정리했고, 본 절은 그것을 93 쪽에서 확인·수용한 기록이다.

| 축 | **93(본 계획)** | **94** |
|---|---|---|
| 질문 | *"이 설정이 저 설정보다 나은가?"* | *"이 기능이 동작하나? 목표 시간 안에 끝나나?"* |
| 판정 | **상대 비교** — arm 간 차이 | **절대 판정** — 합격/불합격 |
| 설정 | 축 12~18개 × 28~41 arm | 운영 1개 + 옵트인 프로파일 소수 |
| 통계 | McNemar·부트스트랩·BH 보정 | 목표 대비 달성률 |
| 산출 | 노브 처분 제안 + 권고 프로파일 | 기능 리포트 + 개선 백로그 |

**공유 인터페이스 3종 — 94가 소유하고 93이 소비한다.** 93은 이것들을 다시 만들지 않는다.

| 자산 | 소유 | 93에서의 쓰임 |
|---|---|---|
| 시나리오 카탈로그 `testdata/scenarios/` | **94** | **선행 P의 실체**(§0.1) — 정상 군만 선별해 축 비교 워크로드로 쓴다 |
| arm 실행 원자(프로파일 기동·에코 검증·JSONL 적재) | **94** | `scripts/bench/runner.py`가 **1 arm = 1 프로파일 실행**으로 호출 |
| 판정·집계 | **94** `assertions.py` | `ex_pass`만 취해 축 비교에 쓴다 |

**R군(복합·오용·실수·착각 75건)은 93의 워크로드가 아니다.** 축 비교는 정답이 정의된 케이스에서만
의미가 있는데, 대응 등급은 설정이 아니라 프롬프트·가드·사전의 함수라 arm 간 신호가 되지 못한다.

**착수 순서**: 94 S0~S3 → **93 B0**(LLM 미호출 문서 작업이라 병행 가능) → 94 S4~S6 → 93 B2~B6.
93의 러너가 94의 실행 원자 위에 서므로 94가 먼저다.

> **트랙 T는 이 경계 밖이다.** 전수 설정 검증(§3.5)은 워크로드가 아니라 카탈로그를 대상으로 하므로
> 94와 겹치지 않고, **선행이 0이라 지금 착수할 수 있다**(Wave T1).

---

## 2. 먼저 인정해야 할 네 가지 제약 (설계가 여기서 갈린다)

### ① 조합 폭발 — "조합을 벤치마크한다"는 문장은 그대로는 실행 불가능하다

2^110은 탐색 불가다. 실행 가능한 것은 **축을 좁힌 뒤의 부분 설계**뿐이다. 본 계획은 3단으로 좁힌다.

```
231 노브
  └─(F1) 성능에 닿을 수 없는 것 제거 ─────────→ 후보 **148** (v7 실측 · 초안 추정 40~60은 과소였다)
        · UNCONSUMED_KEYS(미소비) 20 · 크레덴셜 14 · 접속·저장 정책 20 · 비질의 그룹 133
  └─(F2) 영향 경로 판정 ──────────────────────→ **1차 축 28 · 2차 축 22** (v7 실측)
        · 1차 = 동작 모드를 바꾼다 / 2차 = 상한·예산(정상 경로에서 발동하지 않는다)
        · "존치 35" 중 LLM 호출 수·DB 왕복·프롬프트 길이·재시도 예산을 바꾸는 것만
  └─(F3) 실험 설계 ───────────────────────────→ 실행 arm 20~40회
        · OFAT 주효과 → 상위 효과 축만 2-way 교호작용
```

**전수(full factorial)는 어느 단계에서도 쓰지 않는다.** 축 12개만 해도 4096 arm이고, arm당 워크로드
80건 × LLM 호출 수를 곱하면 폐쇄망에서 며칠 단위다.

### ② 검정력 — 골드셋 26건으로는 정확도 차이를 구분할 수 없다 ★이 계획의 가장 큰 위험

EX 골드셋은 gp 15 + yd 6 + b0 5 = **26건**이다. 1건이 3.8%p다. 두 arm의 EX가 각각 20/26과 22/26이면
차이는 7.7%p지만, 이 표본에서 **통계적으로는 구분되지 않는다**(쌍체 McNemar 기준, 불일치 쌍이 2~3건이면 p≫0.05).

| 목표 검출력 | 필요 워크로드 규모(쌍체·불일치율 가정) | 확보 가능? *(v4)* |
|---|---|---|
| 5%p 차이를 80% 검정력으로 검출 | 대략 **150~250건** | △ 145건으로 하한 근접 |
| 10%p 차이를 80% 검정력으로 검출 | 대략 **60~90건** | ✅ 여유 |

**v4 정정**: `docs/29` **113건** + `docs/synonym_test_cases.md` **32건 = 145건**이 이미 문서로 존재한다
(실측 2026-09-11). 이 중 축 비교에 쓸 수 있는 **정상 군**만 골라도 80건을 넘으므로, v1이 잡았던
*"80건을 새로 만든다"* 는 불필요하다 — **만드는 것이 아니라 기계 판독본으로 옮기는 것**이고 그 소유는
`plans/94`다(§1.5).

→ **판정 대상은 10%p급을 확실히, 5~7%p급을 조건부로 잡는다.** 그 아래는 여전히 `판정 불가`다.
그 아래 차이는 리포트가 **`판정 불가(underpowered)`** 로 표기하고, 그 축은 "성능 근거 없음 = 정적 규칙으로
판정"으로 넘긴다. **없는 유의성을 만들어내지 않는 것이 이 항목의 핵심이다.**

### ③ LLM 비결정성 — 같은 설정도 흔들린다

D-035·Known Mistakes가 반복 기록한 사실이다. 대응 3종:

- **쌍체(paired) 설계** — arm 간 비교는 항상 **같은 질의 id 단위 쌍**으로 한다. 집계 평균 비교보다 분산이 훨씬 작다.
- **반복 n회** — 기본 `--repeat 3` *(v12: 구현 기본값은 **1**이다 — `scripts/bench/__main__.py:582`)*. 정확도는 다수결이 아니라 **건별 성공률(0/⅓/⅔/1)** 로 다룬다.
- **온도 고정** — 벤치마크 중 `LLM_FABRIX_LLM_CONFIG`의 temperature는 arm이 명시적으로 바꾸지 않는 한 고정값을
  주입한다. 고정하지 않으면 모든 축의 효과에 온도 분산이 섞인다.

### ④ 재기동 비용 — 플래그는 기동 시 1회 해석된다

`CLAUDE.md`·D-135 실측: 즉시 반영 7키 / 리로드 78키 / **재시작 148키**. 사다리 단은 `build_graph()` 안에서
**빌드 타임에 배타 확정**된다(D-162 ①). 따라서:

- **arm 1개 = 프로세스 1회 기동.** 인프로세스에서 config만 갈아끼우는 것은 사다리·노드 등록을 바꾸지 못해
  **측정값이 거짓이 된다**.
- 러너는 `subprocess`로 자식 프로세스를 띄우고 **env로만** 설정을 주입한다(`.env` 파일을 수정하지 않는다 — 병행
  세션·작업 트리 오염 금지).
- 자식의 기동 로그에서 `오케스트레이션 사다리 확정: tier=…`(`ladder.py:114`)를 파싱해 **의도한 단으로 떴는지
  검증**하고, 다르면 그 arm을 `INVALID`로 버린다(조용한 강등이 측정에 섞이는 것을 막는다).

---

## 3. 축 선별 — 231개에서 12~18개로

### 3.1 F1 — 성능에 닿을 수 없는 노브 제외 (코드 없이 카탈로그로 결정)

`settings_catalog.field_index()`를 돌려 아래를 기계적으로 제외한다. **판정은 코드가 하고 사람이 검토한다.**

| 제외 규칙 | 근거 | 대략 수 |
|---|---|---|
| `UNCONSUMED_KEYS`에 있음 | 어디서도 읽지 않음(`settings_catalog.py:350`) | 20 |
| `SENSITIVE_VALUE_KEYS`·`_MANUAL_SECRET_KEYS` | 크레덴셜 — 성능 축 아님 | 12+ |
| 타입이 URL·경로·호스트·포트 | 접속 대상이지 동작 모드가 아님 | 40± |
| 이름이 `*_retention_days`·`*_maxlen`·`*_dir` | 보존·저장 정책 | 15± |
| `noise_gate`/`alarm`/`workb` 그룹 전체 | **질의 응답 경로 밖**(알람 파이프라인) — 별도 벤치마크 대상(G-4) | 60± |

### 3.2 F2 — 축 후보 (아래 표는 **자동 선별 규칙의 기대 출력**이지 손으로 유지하는 목록이 아니다)

> **v2 정정**: v1은 이 표를 사람이 `axes.yaml`로 옮겨 적는 것으로 뒀다. 개발자 역할이 실행뿐이므로(§0.4),
> **코드가 카탈로그에서 축을 만들고 이 표는 그 출력이 맞는지 검토할 때 쓰는 기대값**이 된다. 자동 선별이
> 이 표와 다른 축을 뽑으면 그 자체가 리포트에 `축 선별 차이` 항목으로 남는다(선별 로직의 회귀 감시).

`flag_audit` "존치" 판정 + 아래 **영향 경로**가 실재하는 것만 축으로 올린다.
영향 경로는 네 가지뿐이다: **(a) LLM 호출 수 (b) DB 왕복 수 (c) 프롬프트 길이 (d) 재시도·폴백 예산.**

| # | 축 | env 키 | 값역 | 영향 경로 | 실측 근거 |
|---|---|---|---|---|---|
| X1 | **사다리 단** | `ENABLE_DEEPAGENTS_PACKAGE` · `ENABLE_INTENT_ORCHESTRATION` · `ENABLE_SEMANTIC_ROUTING` | 1단/2단/3단/4단 | a·b·c·d 전부 | `docs/21` · `graph.py` `build_graph()` |
| X2 | 다중 후보 수 | `TEXT2SQL_CANDIDATE_COUNT` | 1·3·5 | **a 직접 배수** | `config.py:290` |
| X3 | 후보 선택 전략 | `TEXT2SQL_SELECTION` | consistency·llm·hybrid | a | `config.py:295` |
| X4 | 결정적 조합 | `TEXT2SQL_SEMANTIC_COMPOSE` | on/off | a 감소·정확도 | `config.py:282` |
| X5 | 복잡도 게이트 | `TEXT2SQL_COMPLEXITY_GATE` | on/off | a | `config.py:293` |
| X6 | SQL 재시도 예산 | `QUERY_MAX_RETRY_COUNT` | 1·3·5 | **d** | `QueryConfig` |
| X7 | 스키마 캐시 | `SCHEMA_CACHE_ENABLED` · `SCHEMA_CACHE_BACKEND` | on/off · redis/file | **b·c** | `config.py:623` |
| X8 | 2단 라우팅 | `ROUTER_TWO_STAGE_ENABLED` | on/off | a | `RouterConfig` |
| X9 | 유사어 퍼지/시맨틱 | `TEXT2SQL_SYNONYM_FUZZY` · 시맨틱 매칭 | on/off | b·c·정확도 | `SynonymMatchConfig` |
| X10 | 복합 fan-out | `COMPOSITE_FANOUT_CONCURRENCY` · `COMPOSITE_MAX_TARGETS` | 1·3·5 / 5·10·20 | **b·지연** | `config.py:1071·1073` |
| X11 | 순차 의존 계약 | `COMPOSITE_SEQUENTIAL_*` 7종 | 묶음 on/off | b·정확도 | `config.py:1093-1110` (D-203) |
| X12 | 가용성 사전판정 | `COMPOSITE_AVAILABILITY_PRECHECK_ENABLED` | on/off | b | `config.py:1118` (D-175) |
| X13 | 호스트 소재 탐색 | `COMPOSITE_HOST_DISCOVERY_ENABLED` · `DISCOVERY_EARLY_EXIT` | on/off | **b(존당 SELECT)** | `config.py:1131·1134` |
| X14 | 범위 선택 역질문 | `COMPOSITE_SCOPE_SELECT_ENABLED` | on/off | 지연(HITL 턴) | `config.py:1144` |
| X15 | LLM 온도(결정적) | `LLM_FABRIX_LLM_CONFIG.temperature` | 0.0·0.3 | 정확도·재시도 | D-194 |
| X16 | LLM 온도(응답) | `LLM_FABRIX_ANSWER_LLM_CONFIG.temperature` | 0.3·0.5·0.7 | 응답 품질 | D-194 |
| X17 | 관측 오버헤드 | `OBS_TRACE_ENABLED` · `OBS_SQL_LOG_ENABLED` | on/off | 지연(I/O) | `config.py:612·614` |
| X18 | HITL 승인 게이트 | `ENABLE_STRUCTURE_APPROVAL` | on/off | **지연(사람 대기)** | 기본 on |

**X11(7종)은 개별 축이 아니라 한 묶음으로 다룬다** — D-203이 "순차 의존 계약"이라는 하나의 계약으로 묶었고,
쪼개면 arm 수가 128배가 된다. 묶음 안의 개별 기여는 상위 효과가 확인된 뒤에만 분해한다(G-5).

**X18은 측정 대상이지만 자동 스위프에서 제외한다** — HITL은 사람 응답 시간이 지연에 섞여 재현 불가다.
리포트에는 "측정 제외 · 사유" 행으로 남긴다(침묵 제외 금지).

### 3.3 F3 — 실험 설계

| 단계 | 설계 | arm 수 | 목적 |
|---|---|---:|---|
| **S0 기준선** | 운영 `.env` 그대로 | 1 | 모든 비교의 기준 |
| **S1 무결성** | 기준선 반복 3회 | 3 | **측정 노이즈 상한 확정** — 같은 설정의 arm 간 차이가 축 효과의 하한선이다 |
| **S2 OFAT** | 축 1개씩만 기준선에서 이동 | 12~18 | 주효과. 각 축의 부호와 크기 |
| **S3 교호작용** | S2 상위 효과 축 중 상호작용이 의심되는 **쌍만** | 6~10 | 예: X2(후보 수) × X3(선택 전략), X7(캐시) × X1(사다리 단) |
| **S4 확인** | S2·S3이 지목한 후보 프로파일 2~3개 | 6~9 (반복 포함) | 권고안이 기준선을 실제로 이기는지 |

**실측 62 arm**(2026-09-11 · 1차 축 28개 · bool은 2수준·수치는 3수준). 초안 추정 28~41은 수치 축의 3수준을 세지 않은 값이다.
arm당 워크로드는 94 카탈로그의 정상 군 32건 → **1984회 실행 · 내부망 10~30시간 규모**(G-13에서 실행 창 확정).
`--scale smoke`(축 4개 · 9 arm)로 파이프라인을 먼저 확인하고 `--scale full`로 넘어간다 — 명령이 규모와 예상 시간을 먼저 고지한다.

**S1을 건너뛰지 않는다.** 같은 설정을 3번 돌려 얻은 편차가 곧 *"이 벤치마크가 구분할 수 있는 최소 차이"*다.
이 값 없이 S2의 차이를 해석하면 노이즈를 효과로 읽는다(E1 A/B 사고 2026-08-04와 같은 유형).

### 3.5 트랙 T — 전수 설정 검증: 스윕이 닿지 않는 317개를 덮는다 *(v2 신설 · 사용자 지시)*

> 사용자 지시: *"전체적인 설정 파일을 테스트하고 보고서를 만들 수 있는 계획을 수립해야 한다."*

#### 3.5.1 "전체 설정 테스트"는 두 가지로 갈린다 — 하나는 전수 가능하다

§2-①이 보인 대로 **성능 스윕의 전수는 불가능**하다(2^110). 그런데 지시를 실측 관점에서 다시 읽으면
*"전체 설정 파일을 테스트한다"* 는 **성능 측정만을 뜻하지 않는다.** 운영자가 겪는 *"설정이 어렵다"* 의 실체는
성능보다 앞선 곳에 더 많이 있다.

| 실제 질문 | 어느 층이 답하나 | 전수 가능? |
|---|---|---|
| *"이 키가 먹기는 하나?"* | L4 소비 실증 | **✅** |
| *"값 범위가 뭐지? 이렇게 넣으면 뜨나?"* | L2 기동 안전성 | **✅** |
| *"분명 바꿨는데 왜 안 바뀌지?"* | L3 주입 실효성 | **✅** |
| *"`.env.example`에 있는데 코드엔 없는 키 아닌가?"* | L1 카탈로그 정합 | **✅** |
| *"어느 값이 빠른가?"* | L5 성능 효과 | ❌ 축 12~18만 |

→ **L1~L4를 335필드 전건으로 돌리고, L5만 축으로 좁힌다.** 이러면 *"전체 설정 파일을 테스트했다"* 가
빈말이 아니게 되고, **L1~L4는 LLM을 한 번도 호출하지 않아** 환경 준비만으로 지금 시작할 수 있다.

#### 3.5.2 5층 구조

| 층 | 검증 내용 | 대상 | 실 LLM | 실행 시간(추정) | 산출 |
|---|---|---|---:|---|---|
| **L1** | **카탈로그 정합** — `.env` / `.env.example` / `config.py` / `settings_catalog` **4자 대조**. 고아 키(파일에만 있음)·누락 키(코드에만 있음)·타입 불일치·설명 부재·`.encenv` 가림 대상·중복 등재 | 335 + 파일 키 전건 | **0** | 초 | 정합 표 |
| **L2** | **기동 안전성** — 각 키를 경계값(bool 2종·enum 전 값·int min/max/0/음수·빈 문자열)으로 주입하고 **config 로드가 성공하는가**. 실패는 `ValidationError` 메시지까지 기록 | 335 × 1~5 값 | **없음** | **3~5분**(대표값 1개 · v7 실측 0.5s/건) | 값역 표 + 실패 목록 |
| **L3** | **주입 실효성** — 주입값 = **자식이 실제로 읽은 값**인가. OS env·`.encenv` 가림, `AliasChoices` 순서 함정(D-162 실측), 인라인 주석 파손을 전수로 잡는다 | 335 | **0** | L2에 편승 | 가림 목록 |
| **L4** | **소비 실증** — 값을 바꾸면 **동작이 실제로 달라지는가**. mock 워크로드로 프롬프트 렌더·생성 SQL·`node_path`의 해시가 변하는지 본다. 안 변하면 *노브 환상* 후보 | 축 밖 키 포함 가능한 범위 | **0** | 1~2시간 | 무효 노브 후보 |
| **L5** | **성능 효과** — 정확도·지연·비용 | 축 12~18 | **있음** | §3.3 | 축별 효과표 |

#### 3.5.3 L2를 **경량 모드**로 도는 이유 (실측 근거 있는 함정 회피)

`config.py:1188-1191` 주석이 못박은 실측이 있다 — **nested config를 인스턴스 기본값으로 두면 모듈 임포트
시점의 env로 고정되어, 이후 `os.environ` 변경 + `load_config.cache_clear()`가 반영되지 않는다**(2026-07-15
E1 하네스 A/B 무효화 실측). 그래서:

- **같은 프로세스에서 env를 바꿔가며 335필드를 도는 방식은 쓰지 않는다.** 값이 반영되지 않아 전 항목이
  거짓 통과한다. §2-④와 같은 이유다.
- **경량 모드** = 필드마다 자식 프로세스를 새로 띄워 **config 로드까지만** 하고 자기 config를 JSON으로
  에코한 뒤 종료한다. 서버·그래프 빌드를 하지 않아 1~2초면 끝난다.
  ```
  python -c "from src.config import load_config, ...; print(json.dumps(<실효 설정 전체>))"
  ```
- **서버 모드**는 그래프 배선에 영향을 주는 키에만 쓴다 — 카탈로그 `apply_mode`가 `restart`이면서 사다리·
  노드 등록에 관여하는 것(사다리 3키·`ACTIVE_DB_IDS`·`DB_BACKEND` 등 20개 내외). 전수 335를 서버로 돌면
  기동 비용이 수 시간이고 얻는 것이 없다.

L3는 L2의 에코 산출물을 그대로 재사용한다 — **주입값 사전 vs 에코 사전의 차집합**이 곧 가림 목록이다.
별도 실행이 필요 없다.

#### 3.5.4 L4 소비 실증 — `UNCONSUMED_KEYS`를 사람 손에서 떼어낸다

현재 "미소비" 판정은 `settings_catalog.py:350`의 `UNCONSUMED_KEYS` **손으로 관리하는 frozenset**이다
(D-129가 16개로 시작해 현재 20개). 손 관리는 필연적으로 낡는다 — 소비가 생겨도 목록에서 안 빠지고,
소비가 사라져도 안 들어온다.

L4는 이것을 **동적으로 반증**한다.

```
기준 렌더 : mock 워크로드 N건 → (프롬프트 해시, 생성 SQL 해시, node_path, 호출 계획) 수집
키별 렌더 : 키 하나만 기본값과 다른 값으로 → 같은 수집
판정      : 네 해시가 전부 동일 → "동작 불변(노브 환상 후보)"
            하나라도 다름        → "소비 실증됨"
```

- **판정 방법은 `scripts/prompt_render_diff.py`가 프롬프트에 쓰는 기법과 같다**(렌더 결과 해시 비교).
  다만 그 스크립트는 프롬프트 정본 전환 전용이라 **도구를 재사용하는 것이 아니라 방식을 차용**한다.
- **"동작 불변"은 "삭제해도 된다"가 아니다.** mock 워크로드가 그 키의 경로를 지나지 않았을 뿐일 수 있다.
  리포트는 `노브 환상 후보`로만 적고, `UNCONSUMED_KEYS`와 **대조표**를 낸다:
  | 대조 결과 | 의미 | 조치 |
  |---|---|---|
  | 목록에 있고 L4도 불변 | 일치 — 미소비 확인 | 유지 |
  | 목록에 없는데 L4가 불변 | **목록이 놓친 후보** | 참조 grep으로 확인 후 등재 검토 |
  | 목록에 있는데 L4가 변함 | **목록이 낡음** — 소비가 생겼다 | 목록에서 제거 |

세 번째 칸이 이 층의 진짜 값어치다. 손 관리 목록의 거짓을 **자동으로** 드러낸다.

#### 3.5.5 패키지 경계 — 어디까지가 "전체 설정"인가

본체 `AppConfig` 밖에도 설정 원천이 있다(D-139 패키지 경계). v2는 **범위를 명시하고 나머지는 미포함을
밝힌다**(침묵 제외 금지).

| 원천 | 위치 | 트랙 T 범위 | 사유 |
|---|---|---|---|
| 본체 `AppConfig` 23그룹 | `src/config.py` | **포함**(L1~L5) | 본 계획의 대상 |
| `mcp_server` | `mcp_server/config.toml` · `mcp_server/mcp_server/config.py` | **L1만**(존재·정합 확인) | 별도 프로세스·별도 cwd. 타임아웃·max_rows는 **MCP 서버가 관리**(CLAUDE.md 보안 절)라 본체 스윕의 축이 아니다 |
| `sre_agent` | `sre_agent/sre_agent/settings.py` `AgentSettings` | **제외** | 별도 venv·별도 프로세스. 조사 경로는 워크로드 밖 |
| `.encenv`(시크릿) | 루트 | **L1·L3만** | 값은 읽지 않는다. **가림 대상 키 목록**으로만 쓴다 |
| `noise_gate` 관련 키 | `AppConfig.noise_gate` 등 | **L1~L4 포함 · L5 제외** | 알람 워크로드는 성격이 달라 별도 벤치마크(G-4) |

→ **L5(성능)는 질의 경로만, L1~L4(검증)는 본체 전체.** 이 비대칭이 *"전체 설정 파일을 테스트"* 의 실행
가능한 정의다.

---

## 4. 트랙 A — 벤치마크 러너 (산출물 ①)

### 4.1 위치와 경계

```
scripts/bench/
  __main__.py      # ★ 단일 진입 — `python -m scripts.bench` (점검→계획→실행→리포트→제안)
  __init__.py
  preflight.py     # 환경 사전점검(의존·DB·LLM 도달성·디스크·기존 산출물) · 개발자 대신 진단한다
  axes.py          # 축 **자동 선별**(카탈로그 F1 → 영향경로 F2) · arm 전개(OFAT/쌍) · axes.yaml 생성
  validate.py      # 트랙 T L1~L4 (전수 설정 검증)
  runner.py        # arm별 subprocess 격리 실행 · 원시 JSONL 적재 · **자동 재개**
  workload.py      # 워크로드 로더(골드셋 + docs/29 파생) · 쌍체 id 관리
  probe.py         # 자식 프로세스 기동 검증(사다리 단·설정 실효값 에코) — L2·L3와 공유
  report.py        # 트랙 B (설정 건강 + 성능 + 커버리지 장부)
  optimize.py      # 트랙 C (권고 프로파일·처분 제안·D-161 4항)
config/bench/
  axes.yaml        # **자동 생성물**(검토·동결용 · v1의 "사람이 고치는 파일" 아님 — §3.2 v2 정정)
  profiles.yaml    # 기준선·후보 프로파일 정의(자동 생성 + 사람 추가 가능)
testdata/bench/
  workload_*.yaml  # 선행 P 산출물
results/bench/<run_id>/   # 산출 전량이 이 한 폴더에 모인다(개발자는 이 폴더만 전달한다)
```

**`preflight.py`가 있는 이유**: 개발자가 실패 원인을 진단하지 않게 하려면(§0.4) 코드가 먼저 진단해야 한다.
*"왜 안 되죠"* 를 묻기 전에 *"`ibm-db` 미설치 → b0 워크로드를 건너뜁니다"* 를 명령이 먼저 말한다.

**`src/`를 건드리지 않는다.** 계층 규칙상 `scripts/`는 `arch_check.py` 대상 밖이고(패키지 내 `scripts/` 제외),
벤치마크는 운영 경로에 배선되지 않는다. **신규 config 필드 0 · 신규 `enable_*` 플래그 0**(D-162 주의 준수).

### 4.2 arm 실행 계약

한 arm의 생애는 다음과 같다. **모든 단계가 실패 시 그 arm을 `INVALID`로 표시하고 다음으로 넘어간다(스위프 중단 금지).**

```
1. env 조립   : os.environ 사본 + arm의 축 값 오버라이드 (.env 파일 미수정)
2. 기동       : subprocess.Popen([python, -m, src.main, --server, --port <free>])
                또는 --query 단발 모드(G-8에서 택일)
3. 기동 검증  : stdout/stderr에서 `오케스트레이션 사다리 확정: tier=X` 파싱
                → 의도한 단과 다르면 INVALID(degraded_reason 기록)
                → 헬스 엔드포인트 200까지 대기(상한 90초)
4. 워밍업     : 워크로드 앞 2건은 버린다(캐시·연결 풀 예열 — 측정 대상 아님)
5. 측정       : 워크로드 전건 순차 실행. 건별로
                {id, ok, ex_pass, latency_ms, llm_calls, tokens, retries, node_path, error}
6. 종료       : SIGTERM → 5초 후 SIGKILL. 포트 회수 확인
7. 적재       : results/bench/<run_id>/raw.jsonl 에 append (arm 1건당 워크로드 건수 줄)
```

**재개(resume)**: `raw.jsonl`에 이미 있는 `(arm_id, workload_id, repeat_idx)`는 건너뛴다.
폐쇄망에서 며칠짜리 스위프가 중간에 끊기는 것을 전제한다.

### 4.3 측정 지표 (전부 `eval_text2sql.aggregate` 재사용 또는 확장)

| 지표 | 출처 | 비고 |
|---|---|---|
| `ex_pass` (정확도) | `eval_text2sql` EX 판정 | 결과집합 동등성 |
| `ex_subset_pass` | 동일 | 부분 일치(관대 판정) |
| `latency_ms` | `perf_counter` 왕복 + 응답 `processing_time_ms` 대조 | **둘을 다 남긴다** — 벌어지면 런이 얼어붙은 신호(`_run_meta:1199`) |
| ~~`llm_calls` · `tokens`~~ | — | **측정 불가** — `/query/stream` 의 `done` 페이로드에 없다. `src/` 를 고치지 않고는 얻을 수 없으므로 **추정치를 넣지 않는다**(v8 정정) |
| `node_count` | SSE `node_start` 수 | **비용 축 대체** — 파이프라인이 한 일의 양 |
| `retries` | SSE `node_start` 의 `query_generator` 재진입 수 | 재시도는 지연과 비용 양쪽. 서버 수정 없이 실측된다(v8) |
| `node_path` · 노드별 `elapsed_ms` | `trace_collector` (`OBS_TRACE_ENABLED=true`) | **어느 노드가 느려졌는지** — 축 효과의 기전 설명 |
| `tier` · `degraded_reason` | 기동 로그 | arm 유효성 |
| `sql_error_kind` | 감사 로그 | 실패 유형 분류 |

**p50·p95를 평균과 함께 남긴다.** 평균만으로는 "가끔 아주 느림"이 보이지 않는데, 목표치(단순 ≤10s 등)는
꼬리에서 깨진다.

#### 4.3.0 워크로드 환경 — `closed` 가 기본이다 *(v8 신설 · 2026-09-14 실측 대응)*

94 카탈로그의 정상군 139건은 `env` 로 갈려 있다.

| `env` | 그룹 | 건수 | 내용 |
|---|---|---:|---|
| `closed` | A~K | **107** | 실 질의 워크로드 — 라우팅·조회·성능통계·알람·복합·HITL·멀티턴·폼필·가드·부하 |
| `sandbox` | L | 32 | 유사어·용어(SYN A~I) — 로컬 도커 샌드박스 전용 |

**종전 기본값이 `sandbox` 여서 실 스위프가 유사어 32건만 재고 실 질의를 한 건도 건드리지
않았다**(실측 2026-09-14 · 두 런 3968턴). 폐쇄망이 이 계획의 전제이므로(§1.4) 기본을
`closed` 로 바꾸고, 활성 DB로 자동 판정한다 — 로컬 샌드박스(`polestar`)만 활성이면 `sandbox`,
폴스타 실 DB가 하나라도 활성이면 `closed`. `--env` 로 덮어쓸 수 있다.

**실행 첫 화면에 워크로드 구성을 적는다.** 필터가 조용히 걸리면 결과가 비어도 "원래 그런 것"으로
읽힌다 — 그 한 줄이 없어서 두 번의 런이 끝나도록 아무도 알아채지 못했다.

```
  환경: closed — 실 관측 DB 활성(polestar_cm_gp) — closed
  워크로드: 시나리오 107건 — 그룹 A(12), B(12), C(13), … · 프롬프트 미작성 21건은 건너뛴다
```

#### 4.3.0-1 단언 이관 — 정확도를 잴 수 있게 만들었다 *(v9 · 2026-09-14)*

정상군 107건 중 **기계 단언이 있는 시나리오는 1건**이었다. 나머지는 기대값이 전부
`manual_review` 산문이라 정확도 쌍이 언제나 0쌍이다 — 인증을 고쳐도 성공한 런과 실패한
런이 같은 문장으로 나온다. 산문을 단언으로 옮겼다.

| 군 | 전체 | 기계 단언 | 비고 |
|---|---:|---:|---|
| A 라우팅 | 12 | 10 | `intent` 는 응답에 없어 **`db_ids`** 로 본다. 비조회 질의는 "SQL 을 만들지 않았는가"로 |
| B 조회(EAV) | 12 | 12 | `r.name` vs `r.hostname` · `stringvalue` vs `_short` · NUMERIC 캐스트(D-160) · DB2 `FETCH FIRST` |
| C 성능통계 | 13 | 11 | **절대 기간만 값을 못박고** 상대 기간("지난달")은 모양(`'20\d{4}'`)만 — 값을 박으면 다음 달에 전건 거짓 불합격 |
| D 알람 | 6 | 5 | `cmm_alarm_active` · `alarmseverity` · 명시 건수 LIMIT |
| E 복합 | 7 | 7 | 복합 경로는 `executed_sql` 에 마지막 태스크만 남아 태스크 연결은 사람이 본다 |
| F 존 HITL | 8 | 8 | 미작성 3건을 2턴 HITL 로 작성 · 존 누수(D-154) 부정 단언 |
| G 멀티턴 | 4 | 4 | **승계의 증거** — 2턴 질의에 호스트명이 없는데 SQL 에는 있어야 한다 |
| H 폼필 | 17 | 16 | 17건이 전부 같은 샘플을 올리고 있었다 → 양식별 실 픽스처로 교체(없던 3종은 생성) |
| I 폼필 HITL | 8 | 8 | 표현 불가였다 → `turns[].endpoint` 신설로 해소(아래) |
| J 가드 | 10 | 5 | DML·다중문 부정 단언 · LIMIT. 마스킹·PII·업로드 한도는 픽스처/설정 의존 |
| K 부하 | 10 | 3 | 나머지는 반복·동시성 프로토콜이라 단일 시나리오로 표현되지 않는다 |
| **계** | **107** | **89** | 러너가 실제로 돌리는 98건 기준 **87건(89%)** — A-05·A-10 은 teardown 미지원으로 건너뛴다(v10 정정) |

**막고 있던 것 셋을 먼저 고쳤다.**

1. **`intent` 는 관측되지 않는다** — `done` 페이로드에 없어 그대로 대조하면 전건 거짓
   불합격이다. 사유를 남기고 라우팅은 `db_ids` 로 본다.
2. **모의 실행이 canned 응답을 판정한다** — `mock:` 블록이 없으면 모의 서버는 고정 SQL 을
   돌려준다. 여기에 내용 단언을 걸면 **모의 서버를 판정한 결과**가 불합격으로 쌓인다.
   블록이 없을 때만 내용 단언을 보류한다.
3. **턴마다 엔드포인트를 못 바꾼다** — 폼필 답변 턴은 `form_fill_answers` 를 JSON 으로
   보내야 하는데 `/query/file` 은 그 필드를 받지 않는다. `turns[].endpoint` 를 신설했다.

**실행 전에 판정 가능 비율을 적는다.** 이 비율이 보이지 않으면 1건과 107건이 구별되지 않는다.

```
  워크로드: 시나리오 107건 — 그룹 A(12), B(12), … · 9건은 러너가 건너뛴다(프롬프트 미작성·teardown 미지원)
            정확도 판정 가능 87/98건(89%) — 나머지는 완주율·지연만 잰다
```

#### 4.3.1 판정 신호 2순위 — 완주율·SQL 생성률 *(v8 신설 · 2026-09-14 실측 대응)*

**정확도만으로는 아무것도 재지 못하는 상태였다.** 94 카탈로그의 정상군 시나리오 32건은 기대값이
전부 `manual_review` 산문뿐이라 **기계 단언이 0개**다(실측 2026-09-14). 판정기는 단언이 없으면
`manual`을 주고 93은 `manual`을 정확도 비교에서 제외하므로, 정확도 쌍은 **항상 0쌍**이다.
이 상태에서는 성공한 런과 전건 401인 런이 같은 문장(`판정 불가`)으로 나온다.

그래서 판정 신호를 2단으로 둔다.

| 순위 | 신호 | 어디서 오나 | 사람 손 |
|---:|---|---|---|
| 1 | **정확도** (`passed`) | 시나리오에 옮겨진 기계 단언 | 필요 — 단언을 옮겨 적어야 한다 |
| 2 | **완주율** (`completed`) | `func_verdict`·`response_mode` (오류·크래시·행이 아닌가) | 불요 |
| 2 | **SQL 생성률** (`sql_generated`) | `executed_sql` 비었는가 | 불요 |

- 1순위 쌍이 0쌍일 때만 2순위로 내려간다. **이름을 바꿔 적는다**(`정확도` → `완주율`) — 무엇으로
  판정했는지 문장이 숨기지 않는다.
- 2순위는 `manual` 건도 센다. "정답인지 모른다"와 "끝까지 돌지 않았다"는 다른 사실이다.
- 2순위는 설정 축이 실제로 흔드는 값이다 — 재시도 폭주·생성 실패·조기 종료가 전부 여기 찍힌다.
- **1순위를 대체하지 않는다.** 단언을 옮겨 적으면 자동으로 1순위로 복귀한다(코드 변경 없음).

#### 4.3.2 건전성 관문 — 판정 이전의 질문 *(v8 신설 · **v10에서 확장** 2026-09-21)*

판정을 내기 전에 **이 런이 판정할 자격이 있는가**를 먼저 묻는다. 아래에 걸리면 통계를 내지 않고
사유와 대표 오류 표지를 출력한 뒤 `exit 1` 한다.

**전단 관문 — 판정 이전의 신호로 막는다** (`sweep.RunHealth.blocking_reason`)

| 조건 | 뜻 |
|---|---|
| 유효 프로파일 0개 | 어떤 arm도 기동 검증을 통과하지 못했다 |
| 실행 턴 0건 | 아무것도 돌지 않았다 |
| **무효 턴 = 전체** *(v10)* | 측정이 성립한 턴이 하나도 없다(D-218 `invalid`) |
| 오류율 ≥ 50% | 측정이 아니라 사고다 |
| **SQL 관측 턴 0건** *(v10)* | 질의 벤치마크인데 SQL 이 한 건도 관측되지 않았다 — 파이프라인이 못 닿았거나 하네스가 못 읽었고, 어느 쪽이든 「SQL 생성률」은 신호가 아니라 **상수**다 |

**후단 관문 — 판정표가 아무것도 판정하지 못한 것도 사고다** *(v10 신설 · `__main__.cmd_sweep`)*

| 조건 | 처리 |
|---|---|
| 전 arm 이 「판정 불가」 | 판정표를 쓰되 **머리말에 「이 판정표는 무효다」를 박고** 불일치 쌍 합·도달 지표를 함께 적은 뒤 `exit 1`. §6.5.2 7행(보류)의 입력으로 보내지 않는다 |

**고지만 하는 것 — 차단하지 않는다** *(v10 신설 · `RunHealth.warnings()`)*

임계가 워크로드 구성의 함수라 못 박으면 정상 런을 막는다. 대신 **침묵하지 않는다**.

| 지표 | 고지 조건 |
|---|---|
| 그래프 진입률 | < 80% — 노드를 하나도 밟지 않은 턴에는 설정 축이 작용할 여지가 없다 |
| 역질문 종료율 | ≥ 30% — 축이 아니라 역질문 응답 경로를 재고 있을 수 있다 |
| 무효 턴 | 1건 이상 — 분모에서 뺐다는 사실 자체를 남긴다 |
| 기준선 실행 순서 | 1번째가 아님 — 쌍체 지연이 실행 시각과 교란된다 |

**근거 ①**(v8): 2026-09-14 실 스위프에서 1984턴이 **전량 HTTP 401**이었는데도 산출된
`axis_verdicts.md`는 62개 arm에 대해 "판정 불가 — 실 모드에서 정확도를 다시 잰다"만 찍었다.
오류율은 통계의 입력이 아니라 **통계를 낼지 말지를 정하는 선행 조건**이다.

**근거 ②**(v10 · run `20260914-185540` 실측 · `plans/97` §0.5): **오류율만으로는 부족하다.**
그 런은 오류율 **0.8%**로 위 관문을 여유 있게 통과했지만 6567턴 전건 `executed_sql=null`,
그래프 미진입 34%, 역질문 56%였고, 62 arm 중 60개가 기준선과 **합격 시나리오 집합이 동일**했다.
결과로 나온 판정표 62줄 중 61줄이 「불일치 쌍 0건」이었는데 **형식은 정상**이라 그대로 처분
파이프라인에 흘러들 수 있었다. 관문은 "오류가 났는가"가 아니라 **"측정이 축에 닿았는가"**를
물어야 한다.

### 4.4 실행 게이트 — 내부망 면제 · 외부 프로바이더 유지 *(v5 개정 · 사용자 확정)*

> 사용자 확정: *"내부망에서 밴치마크 테스트를 진행할 예정이라 과금 승인은 필요 없다."* → **G-7 해소.**

**승인 게이트를 지우는 것이 아니라 조건부로 만든다.** 지시를 *"승인 로직 전부 삭제"* 로 읽으면 개발망에서
Gemini로 스모크를 돌릴 때 무승인 외부 호출이 되고, 그것은 D-127 위반이자 사용자 의도도 아니다.

| `LLM_PROVIDER` | 승인 프롬프트 | 옵트인 env | 근거 |
|---|---|---|---|
| **`fabrix`**(내부망 KBGenAI) | **없음** | **없음** | 사내 인프라 — 외부 과금 API가 아니고 데이터가 밖으로 나가지 않는다(D-120과도 정합). **사용자 확정 2026-09-11** |
| `gemini` · 기타 외부 | **유지**(예상치 제시 후 1회) | `RUN_BENCH=1` *(v12: 코드에 없다 — 구현은 `--yes` · `__main__.py:335-338`)* | **D-127 존속** — 내부망 면제는 내부망에만 적용된다 |
| `ollama`(로컬) | 없음 | 없음 | 로컬 프로세스 |

- **판정은 코드가 한다.** 개발자가 어느 쪽인지 알 필요도, 플래그를 외울 필요도 없다(§0.4).
  `preflight`가 프로바이더를 읽어 *"내부망 FabriX — 승인 없이 진행합니다"* 를 한 줄로 알린다(침묵 금지).
- `--dry-run`(arm 전개·검증만) / `--mock`(주입형 예측기)은 **디버깅 수단으로 남는다.** 더 이상 "과금을 막는
  기본 동작"이 아니라, 파이프라인을 LLM 없이 확인하는 용도다.
- **`--estimate`는 유지한다.** 승인 근거가 아니라 **소요 시간·서버 부하 예고**로 쓴다 — 내부망에서도 10~30시간
  스위프가 언제 끝날지는 알아야 하고, 운영 시간대를 피할 근거가 된다(§A.8 6번).

**여전히 유효한 안전 제약**(과금과 무관한 것들):

- DB는 **읽기 전용만**(D-003). 벤치마크가 쓰기를 하는 경로는 없다.
- 결과 JSONL은 `trace_writer._sanitize`(`:125`)와 같은 마스킹을 거친다 — PII는 내부망에서도 남기지 않는다.
- 동시성 1 · 운영 부하 시간대 회피(R6). **승인이 없어진 것이지 부하가 없어진 것이 아니다.**
- DB는 **읽기 전용만**(D-003). 벤치마크가 쓰기를 하는 경로는 없다.
- 결과 JSONL에 PII가 섞이지 않도록 `trace_writer._sanitize`(`:125`)와 같은 마스킹을 거친다.

### 4.5 신뢰성 장치 (E1 A/B 사고 재발 방지)

`_run_meta`(`eval_text2sql.py:1194`)가 기록하는 provenance를 **run 단위와 arm 단위 양쪽에** 남긴다.

- run: 커밋 sha·dirty 여부·시작 벽시계·호스트명·python 버전·설치 패키지 해시
- arm: **실효 설정 전체 스냅샷**(자식 프로세스가 기동 직후 자기 config를 직렬화해 에코 — 우리가 주입했다고
  믿는 값이 아니라 **자식이 실제로 읽은 값**)

주입값과 에코값이 다르면 그 arm은 `INVALID`다. `.encenv`·OS env 우선순위(D-129 실측: OS env > `.encenv` > `.env`)
때문에 **주입이 조용히 무시되는 경우가 실재한다** — 이 장치가 없으면 "효과 없음"으로 오독한다.

#### 4.5.1 스냅샷 이행 상태 *(v10 신설 2026-09-21 · 실측)*

위 「arm: 실효 설정 전체 스냅샷」은 **오래 미이행 상태였다.** run `20260914-185540` 의
`run.json` arm 레코드 키는 9개뿐이다 — `name·port·valid·tier·degraded_reason·echo_ok·
echo_mismatch·auth_enabled·reasons`. **불일치만 남고 실효값 자체가 없다.** 그래서
*"이 arm 의 주입값이 기준선 실효값과 같은가"*(= 대조군인가)를 사후에 판정할 수 없었다.

| 누가 | 무엇을 | 상태 |
|---|---|---|
| **93** | `sweep.capture_config_snapshot` — 서버 기동 **전에** arm 마다 자식 파이썬으로 실효 설정을 읽어 `config_snapshot.json` 산출 | **이행(v10)** |
| 94 | `server.verify_profile` 가 라이브 서버에서 받은 `effective` 전체(`server.py:349`)를 `ProfileStatus` 에 보존 | **잔여** — `scripts/scenario/` 는 `plans/94` 소유(§1.5) |

- 93 이 쓰는 경로는 `probe.echo_config`(자식 프로세스 · `probe.py:131`)다. 자식은 서버와
  **같은 호스트·같은 `.env`/`.encenv`·같은 `ISOLATION_ENV`** 로 `load_config()` 를 한 번 한다 —
  §4.5가 말하는 *"자식이 실제로 읽은 값"* 그 자체다. 비용은 arm 당 1~2초(62 arm ≈ 2분).
- **비결정 필드는 자동 제외**한다 — `probe.detect_nondeterministic_keys`(`probe.py:220`)가
  이미 `auth.jwt_secret`(미설정 시 기동마다 생성)을 잡는다. 하드코딩 목록을 새로 만들지 않는다.
- 민감값은 에코 단계에서 `sha256:` 접두 해시로 바뀌므로(`probe.py` `_safe`) 스냅샷에 평문이 남지 않는다.
- 94 잔여가 닫히면 **라이브 서버의 실효값**이 추가돼 자식 에코와 교차 검증이 가능해진다.
  그때까지는 자식 에코가 유일한 스냅샷이다.

---

## 5. 트랙 B — 리포트 생성기 (산출물 ②)

### 5.1 입력·출력

```
입력 : results/bench/<run_id>/raw.jsonl  (+ 이전 run들 — 회귀 비교용)
출력 : results/bench/<run_id>/report.md     사람이 읽는 정본
       results/bench/<run_id>/report.html   표·차트 포함(선택 · G-8)
       results/bench/<run_id>/summary.json  트랙 C의 입력
```

### 5.2 리포트 구성 (섹션 순서 고정)

1. **실행 요약** — run_id·커밋·기간·arm 수·유효/INVALID·총 LLM 호출·총 토큰
2. **측정 노이즈 상한(S1)** — 기준선 반복 간 EX 편차와 지연 편차. **이 절이 맨 앞에 온다.**
   이후 모든 차이는 이 값과 대비해 읽어야 하기 때문이다.
3. **축별 주효과(S2)** — 축마다 한 행:
   | 축 | 값 | ΔEX (95% CI) | Δp50 지연 | Δp95 지연 | ΔLLM 호출 | Δ토큰 | 판정 |
   판정은 `유의(+)` · `유의(−)` · **`판정 불가(underpowered)`** · `무효(INVALID arm)` 넷 중 하나.
4. **교호작용(S3)** — 쌍별 표. 주효과 합과 실제 차이의 괴리를 명시.
5. **Pareto 전선** — 정확도 vs 지연 vs 비용 3축. 지배당하는 arm은 접어둔다.
6. **목표치 대조** — `docs/29` 목표(단순 ≤10s·복합 ≤30s·문서 ≤60s·라우팅 ≤5s) 대비 arm별 달성률.
7. **노드별 지연 분해** — 효과가 큰 축에 한해, 어느 노드의 `elapsed_ms`가 움직였는지. **기전 설명 없는 효과는
   신뢰하지 않는다**는 원칙의 장치다.
8. **INVALID 사유 목록** — 버린 arm과 이유(조용한 강등·주입 무시·타임아웃). **비우지 않는다.**
9. **재현 명령** — 이 리포트를 다시 만드는 정확한 명령줄.

### 5.3 통계 처리 (과소·과대 주장 금지)

- 정확도: **쌍체 McNemar 검정** + Wilson 신뢰구간. 표본이 부족하면 p값 대신 `판정 불가`.
- 지연: 분포가 치우치므로 평균 t검정이 아니라 **부트스트랩 신뢰구간**(건별 쌍 차이 재표본).
- 다중비교: 축이 12~18개이므로 **Benjamini–Hochberg** 보정을 적용하고, 보정 전/후를 둘 다 표기한다.
- **효과 크기를 항상 함께 적는다.** "유의하지만 0.5%p"는 운영 판단에서 무의미하다.

#### 5.3.1 비교 단위는 둘이다 — arm 대 기준선 · **레벨 대 레벨** *(v10 신설 2026-09-21)*

종전 구현은 **모든 arm 을 기준선하고만** 비교했다(`compare.judge`). 그러면 불린 축은
`-true`·`-false` 두 줄이 각각 기준선과 비교돼 나오는데, **정작 "켰을 때 vs 껐을 때"는
어디에도 계산되지 않는다.** 이 벤치마크의 목적이 그것인데도 그렇다.

| 단위 | 묻는 것 | 함수 | 판정 어휘 |
|---|---|---|---|
| arm ↔ 기준선 | "이 값이 **지금 설정보다** 나은가" | `compare.judge` | 채택 권고·조건부·기각·차이 없음·판정 불가 |
| **레벨 ↔ 레벨** | "**이 축의 최적 레벨은 무엇인가**" | `compare.compare_levels`·`compare.optima` | **최적 레벨**·**레벨 간 차이 없음**·판정 불가 |

- **어휘를 섞지 않는다.** arm 판정 5어휘는 §5.5의 폐쇄 집합 그대로 두고, 축 판정은
  전용 2어휘(`BEST_LEVEL`·`LEVELS_TIED`) + `판정 불가`를 쓴다 — 묻는 것이 다르기 때문이다.
- 레벨이 3개 이상인 축(`COMPOSITE_FANOUT_CONCURRENCY` 1·3·6)은 쌍이 3건이므로
  **축 안에서도** Benjamini–Hochberg 를 돌린다(축 사이 보정과 같은 장치).
- 우세 레벨이 둘 이상으로 갈리면(순위 비일관) **최적을 만들어내지 않는다** — `판정 불가`.
- **arm ↔ 기준선 판정은 남긴다.** 기준선 대비 절대 위치는 마이그레이션 판단(§6.7)의 입력이고,
  지우면 회귀다. 리포트는 축 표를 **먼저**, arm 표를 근거로 낸다.

#### 5.3.2 대조군 arm — 노이즈 바닥을 실측한다 *(v10 신설 2026-09-21)*

OFAT 전개는 축마다 모든 레벨을 펴므로, **주입값이 기준선 실효값과 같은 arm**이 구조적으로
생긴다(코드 기본값 == `.env` 실효값인 경우). 그 arm 은 설정이 기준선과 **동일**하므로
측정된 "축 효과"가 정의상 **순수 노이즈**다.

- `sweep.ConfigSnapshot.control_arms()` 가 실효 설정 지문(비결정 필드 제외)이 기준선과
  같은 arm 을 자동 식별한다. 판정표는 그 arm 을 **(대조군)** 으로 표기하고, 축 판정 문장은
  어느 레벨이 대조군인지 적는다.
- 그 arm 들의 쌍체 델타가 **노이즈 바닥**이다 — `compare.latency_noise_floor`(지연) ·
  `compare.noise_floor`(정확도, 대조군을 기준선 반복으로 투입). **`--repeat 1` 런에서도
  바닥을 실측할 수 있는 유일한 경로**다(종전에는 반복이 없으면 0.0을 썼다).
- 실측(run 20260914-185540 · 대조군 3개): 지연 **±4,009ms** · 정확도 **2.0%p**.
  같은 런의 판정표가 축 효과로 보고한 지연 델타는 -8.6초 ~ +12.7초로 **같은 크기대**였다.
- **스냅샷이 없으면 대조군 판정을 하지 않는다**(추정 금지). 과거 런은 「대조군 판정 불가」로
  명시하고 넘어간다.

### 5.4 설정 건강 보고서 + 전건 커버리지 장부 *(v2 신설 · "전체 설정 파일" 요건의 산출물)*

성능 리포트(§5.2)와 **별도 문서**로 낸다. 독자가 다르기 때문이다 — 성능 리포트는 *"어느 값이 빠른가"*,
건강 보고서는 *"내 설정 파일이 멀쩡한가"* 에 답한다. 후자는 **벤치마크를 돌리지 않아도 나온다**(L1~L4는 LLM 미호출).

```
results/bench/<run_id>/config_health.md      설정 건강 보고서 (사람이 읽는 정본)
results/bench/<run_id>/config_health.json    기계 판독본 (트랙 C 입력 · CI 게이트 가능)
results/bench/<run_id>/coverage_ledger.md    전건 커버리지 장부 — 335필드 한 줄씩
```

#### 5.4.1 건강 보고서 구성

| # | 절 | 내용 | 심각도 |
|---|---|---|---|
| 1 | **즉시 조치** | 기동을 깨뜨리는 값·주입이 먹지 않는 키·고아 키 | **높음** |
| 2 | 카탈로그 정합(L1) | 4자 대조 불일치 전건. `.env`에만·코드에만·타입 충돌·설명 부재 | 중 |
| 3 | 값역 표(L2) | 필드별 허용 범위와 **실제로 거부된 값**. `ValidationError` 원문 | 낮 |
| 4 | 가림 목록(L3) | OS env·`.encenv`가 `.env`를 덮고 있는 키. **"바꿨는데 안 바뀜"의 원인 목록** | **높음** |
| 5 | 노브 환상 후보(L4) | 동작 불변 키 + `UNCONSUMED_KEYS` 대조표(§3.5.4) | 중 |
| 6 | 등급 제안 | A 환경결속 / B 운영레버 / C 내부상수급(§6.3) 초안 배정 | — |
| 7 | 전건 커버리지 요약 | 아래 장부의 집계 | — |

#### 5.4.2 전건 커버리지 장부 — **미측정을 숨기지 않는다**

335필드 **전건**이 한 줄씩 들어간다. 이 장부가 *"전체 설정 파일을 테스트했다"* 의 증거이자,
동시에 *"어디까지만 했는지"* 의 정직한 고백이다.

| 키 | 그룹 | 타입 | L1 | L2 | L3 | L4 | L5 | 등급 | 미측정 사유 |
|---|---|---|---|---|---|---|---|---|---|
| `TEXT2SQL_CANDIDATE_COUNT` | text2sql | int | ✅ | ✅ | ✅ | ✅ | ✅ X2 | B | — |
| `OBS_TRACE_RETENTION_DAYS` | observability | int | ✅ | ✅ | ✅ | ⚪ | ⚪ | C | 보존 정책 — 질의 경로 무관(F1 제외) |
| `NOISE_FLAPPING_ENABLED` | noise_gate | bool | ✅ | ✅ | ✅ | ✅ | ⚪ | C | 알람 파이프라인 — 별도 벤치마크(G-4) |
| `LLM_FABRIX_API_KEY` | llm | secret | ✅ | ⚪ | ✅ | ⚪ | ⚪ | A | 시크릿 — 값 미취급 |

기호: ✅ 검증됨 · ❌ 실패(조치 필요) · ⚪ **미측정 + 사유 필수** · ⏭ 해당 없음

> **규칙: ⚪에 사유가 비어 있으면 리포트 생성이 실패한다.** 침묵 제외 금지 원칙의 기계적 강제다.
> 이 한 줄이 *"전체를 테스트했다"* 가 빈말이 되는 것을 막는 유일한 장치다.

#### 5.4.3 CI 게이트로 쓸 수 있다 (선택 · G-9)

L1·L3는 초 단위이고 LLM을 부르지 않아 **평소 게이트로 상시 실행**할 수 있다.

```bash
python -m scripts.bench --quick --ci     # L1·L3만. 고아 키·가림·타입 충돌이 생기면 exit 1
```

`.env.example`에 키를 추가하고 `config.py`에 안 넣는(혹은 그 반대) 드리프트를 **커밋 시점에** 잡는다.
D-129가 카탈로그를 SSOT로 만든 뒤에도 `GROUP_ORDER` 미등재로 UI에서 그룹이 통째로 탈락한 전례가 있다
(D-184 부기 — 카운트 테스트가 실패 상태로 방치돼 있었다). 그 유형을 자동으로 막는다.

### 5.5 리포트가 결론을 낸다 — 개발자는 통계를 해석하지 않는다 *(v2 · §0.4 요건)*

각 표의 마지막 칸은 숫자가 아니라 **문장**이다. 판정 어휘는 다섯 개로 고정한다.

| 판정 | 뜻 | 트리거 조건 |
|---|---|---|
| **채택 권고** | 기준선보다 낫고 부작용이 목표치 안 | 효과가 노이즈 상한 초과 + 보정 후 유의 + 목표치 위반 없음 |
| **조건부 권고** | 한 지표는 좋아지고 다른 지표는 나빠짐 | Pareto 비지배이나 트레이드오프 존재 → **무엇과 무엇을 맞바꾸는지 문장으로 명시** |
| **기각** | 기준선보다 나쁨 | 유의하게 악화 |
| **차이 없음** | 효과가 노이즈 상한 이하 | → 처분 규칙 6조 대상(§6.2) |
| **판정 불가** | 검정력 부족 또는 arm 무효 | → **성능 근거 없음. 정적 규칙으로 판정** |

**리포트 맨 앞 "한 눈에" 블록**(개발자가 그대로 전달할 수 있는 형태):

```
run 20260911-01 · 커밋 9e73cc9(clean) · 폐쇄망 FabriX · 소요 11h20m
설정 건강 : 즉시 조치 2건(가림 키 TEXT2SQL_SELECTION · 고아 키 OBS_LEGACY_DUMP)
성능      : 채택 권고 3 · 조건부 1 · 차이 없음 7 · 판정 불가 2 · 무효 arm 1
권고 요약 : 후보 수 3 채택(+9%p 정확도 / +2.1s) · 스키마 캐시 redis 유지 · 2단 라우팅 기각
다음 할 일: proposals/ 폴더를 설계자에게 전달 (자동 반영 없음)
```

**`다음 할 일` 줄이 항상 있다.** 개발자가 *"그래서 뭘 하죠"* 를 묻지 않게 하는 것이 이 블록의 목적이다.

---

## 6. 트랙 C — 최적화 제안기 (산출물 ③)

### 6.1 원칙 — 제안까지만, 적용은 사람이

`optimize.py`는 **`.env`·`config.py`를 수정하지 않는다.** 산출물은 세 가지 문서다.

| 산출물 | 내용 | 소비처 |
|---|---|---|
| `recommended.env.diff` | 기준선 대비 권고 설정 **diff 형식 제안** | 운영자가 검토 후 손으로 반영 |
| `knob_disposition.md` | 노브별 처분 제안 표 (존치/상수화/기한부/삭제) + **근거** | `plans/70` P1 입력 · `docs/flag_audit.md` 갱신 |
| `d161_evidence.md` | 삭제·상수화 제안 노브마다 **D-161 4항 실측 자동 수집** | 폐기 제안의 필수 첨부 |

**자동 적용을 하지 않는 이유**: 환경변수 삭제는 D-161이 정의한 *폐기 제안*이고, 4항 실측 첨부 없는 폐기 제안은
반려된다. 자동 반영은 그 반려 절차를 우회하게 된다. 또한 `.env`는 git 미추적이라 자동 수정은 복구 근거가 남지 않는다.

### 6.2 노브 처분 규칙 — D-162 §6의 5조에 **6조를 더한다**

기존 5조(`docs/flag_audit.md`)는 위에서부터 먼저 맞는 것을 적용한다. 본 계획은 그 **앞에** 한 조를 넣는다.

| # | 조건 | 판정 | 신설 여부 |
|---|---|---|---|
| **0** | **벤치마크에서 유의한 성능 효과(정확도 또는 지연 또는 비용)** | **존치 + 권고값 명시** | **신설(D-211)** |
| 1 | 프로덕션 참조 0건 | 기한부 | 기존 |
| 2 | `.env` 실제값이 코드 기본값과 다름 | 존치(상수화 금지) | 기존 |
| 3 | 프로덕션 참조 3건 이상 | 존치 | 기존 |
| 4 | 참조 1~2건 · 기본값 ON | 상수화 | 기존 |
| 5 | 참조 1~2건 · 기본값 OFF | 기한부 | 기존 |
| **6** | **벤치마크에서 효과가 노이즈 상한 이하 + 규칙 2~3으로 "존치"** | **"존치하되 운영자 비노출"**(§6.3 등급 C) | **신설(D-211)** |

**규칙 0을 맨 위에 두는 이유**: 규칙 2가 `.env`에 값이 있다는 이유만으로 존치를 강제하는데, 그 값이 **성능에
무해하다는 측정이 있으면** 존치의 근거가 "누군가 켜뒀다"뿐이다. 규칙 0과 6은 그 경우를 갈라준다.

**규칙 6이 "삭제"가 아니라 "비노출"인 이유**: 효과 없음은 삭제 근거가 못 된다 — 측정하지 않은 워크로드에서
효과가 있을 수 있고(우리 워크로드는 질의 경로 중심이다), 안전·감사 목적 노브는 성능 효과가 0인 것이 정상이다.

### 6.3 간소화의 실물 — 노브 3등급 + 프로파일

**"151개를 줄인다"의 실행 가능한 정의**는 삭제 수가 아니라 **운영자가 보는 수**다.

| 등급 | 정의 | 목표 수 | 노출 |
|---|---|---:|---|
| **A. 환경 결속** | 그 설치에서 반드시 정해야 함(접속 정보·활성 DB·프로바이더) | 15~25 | `.env` 상단 · 웹UI 기본 화면 |
| **B. 운영 레버** | 벤치마크에서 효과가 확인돼 **권고값과 함께** 제공 | 10~15 | `.env` · 웹UI · **권고값 주석 필수** |
| **C. 내부 상수급** | 효과 없음 또는 안전·감사 목적. 기본값 사용 | 나머지 | `.env.example`에서 **접기** · 웹UI 고급 섹션 |

**프로파일 프리셋**(G-6에서 채택 여부 확정):
`config/bench/profiles.yaml`이 정의한 검증된 조합을 `.env.example`에 **주석 블록으로** 제공한다.
예: `# --- 프로파일: 폐쇄망 운영(정확도 우선) --- ` 아래에 등급 B 키의 권고값만 나열.

> **주의(D-162)**: *"플래그 부채를 정리하면서 플래그를 늘리는 것은 자기모순"*이다.
> 따라서 **`CONFIG_PROFILE` 같은 신규 설정 키는 만들지 않는다**. 프로파일은 문서·주석·웹UI 프리셋 버튼으로만
> 존재하고, 실체는 여전히 개별 env 키다. 이 제약을 뒤집으려면 G-6에서 사용자 확정이 필요하다.

### 6.4 D-161 4항 실측 자동 수집

`d161_evidence.md`는 삭제·상수화 후보마다 네 항을 **코드로** 채운다.

| 항 | 수집 방법 |
|---|---|
| ① 운영 설정 실제값 | `.env`·`.encenv` 파싱(카탈로그의 alias 우선) — **코드 기본값이 아니라** |
| ② 런타임 가용성 | 관련 패키지 `importlib.metadata` 조회 · 관련 엔드포인트 설정 유무 |
| ③ 최근 개발 활동 | `git log -1 -S'<키>' -- <참조파일들>` + **`git merge-base --is-ancestor`로 현 브랜치 소속 확인** |
| ④ 역방향 의존 | 워드 경계 grep(`-w`)으로 참조 파일 전수 — `.venv` 제외(flag_audit 측정 오염 1번 사례) |

**`.venv` 제외를 기본값으로 넣는다.** `docs/flag_audit.md`가 기록한 실사례: 벤더 venv 오염이 `trace_enabled`를
52건(실제 5건)으로 부풀렸다.

### 6.5 리포트 → 처분 결정 파이프라인 *(v3 신설 · "레포트를 어떻게 분석하나")*

리포트가 나온 뒤 노브 하나하나를 어느 처분으로 보낼지는 **순서가 정해진 결정 절차**다.
`optimize.py`가 이 절차를 코드로 실행하고, 사람은 그 결과를 검토한다.

#### 6.5.1 입력 신호 — 리포트에서 뽑아 쓰는 것

| 출처 | 신호 | 값역 |
|---|---|---|
| L1 | 정합 상태 | `정상` · `고아 키` · `누락 키` · `타입 충돌` · `설명 부재` |
| L2 | 값역·기동 | `전 값 성공` · `일부 거부` · `기본값도 실패` |
| L3 | 주입 실효 | `실효` · **`가림`**(OS env·`.encenv`가 덮음) |
| L4 | 소비 실증 | `변함` · `불변` · `미검사` |
| L5 | 성능 판정 | `채택 권고` · `조건부` · `기각` · `차이 없음` · `판정 불가` · `측정 안 함` |
| 정적 | 참조 수 · `.env` 실제값 · 생성 경과일 | `flag_audit` 방식 재수집 |

#### 6.5.2 결정 절차 — 위에서부터 먼저 맞는 것을 적용한다

**0단계(간소화가 아니라 결함 수정)가 맨 앞에 온다.** 깨진 설정을 안고 간소화를 논하면
"줄였더니 안 된다"가 된다.

| 순 | 조건 | 처분 | 성격 |
|---:|---|---|---|
| **0-a** | L2 = `기본값도 실패` | **즉시 수정** — 기동 불가 결함 | 결함 |
| **0-b** | L1 = `고아 키`(파일에만 있음) | **파일에서 제거** — 코드가 읽지 않는다 | 결함 |
| **0-c** | L1 = `누락 키`(코드에만 있음) | **`.env.example`에 등재** | 결함 |
| **0-d** | L3 = `가림` | **즉시 조치 + 축에서 제외** — 이 키는 측정 자체가 불가 | 결함 |
| **1** | L4 = `불변` **AND** 참조 0건 | **삭제 제안**(D-161 4항 첨부 · §6.4) | 간소화 |
| **2** | L4 = `불변` **AND** 참조 1~2 **AND** 기본값 ON | **상수화 제안** | 간소화 |
| **3** | L5 = `채택 권고` | **기본값을 권고값으로 변경** + **등급 B** + 권고값 주석 | 최적화 |
| **4** | L5 = `조건부` | **등급 B 유지 + 트레이드오프 문장 병기** — 기본값은 바꾸지 않는다 | 최적화 |
| **5** | L5 = `기각` | **기본값 유지 + "권장하지 않음" 주석** | 최적화 |
| **6** | L5 = `차이 없음` **AND** 참조 3건 이상 | **등급 C 강등**(존치·비노출 — 처분 규칙 6조) | 간소화 |
| **7** | L5 = `판정 불가` | **보류** — 정적 규칙(1~5조)으로 폴백, 재측정 대기 목록에 등재 | 보류 |
| **8** | L5 = `측정 안 함`(축 밖) | **등급 C 기본 배정** + L1~L4 결과만 반영 | 간소화 |
| **9** | `.env` 실제값 ≠ 코드 기본값 **AND** 위 어디에도 안 걸림 | **존치**(규칙 2 — 살아있는 레버) | 존치 |

**규칙 1·2가 L5보다 위에 있는 이유**: 동작이 변하지 않는 키는 성능을 볼 것도 없다.
반대로 **규칙 6이 규칙 1보다 아래인 이유**: 성능 차이가 없어도 참조가 많으면 그 코드가 실제로
그 값을 읽고 있다는 뜻이라 삭제가 아니라 비노출이 맞다.

#### 6.5.3 처분별 산출 형식

각 처분은 `proposals/knob_disposition.md`에 한 행으로 쌓이고, **실행 가능한 형태**를 함께 낸다.

| 처분 | 함께 내는 것 | 반영 단계(§6.6) |
|---|---|---|
| 즉시 수정(0-a~0-d) | 수정할 파일·줄·전후 값 | **R0** |
| 삭제 제안 | `d161_evidence.md` 4항 + 제거 대상 파일 목록 + 그 값을 단언하는 **테스트 목록** | R4 |
| 상수화 제안 | 남길 상수값 + 제거할 분기 위치 + 테스트 목록 | R4 |
| 기본값 변경 | `config.py` 전후 값 + **마이그레이션 필요 여부**(§6.7) | R3 |
| 등급 배정 | A/B/C + `.env.example` 배치 위치 | R1·R2 |
| 보류 | 재측정에 필요한 워크로드 규모(검정력 역산) | — |

### 6.6 간소화 실행 — 위험도 순 5단계 롤아웃 R0~R4 *(v3 신설)*

> **기호 주의**: §3.3의 실험 설계 단계(S0 기준선·S1 무결성·S2 OFAT·S3 교호작용·S4 확인)와 구분하려고
> 롤아웃은 **R**을 쓴다. S는 *측정하는 순서*, R은 *바꾸는 순서*다.

**한 번에 다 바꾸지 않는다.** 되돌리기 쉬운 것부터, 런타임 위험이 없는 것부터 간다.

| 단계 | 바꾸는 것 | 런타임 위험 | 되돌리기 | 체감 간소화 기여 | 승인 |
|---|---|---|---|---|---|
| **R0 결함 수정** | 고아 키 제거 · 누락 키 등재 · 가림 해소 · 기동 실패 값 교정 | **0**(오히려 복구) | 1커밋 | — | 낮음 |
| **R1 문서 재편** | `.env.example` 3등급 재배치 · C등급 접기 · **권고값 주석** · 프로파일 주석 블록 | **0** — 런타임 무영향 | `git revert` | **큼** | 낮음 |
| **R2 노출 재편** | 웹UI 기본/고급 분리 · 카탈로그 등급 메타 · 미소비 뱃지 갱신 | **0** — 값 불변 | 1커밋 | **큼** | 낮음 |
| **R3 기본값 변경** | 코드 기본값을 권고값으로 | **중** — 미명시 설치의 동작이 바뀐다 | 1커밋 + `.env` 명시로 무력화 | 중 | 중간 |
| **R4 삭제·상수화** | env 키 제거 · 분기 제거 · 테스트 갱신 | **높음** | 어려움 | 작음 | **높음** · D-161 4항 |

#### ★ 이 표가 말하는 것 — 체감 간소화의 대부분은 무위험 구간에서 나온다

*"설정하기 너무 어렵다"* 는 **운영자가 보는 수**의 문제다. R1·R2는 런타임을 전혀 건드리지 않으면서
그 수를 줄인다. 반대로 R4(삭제)는 위험이 가장 크면서 운영자 체감에는 거의 기여하지 않는다 —
이미 접혀서 안 보이던 것이 사라질 뿐이다.

→ **권고: R0~R2를 먼저 완주하고, R3·R4는 별건으로 판단한다.** R4는 본 계획의 산출물이 아니라
`plans/70` P1의 입력이다(§13).

#### 6.6.1 단계별 커밋 규율

- **처분 1건 = 커밋 1건**(R3·R4). `docs/flag_audit.md`가 이미 *"플래그당 1커밋"* 규율을 쓴다.
- 커밋 메시지에 **근거 run_id와 처분 규칙 번호**를 적는다: `config(bench): X2 기본값 n1→n3 (run 20260911-01 · 규칙 3)`
- R1·R2는 묶어도 된다(런타임 무영향).

### 6.7 회귀 방어 — R3·R4가 조용히 동작을 바꾸지 않게 *(v3 신설)*

#### 6.7.1 R3(기본값 변경)의 진짜 위험

`.env`에 명시되지 않은 키는 코드 기본값을 쓴다. 그 기본값을 바꾸면 **이미 배포된 설치의 동작이
말없이 바뀐다.** 폐쇄망에 여러 설치가 있으면 어느 설치가 영향받는지도 알기 어렵다.

**방어: 기본값을 바꾸기 전에 현재 기본값을 `.env`에 박는다.**

```
① 마이그레이션 생성   현재 실효값을 그대로 .env에 명시하는 블록을 만든다(값 변화 0)
② 배포·확인           설치들이 그 블록을 반영. 이 시점에 동작 변화 0
③ 코드 기본값 변경     .env가 명시하고 있으므로 기존 설치는 불변, 신규 설치만 새 기본값
```

`optimize.py`는 R3 제안마다 **①의 블록을 함께 생성**한다(`proposals/migration_pin.env`).
이 블록 없이 기본값을 바꾸는 제안은 내지 않는다.

#### 6.7.2 비트 동일성 가드 (기존 관행 재사용)

이 저장소는 이미 *"OFF 골든 + sha256 동일"* 방식을 쓴다(D-134 · D-128). 그대로 쓴다.

| 대상 | 가드 | 기존 자산 |
|---|---|---|
| 프롬프트 렌더 | 변경 전후 sha256 동일 | `tests/test_semantic_routing/test_prompt_byte_identity.py` · `tests/test_prompt_render_matrix.py` |
| 생성 SQL | 골든 스냅샷 동일 | `tests/text2sql/test_semantic_golden.py` |
| 카탈로그 | 그룹 집합·필드 집합 등가 | `tests/test_api/test_settings_catalog.py:204-205` |
| 경로 대칭 | 단일/멀티 동일 | `tests/test_nodes/test_path_parity.py` |

**R1·R2는 이 가드가 전부 그린이어야 한다** — 문서·노출만 바꾸는 단계에서 렌더 해시가 움직이면
그건 문서 변경이 아니라 동작 변경이다.

#### 6.7.3 R4(삭제)의 테스트 연쇄

Known Mistakes가 기록한 함정: *"결정적 상수·매트릭스 값 변경 시 그 값을 단언하는 테스트를 repo 전체
grep으로 일괄 갱신. 기존 테스트가 버그를 정답으로 굳혔는지도 점검."*

→ 삭제·상수화 제안에는 **그 키를 단언하는 테스트 목록**을 반드시 붙인다(§6.5.3). 목록이 비어 있으면
"테스트가 없다"가 아니라 **"수집에 실패했다"로 간주하고 제안을 보류**한다.

또한 **카운트 단언은 낡는다** — `test_settings_catalog.py`가 스스로 *"이 숫자 단언은 본질적으로
취약하다"* 고 적어둔 그 지점이다. 키를 지우면 335가 깨지므로 같은 커밋에서 갱신한다.

### 6.8 정량 목표와 완료 판정 *(v3 신설)*

*"간소화했다"* 를 느낌이 아니라 숫자로 판정한다. **줄이는 대상은 코드 노브 수가 아니라 운영자가
보는 수**다(§6.6 ★).

| 지표 | 현재(실측 2026-09-11) | 목표 | 측정 방법 |
|---|---:|---:|---|
| `.env.example` **기본 노출** 키 | 216 | **≤ 45** | 접기(C등급) 밖 키 수 |
| 신규 설치가 **반드시 채우는** 키 | 미정(빈 값 17개는 하한) | **≤ 25** | §6.8.1 시나리오 검증 |
| 웹UI **기본 화면** 항목 | 335 | **≤ 45** | 고급 섹션 밖 항목 수 |
| `.env` 현재 등재 키 | 151 | 강제하지 않음 | 기존 설치는 그대로 동작해야 한다 |
| 카탈로그 필드(코드 노브) | 335 | **목표로 삼지 않음** | 삭제는 결과이지 목표가 아니다(§13) |

**마지막 두 행이 중요하다.** 기존 `.env` 151키를 강제로 줄이면 배포된 설치가 깨진다.
간소화는 *"새로 설정하는 사람이 보는 것"* 을 줄이는 것이지 기존 파일을 정리하는 것이 아니다.

#### 6.8.1 완료 판정 — 신규 설치 시나리오

가장 직접적인 수용 시험이다. **빈 `.env`에서 시작한다.**

```
1. .env를 비운다(빈 파일)
2. .env.example의 A등급 블록만 복사해 값을 채운다
3. python -m scripts.bench --preflight   → 전 항목 통과해야 한다
4. python -m scripts.bench --sweep --mode run --scale smoke → 기준선과 동등한 성공률   # v12: --mode run 추가(기본값은 mock) · 워크로드는 20건이 아니라 전 카탈로그
```

3·4가 통과하면 *"A등급만 채우면 동작한다"* 가 실증된다. 이것이 **간소화 완료의 정의**다.
실패하면 그 키는 A등급으로 승격되어야 한다 — 즉 이 시나리오가 **등급 배정의 최종 심판**이다.

#### 6.8.2 재측정 루프 — 간소화가 성능을 깨지 않았는지

R3 적용 후 반드시 한 번 돌린다.

```bash
python -m scripts.bench --sweep --mode run --scale smoke   # v12: --compare 는 없다 — 직전 run 과의 비교는 미구현(94 report.md §8 「직전 run 대비 회귀」가 같은 환경·성격의 직전 run 을 자동 대조한다)
```

리포트가 회귀 비교표를 낸다(§5.1). **기준선 대비 유의한 악화가 있으면 그 R3 커밋을 되돌린다.**
되돌림 판단도 리포트의 판정 어휘(§5.5)로 나오므로 사람이 통계를 읽지 않는다.

---

## 7. 산출물 스키마

### 7.1 `config/bench/axes.yaml` (**자동 생성물** · 사람은 검토·동결만)

```yaml
axes:
  - id: X2
    name: 다중 후보 수
    env: {TEXT2SQL_CANDIDATE_COUNT: null, TEXT2SQL_MULTI_CANDIDATE: null}
    levels:
      - {label: "off",  values: {TEXT2SQL_MULTI_CANDIDATE: "false", TEXT2SQL_CANDIDATE_COUNT: "1"}}
      - {label: "n3",   values: {TEXT2SQL_MULTI_CANDIDATE: "true",  TEXT2SQL_CANDIDATE_COUNT: "3"}}
      - {label: "n5",   values: {TEXT2SQL_MULTI_CANDIDATE: "true",  TEXT2SQL_CANDIDATE_COUNT: "5"}}
    baseline: "off"
    impact: [llm_calls, accuracy]
    rationale: "config.py:290 — 후보 수는 LLM 호출의 직접 배수"
```

한 축이 **여러 env 키를 동시에 움직일 수 있다**(X1 사다리 단, X11 순차 의존 7종). 이것이 단일 키 스위프가 아니라
"축" 개념을 쓰는 이유다.

### 7.2 `raw.jsonl` 한 줄 = 워크로드 1건 실행 1회

```json
{"run_id":"...","arm_id":"S2-X2-n3","repeat":0,"workload_id":"gp-007",
 "ok":true,"ex_pass":true,"ex_subset_pass":true,
 "latency_ms":8421.3,"processing_time_ms":8390,
 "llm_calls":7,"tokens":12840,"retries":1,
 "node_path":["field_mapper","deep_agent"],"node_elapsed_ms":{"deep_agent":7900},
 "tier":"deep_agent","degraded_reason":"none",
 "error":null,"sql_hash":"a1b2..."}
```

`summary.json`은 축별 집계 + 검정 결과 + 노이즈 상한을 담아 트랙 C가 읽는다.

---

## 8. 구현 Wave

| Wave | 범위 | 산출 | 실 LLM | 선행 |
|---|---|---|---|---|
| **T1** | **L1 카탈로그 정합 + L3 주입 실효성** — 4자 대조 · 가림 탐지 · `--quick --ci` 게이트 | `validate.py` · `config_health.*` | **0** | 없음 — **지금 착수 가능** |
| **T2** | **L2 기동 안전성 전수** — 경량 모드(자식 프로세스 config 로드)·경계값 생성기·값역 표 | 값역 표 · 실패 목록 | **0** | T1 |
| **T3** | **L4 소비 실증** — 렌더 해시 비교 · `UNCONSUMED_KEYS` 대조표 | 노브 환상 후보 | **0** | T2 |
| **B0** | **축 자동 선별** — `field_index()` 기반 F1 필터 + F2 영향경로 규칙 → `axes.yaml` **자동 생성**. §3.2 기대 표와 대조 | `config/bench/axes.yaml` · `docs/bench_axis_selection.md` | **0** | T1(가림 키를 축에서 제외해야 함) |
| **B1** | 워크로드 **선별**(선행 P) *(v4 성격 변경)* — 94 카탈로그 145건에서 **축 비교 적격 케이스만 선별**(R군 제외·정답 정의된 것만) · 쌍체 id 부여 · 카테고리 층화 | 선별 목록(94 카탈로그 참조) | **0** | **94 S1** · G-2 |
| **B2** | 러너 골격 — arm 전개·subprocess 격리·기동 검증·JSONL 적재·**자동 재개**. `--mock`으로 전 경로 검증 | `scripts/bench/{axes,runner,workload,probe}.py` | **0** | B0·B1 |
| **B3** | 리포트 생성기 — 성능 리포트 + **건강 보고서 + 커버리지 장부** + **결론 문장 생성**(§5.5). 합성 데이터로 검증 | `scripts/bench/report.py` | **0** | B2·T3 |
| **B4** | 최적화 제안기 — 처분 규칙 0·6 · 3등급 분류 · D-161 4항 자동 수집 | `scripts/bench/optimize.py` | **0** | B3 |
| **B5** | **단일 진입·사전점검·문서** — `__main__.py` + `preflight.py` + **프로바이더 판정**(§4.4) + **퀵 가이드 추출** `docs/30_bench_quick_guide.md`(「실행 가이드 ① 퀵」) | `python -m scripts.bench` · `docs/30_*` | **0** | B4 |
| **B6** | **소규모 실 스위프** — 축 3~4개 · 워크로드 20건 · 파이프라인 전체가 도는지 확인 | `results/bench/<run>/` | **소** — 내부망이면 승인 불요(§4.4) | B5 |
| **B7** | **내부망 본 스위프** — T 전층 + 실험설계 S0~S4 전체(§3.3 — 롤아웃 R0~R4와 다른 기호다) | 리포트·건강 보고서·제안서 | **대**(10~30h) | G-1 · B6 |
| **R0** | **결함 수정** — 고아 키 제거·누락 키 등재·가림 해소·기동 실패 값 교정(§6.5.2 0-a~0-d) | 수정 커밋 | **0** | T1(리포트만 있으면 됨 — **B7 불요**) |
| **R1** | **문서 재편** — `.env.example` 3등급 재배치·C등급 접기·권고값 주석·프로파일 블록 | `.env.example` | **0** | R0 · B7(권고값) |
| **R2** | **노출 재편** — 웹UI 기본/고급 분리·카탈로그 등급 메타·미소비 뱃지 갱신 | `settings_catalog.py`·`admin.js` | **0** | R1 |
| **R3** | **기본값 변경** — `migration_pin.env` 선배포 후 코드 기본값 이동 + **재측정 루프**(§6.8.2) | `config.py` · 재측정 run | **소**(재측정 smoke) | R2 · B7 |
| **R4** | **삭제·상수화** — env 키 제거·분기 제거·테스트 일괄 갱신 · **`plans/70` P1으로 이관** | 폐기 제안서 | **0** | R3 · **D-161 4항 · 사용자 승인**(과금이 아니라 *폐기* 승인이다 — 존치) |
| **B8** | 규칙 반영 — `docs/flag_audit.md` 규칙 0·6 등재 · D-211 본문 등재 | 문서 | **0** | R1 |

**T1~T3 · B0~B5 · R0~R2 · B8은 LLM을 호출하지 않는다.** 실 LLM은 B6에서 처음 쓰고, 그 뒤로는 R3의 재측정뿐이다.
**내부망 실행이면 승인 프롬프트는 전 Wave에서 나오지 않는다**(§4.4 · v5) — 남는 승인은 R4의 *폐기* 승인 하나이며
그것은 과금이 아니라 D-161 절차다.

**★ R0는 B7(본 스위프)을 기다리지 않는다.** 고아 키·가림·기동 실패는 T1 리포트만으로 전부 드러나고,
이것이 *"설정하기 너무 어렵다"* 의 즉효 처방이다(§6.6). 성능 스위프는 R1의 **권고값**에만 필요하다. 특히 **T1은 선행이 없어 지금 바로 착수할 수 있고**, 그것만으로도 *"바꿨는데 왜 안 바뀌지"*
(가림 목록)와 고아 키가 드러난다 — 사용자가 말한 *"설정하기 너무 어렵다"* 의 즉효 처방에 가장 가깝다.

**B5를 마지막 LLM 미호출 Wave로 둔 이유**: 단일 진입은 T1~B4가 다 있어야 감쌀 것이 생긴다. 다만 B5 이전에도
각 모듈은 개별 실행이 가능하므로(「실행 가이드 ② 상세」 A.5 "내부 명령") 개발 중에는 막히지 않는다.

---

## 9. 검증·수용 기준

| # | 기준 | 검증 방법 |
|---|---|---|
| V1 | 러너가 arm의 설정을 **자식이 실제로 읽었는지** 확인한다 | 주입값 ≠ 에코값인 arm을 일부러 만들어 `INVALID` 판정되는지 테스트 |
| V2 | 조용한 강등을 잡는다 | 1단 플래그를 켠 채 deepagents 미설치 상태를 흉내내 `tier` 불일치 `INVALID` 확인 |
| V3 | 리포트가 **없는 유의성을 만들지 않는다** | 합성 데이터로 "차이 0" 입력 → 전 축 `판정 불가` 또는 `무효과` 출력 |
| V4 | 검정력 한계가 리포트에 드러난다 | 워크로드 26건 입력 → 5%p 차이가 `underpowered`로 표기 |
| V5 | 최적화기는 파일을 쓰지 않는다 | `.env`·`config.py` mtime 불변 단언 |
| V6 | D-161 4항이 전부 채워진다 | 삭제 제안 노브 전건에 4항 존재 단언(빈 항 = 제안 자체를 보류) |
| V7 | **LLM 미호출 경로만으로 B0~B4 전체가 돈다** | 네트워크 가드(`tests/conftest.py`) 하에서 `--mock` 전 경로 통과 |
| V8 | 기존 게이트 무회귀 | `arch_check --ci` 0 · `overfit_check --ci` 0 · 전체 pytest 기준선 대비 신규 실패 0 |
| V9 | 스위프 중단·재개가 동작한다 | 절반에서 SIGINT 후 재실행 → 중복 실행 0·누락 0. **`--resume` 없이 같은 명령만으로** 이어지는지(§0.4-3) |
| **V10** | **커버리지 장부에 335필드가 전건 들어간다** | 카탈로그 필드 수 = 장부 행 수 단언. 누락 1건도 실패 |
| **V11** | **미측정(⚪)에 사유가 비어 있으면 리포트가 실패한다** | 사유를 지운 합성 입력 → 생성 실패(exit≠0) 확인(§5.4.2) |
| **V12** | L2가 **같은 프로세스 env 교체 함정에 빠지지 않는다** | 필드 1개를 바꾼 arm의 에코값이 실제로 바뀌는지. 인프로세스 방식이면 불변으로 나와 실패(§3.5.3) |
| **V13** | **L4가 `UNCONSUMED_KEYS`의 거짓을 잡는다** | 목록에 있는 키 중 실제 소비되는 것을 하나 심어 `목록이 낡음`으로 분류되는지 |
| **V14** | **개발자 표면이 명령 하나다** | 「실행 가이드 ② 상세」의 절차를 그대로 따라 산출물이 나오는지. 중간에 YAML 편집·추가 명령이 필요하면 실패 |
| **V15** | 리포트가 **판정 문장**을 낸다 | 다섯 어휘(§5.5) 밖의 판정이 나오지 않는지 · `다음 할 일` 줄이 항상 존재하는지 |
| **V16** | **결정 절차가 결정적이다** | 같은 리포트를 두 번 넣으면 처분이 바이트 동일. 순위 충돌 시 위 규칙이 이기는지(§6.5.2) |
| **V17** | **R3 제안에 `migration_pin.env`가 반드시 붙는다** | 마이그레이션 블록 없는 기본값 변경 제안 → 생성 거부(§6.7.1) |
| **V18** | **R1·R2가 런타임을 바꾸지 않는다** | 문서·노출 재편 커밋에서 비트 동일성 가드 4종 전부 그린(§6.7.2). 렌더 해시가 움직이면 실패 |
| **V19** | **신규 설치 시나리오가 통과한다** | 빈 `.env` + A등급만 → `--preflight` 전항 통과 + smoke 20건 기준선 동등(§6.8.1) |
| **V20** | 삭제 제안에 **테스트 목록**이 붙는다 | 목록이 비면 "테스트 없음"이 아니라 **수집 실패**로 보고 제안 보류(§6.7.3) |

**V8 주의**: `overfit_check`의 스캔 대상에 `scripts/`는 포함되지 않지만, `axes.yaml`에 폴스타 스키마 리터럴이
들어가면 안 된다(축은 설정 키지 스키마가 아니다). 기준선은 **자기 델타만 소거**한다(전면 재생성 금지).

---

## 10. 리스크

| # | 리스크 | 영향 | 완화 |
|---|---|---|---|
| R1 | **검정력 부족으로 전 축이 `판정 불가`** | 계획 전체가 헛돔 | B1(워크로드 80+)을 B5 이전 필수 선행으로 고정. S1 노이즈 상한을 먼저 측정해 **검출 가능 최소 차이를 숫자로 공표** |
| R2 | 폐쇄망 스위프가 며칠 걸려 중단 | 재실행 비용 | resume 필수(V9) · arm 단위 원자 적재 · arm당 타임아웃 |
| R3 | **주입이 조용히 무시됨**(OS env·`.encenv` 우선순위) | 효과를 0으로 오독 | V1 에코 검증. `.encenv` 등재 키는 축에서 제외 |
| R4 | 축 효과가 **워크로드 구성에 종속** | 일반화 실패 | 카테고리별(단순/복합/폼필/알람) 층화 집계를 리포트에 필수 포함 |
| R5 | LLM 서버 측 부하·큐잉이 지연에 섞임 | 지연 축 오염 | arm 순서를 **무작위화**하고, S1 기준선을 스위프 **앞·중간·뒤 3회** 배치해 드리프트 감지 |
| R6 | 벤치마크가 운영 DB·LLM 서버에 부하 | 운영 영향 | 읽기 전용 · 동시성 1 · **실행 창 합의(G-13)**. ★승인이 면제돼도 부하는 면제되지 않는다 — v5에서 이 리스크의 무게가 오히려 커졌다 |
| R7 | 제안이 D-161 절차를 우회 | 정본 붕괴 | V5·V6. 트랙 C는 제안 문서만 생성 |
| R8 | `docs/29` 스위트·`plans/94`와 목적 혼동 | 중복 작업 | **역할 분리 명문화(§1.5 · v4)**: 94=기능 회귀(합격/불합격) · 93=설정 비교(상대 차이). 워크로드·실행 원자·판정기는 **94 소유·93 소비**, 93은 재구현하지 않는다 |

---

## 11. 사용자 확정 게이트 (착수 전 필요)

| # | 질문 | 선택지 | 권고 |
|---|---|---|---|
| **G-1** | 본 스위프를 어느 환경에서 돌리나 | (a) 폐쇄망 FabriX (b) 개발망 Gemini (c) 둘 다 | **(a)** — 운영과 같은 프로바이더가 아니면 권고값이 운영에 적용되지 않는다. (b)는 B5 파이프라인 확인용으로만 |
| **G-2** | 워크로드 확장(선행 P)을 먼저 하나 | (a) 80건까지 확장 후 착수 (b) 26건으로 착수하고 `판정 불가` 감수 | **(a)** — (b)는 §2-②대로 10%p 미만 차이를 전부 놓친다 |
| **G-3** | §3.2 축 18개 초안을 그대로 쓰나 | (a) 그대로 (b) 축소(질의 경로만) (c) 확대(noise_gate 포함) | **(b)→(a)** — 1차는 X1~X10으로 좁혀 파이프라인을 확인하고, 2차에서 전체 |
| **G-4** | 알람 파이프라인(`noise_gate`) 축을 포함하나 | (a) 제외(별도 계획) (b) 포함 | **(a)** — 워크로드 성격이 완전히 다르다(스트림 입력·억제율 지표). 별도 벤치마크가 맞다 |
| **G-5** | X11(순차 의존 7종)을 묶음으로 다루나 | (a) 묶음 (b) 개별 7축 | **(a)** — 개별은 arm 128배. 묶음 효과 확인 후 분해 |
| **G-6** | 프로파일 프리셋을 **신규 설정 키**로 만드나 | (a) 문서·주석·UI 프리셋만(키 0) (b) `CONFIG_PROFILE` 키 신설 | **(a)** — (b)는 D-162 *"신규 플래그 추가 0"* 과 정면 충돌. (b)를 원하면 그 충돌을 D-211에서 명시적으로 해제해야 한다 |
| ~~**G-7**~~ | ~~과금 승인 단위~~ | — | **해소(2026-09-11 사용자 확정)** — *"내부망에서 진행할 예정이라 과금 승인은 필요 없다"*. 내부망 FabriX는 승인·옵트인 없이 실행하고, **외부 프로바이더는 D-127이 그대로 산다**(§4.4). 코드가 `LLM_PROVIDER`로 자동 판정 |
| **G-8** | arm 실행 형태 | (a) 서버 기동 후 HTTP 질의 (b) `--query` 단발 CLI | **(a)** — 운영 경로(미들웨어·감사·SSE)를 포함해야 지연이 운영과 같은 의미. (b)는 기동 비용이 arm마다 반복돼 오히려 느리다 |
| **G-9** *(v2)* | 트랙 T 범위 | (a) L1~L4 전부 (b) L1·L3만(초 단위·CI 게이트) (c) L2 서버 모드까지 확대 | **(a)** — 전부 LLM 미호출이다. 다만 **(b)를 먼저 상시 게이트로 넣는 것을 권고**(§5.4.3): 초 단위라 평소 커밋에 붙일 수 있고, 드리프트를 커밋 시점에 잡는다 |
| **G-10** *(v2 · v5 개정)* | 단일 명령의 기본 동작 | (a) 전수 검증(트랙 T)까지만, 스윕은 `--sweep` 명시 시 (b) 항상 전체 | **(a) 유지** — 승인이 없어졌어도 **10~30시간짜리 스위프가 실수로 도는 것**은 여전히 막아야 한다. 근거가 과금에서 **소요 시간·서버 부하**로 바뀐 것뿐이다 |
| **G-11** *(v3)* | 간소화를 어디까지 진행하나 | (a) R0~R2까지(무위험 · 체감 대부분 확보) (b) R3까지(기본값 이동) (c) R4까지(삭제·상수화) | **(a)** — R3는 배포된 설치의 동작을 바꾸고, R4는 D-161 절차와 테스트 연쇄를 부른다. **둘 다 체감 간소화 기여는 작다**(§6.6 ★). (b)·(c)는 R0~R2 완주 후 별건으로 판단 |
| **G-12** *(v3)* | R4(삭제) 소유 계획 | (a) `plans/70` P1으로 이관 (b) 본 계획에서 수행 | **(a)** — 본 계획은 *측정과 제안*이 범위다. 삭제 실행은 이미 P1이 그 절차를 갖고 있다 |
| **G-13** *(v5)* | 실행 창(언제 돌리나) | (a) 업무 외 시간·야간 배치 (b) 상시 허용 (c) 운영 부하 모니터링 후 결정 | **(a)** — 승인은 면제됐지만 **부하는 그대로다**(R6). 본 스위프는 10~30시간이고 LLM 서버·DB를 계속 두드린다. *과금 승인의 부수 효과였던 "언제 돌릴지 사람이 본다"가 사라졌으므로* 이 게이트가 그 자리를 대신한다 |

---

## 12. 신규 결정 예약 — D-211

착수 시 `docs/02_decision.md`에 아래 골자로 등재한다(문구는 구현 후 확정).

> **D-211. 설정 노브 처분에 측정 근거 도입 — 벤치마크 하네스 + 처분 규칙 0·6 + 노브 3등급**
>
> - **배경**: 환경변수 151개(`.env` 실측)·카탈로그 335필드로 설정이 어렵다는 사용자 지시. 기존 판정 근거
>   (`docs/flag_audit.md` 5조 · D-162)는 **참조 수·수명·`.env` 실제값만 보고 성능을 보지 않는다.**
> - **결정**: ①벤치마크 하네스를 `scripts/bench/`에 두되 **신규 config 필드·신규 `enable_*` 플래그 0**
>   (D-162 주의 준수) ②arm은 **subprocess 격리 기동**이며 자식이 읽은 설정을 에코해 검증한다(주입 무시 오독 차단)
>   ③처분 규칙에 **0조(측정 효과 → 존치+권고값)** 와 **6조(효과 없음+존치 → 운영자 비노출)** 를 추가
>   ④노브를 **A 환경결속 / B 운영레버 / C 내부상수급** 3등급으로 나누고 `.env.example`·웹UI 노출을 등급에 맞춘다
>   ⑤최적화기는 **제안만 생성하고 파일을 수정하지 않는다** — 삭제는 D-161 4항 실측을 첨부한 폐기 제안으로만
>   ⑥**(v2)** *"전체 설정 파일 테스트"* 는 **검증(L1~L4)은 335필드 전수 · 성능(L5)은 축 12~18**의 비대칭으로
>   정의하고, **전건 커버리지 장부**로 미측정을 사유와 함께 공개한다 — **⚪에 사유가 없으면 리포트 생성이 실패**한다
>   ⑦**(v2)** 개발자 표면은 **단일 명령 하나**다(`python -m scripts.bench`) — 축 선별·재개·통계 해석·결론 작성을
>   전부 코드가 진다. `axes.yaml`은 사람이 유지하는 파일이 아니라 자동 생성물이며, 리포트는 숫자가 아니라
>   **다섯 어휘의 판정 문장**과 `다음 할 일` 줄로 끝난다
>   ⑩**(v5)** **과금 승인은 내부망에 한해 면제**한다(사용자 확정 2026-09-11) — 삭제가 아니라 **프로바이더 기반 조건부**다:
>   `LLM_PROVIDER=fabrix`면 승인·옵트인 없이 실행하고 **외부 프로바이더는 D-127이 그대로 산다**(개발망 스모크가 무승인으로
>   새는 것을 막는다). 판정은 코드가 하고 개발자는 구분하지 않는다. 문서의 「과금」 축은 「**실 LLM**」으로 바꾼다 —
>   내부망에서도 시간·서버 부하는 실재하므로 10~30시간 스위프의 실수 실행 방지(G-10)는 존치한다
>   ⑨**(v3)** 리포트 분석은 **10순위 결정 절차**로 고정한다(§6.5.2) — **0단계(고아 키·가림·기동 실패)는 간소화가
>   아니라 결함 수정**이며 성능 판정보다 먼저다. 반영은 **위험도 순 R0~R4 롤아웃**(결함→문서→노출→기본값→삭제)이고,
>   **체감 간소화의 대부분은 런타임 위험 0인 R1·R2에서 나온다**. R3는 `migration_pin.env`로 현재 실효값을 먼저
>   고정한 뒤에만 기본값을 옮긴다(미명시 설치의 침묵 동작 변경 차단). **간소화의 성공 판정은 코드 노브 수가 아니라
>   "새로 설정하는 사람이 보는 수"**이며, 최종 심판은 **빈 `.env` + A등급만으로 기동·질의가 되는가**(§6.8.1)다
>   ⑧**(v2)** L2는 **반드시 자식 프로세스 격리**로 돈다 — 같은 프로세스에서 env를 갈아끼우는 방식은
>   `config.py:1188-1191`이 기록한 실측(nested 기본값이 임포트 시점 env로 고정 · 2026-07-15 E1 하네스 무효화)
>   때문에 전 항목이 거짓 통과한다
> - **근거**: 규칙 2("`.env` 값이 기본값과 다르면 존치")가 *"누군가 켜뒀다"* 를 존치 근거로 삼는 구멍을 측정이 메운다.
> - **대안(기각)**: ①전수 조합 탐색 — 2^110 ②인프로세스 config 교체 — 사다리가 빌드 타임 배타라 측정이 거짓
>   ③자동 `.env` 반영 — D-161 폐기 절차 우회 · `.env` 미추적이라 복구 근거 소실
> - **관련**: D-127(**내부망 면제 · 외부 존속**)·D-120(데이터 외부 송신 금지 — 내부망 면제의 정합 근거)·D-129·D-135·D-161·D-162·D-194 · `plans/70` P1 · `plans/94`(D-212) · `docs/flag_audit.md` · `docs/29`

---

## 13. 이 계획이 하지 않는 것

- **환경변수를 삭제하지 않는다.** 삭제 제안서까지가 범위다 — 실행(R4)은 `plans/70` P1 소관이다(G-12).
  본 계획이 직접 수행하는 간소화는 **R0 결함 수정 · R1 문서 재편 · R2 노출 재편**이며, 셋 다 런타임 위험 0이다.
- **기존 `.env` 151키를 줄이지 않는다.** 배포된 설치가 깨진다. 줄이는 대상은 *새로 설정하는 사람이 보는 수*다(§6.8).
- **`src/`를 수정하지 않는다.** 벤치마크는 운영 경로 밖이다. 단 B7에서 `.env.example`·웹UI 노출 등급은 바꾼다.
- **알람 파이프라인의 성능을 측정하지 않는다**(G-4 (a) 권고 시) — 다만 **설정 검증(L1~L4)은 포함**한다(§3.5.5).
  성능 벤치마크는 워크로드 성격이 달라 별도 계획이 필요하다.
- **`sre_agent` 설정을 다루지 않는다** — 별도 venv·별도 프로세스라 본 하네스의 격리 모델 밖이다(§3.5.5).
- **내부망 면제를 외부 경로로 확대하지 않는다** — 승인 불요는 `LLM_PROVIDER=fabrix`에만 적용되고,
  외부 프로바이더에서는 D-127이 그대로 산다(§4.4 · v5).
- **개발자에게 판단을 요구하지 않는다** — 통계 해석·축 선별·재개 여부는 전부 코드 몫이다(§0.4). 반대로
  **제안서를 자동 반영하지도 않는다** — 그것은 설계자의 판단이다.
- **LLM 프롬프트를 튜닝하지 않는다.** 축은 설정이지 프롬프트가 아니다.
- **HITL 경로의 지연을 측정하지 않는다**(X18) — 사람 응답 시간이 섞여 재현 불가.
