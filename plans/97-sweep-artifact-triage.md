# 97. 벤치 스위프 산출물 분석 — 반출 전 축소 · 단계 분리 · config 처분 연결

> **★ 완결(시점 문서) · 2026-09-21** — [`plans/109`](./109-WIP-config-simplification-consolidated.md)(config 간소화 통합 계획)로 잔여를 이관하고 종료했다(INDEX 「이관 조항」 D-208 2차 적용 · 파일명 상태 태그 해제). 잔여(R-8 `triage.py` · §5 크기 계약 · §11.5 G-3 · R-16 배선 · D-219 ⑥ 누수 · 원본 보관)는 109 §3이, §11.5 G-1(판정 계약)·G-2(teardown `invalid`)는 `plans/110` §3.1이 소유한다. 이 문서는 설계 근거·시점 기록으로 보존한다 — 아래 상태 표기는 **이관 직전 원문**이다.
>
> **작성일**: 2026-09-16 · **갱신** 2026-09-21 (v2 — §0.5 실행 결과 · §11 코드 랜딩 · `-TODO`→`-WIP` / v3 — §11.2 레벨 간 비교·대조군·스냅샷(D-237) / **v4 — §11.3 하네스 전수 감사표 + `optimize.py`·`report.py` 정리 · R-6·R-7·R-9 해소(D-238)**)
> **성격**: 분석 절차 계획 + 관문 구현 · **상태: 일부 구현** — 파일명 `-WIP`
> **대상**: `plans/93` L5 실 스위프 산출물 — 2026-09-16 시점 **폐쇄망 서버에 7GB+ · 미반출**,
> **2026-09-18 축소본 반출 완료**(`reports/bench_results/bench/20260914-185540/` · 56 MB · §0.5)
> **선행 계획**: `plans/93`(축 판정·처분 규칙 §6.5·롤아웃 §6.6의 **정본**) · `plans/94`(하네스) ·
> **`plans/96`(측정 신뢰성 — 이 계획의 Phase 0 관문이 그대로 96 §1·§2다)**
> **관련 결정**: **D-127**(내부망 FabriX 면제 · 외부 프로바이더 유지) · **D-161**(폐기 4항 실측) ·
> D-211(93 트랙 T) · D-212(94 하네스) · **D-218**(96 러너 토큰·판정 계약)
> **신규 결정 예약**: **D-219**(§9). `docs/02_decision.md` 「채번 이력」 표에 등재(2026-09-16).
> ※ 채번 실측 2026-09-16 — `## D-` 헤더 최댓값 **D-217** · 「변경 이력」 최댓값 **D-217** ·
> 「채번 이력」 예약(D-105·115·134·158·163~168·176·195·210·212·214·**218**=`plans/96`) 대조 → **D-219**.
> **실측 기준**: 아래 수치는 반출된 동형 산출물 `20260915-131903`(1.4GB · 폐쇄망 Windows `GE-IT19Y040` ·
> commit `69b665f` · dirty)을 2026-09-16에 직접 계측해 얻었다. 리포트 본문 수치를 옮긴 곳은 없다.

---

## 0. 한 줄 결론

**7GB를 다 분석할 필요가 없다 — 다 반출할 필요도 없다.**
config 처분에 실제로 쓰이는 것은 **arm 당 `raw.jsonl` + `summary.json` + 서버 로그 머리 20줄**이고,
동형 산출물 기준 **전체의 0.1% 미만**이다. 나머지 99.9%는 **체크포인트 DB(용량의 98.6%)**와
**`schema_analyzer` 디버그 덤프(서버 로그의 65%)**이며, **축 판정 코드는 그 둘을 한 줄도 읽지 않는다**
(`scripts/bench/sweep.py:303` `read_observations(raw_path)`가 유일한 입력 경로).

그리고 분석보다 먼저 답해야 하는 질문이 하나 있다 — **이 스위프가 판정 자격이 있는가**.
`plans/96`이 확정한 대로 직전 run 은 **383턴 중 103턴(26.9%)이 러너 JWT 만료로 무효**였다.
다만 그 원인은 "실행이 길어서"가 아니라 **프로파일이 하나뿐이어서**다 — 러너는 프로파일(=arm)마다
서버를 새로 띄우고 **새로 로그인**하며(`runner.py:507`·`532`), 그 run 은 `baseline` 단일 프로파일로
**연속 7.52시간**을 돌아 8시간 수명에 닿았다(실측 2026-09-16: 첫 401이 **281번째 턴 · 누적 7.52시간**,
`run.json`의 `profiles[].reasons`는 비어 있고 `echo_ok=true`).
**따라서 62 arm 스위프는 arm 당 10~30분이면 구조적으로 안전하다** — 만료 위험이 남는 경로는 둘뿐이다:
**①`--token`/`--admin-token` 주입**(`runner.py:420-421`이 런 전체에서 그 토큰 하나를 재사용한다)
**②단일 arm 이 6.4시간(8h의 80%)을 넘는 워크로드**. 그럼에도 Phase 0 에서 무효율은 잰다 —
401·INVALID 는 만료 말고도 기동 실패·계정 미지정·에코 불일치에서 나오고, 확인 비용이
**로그 본문을 열지 않고 10분**이기 때문이다.

---

## 0.5 Phase 0~3 실행 결과 — run `20260914-185540` *(v2 신설 · 2026-09-21)*

> **실측 기준**: `reports/bench_results/bench/20260914-185540/` 의 `raw.jsonl` **6567행 전건** ·
> `run.json` · `server_logs.tar.gz`(arm 62개) · `bench_validate/20260914-01/` 를 직접 파싱했다.
> `README.md`·`axis_verdicts.md` 의 결론을 인용한 곳은 없다 — 아래는 전부 원본 재계산이다.

### 0.5.1 한 줄 결론 — **이 런은 판정 자격이 없다. 설정 처분의 입력으로 쓸 수 없다.**

| 관문 | 값 | 판정 |
|---|---|---|
| 유효 프로파일 | 62/62 (`echo_ok=true` 전건) | 통과 |
| 오류율 | 52/6567 = **0.8%** | 통과 (종전 관문 기준 50%) |
| **SQL 관측률** | **0/6567 = 0.0%** | **차단** — `executed_sql` 전건 `null`, `executed_sqls` 칸 자체가 없다 |
| 그래프 진입률 | 4332/6567 = **66%** | 고지 — 2235턴이 노드를 하나도 밟지 않았다 |
| 역질문 종료율 | 3705/6567 = **56%** | 고지 |
| 무효 턴 | 0 (D-218 이전 코드라 `invalid` 판정 자체가 없다) | — |
| 기준선 실행 순서 | **62/62 번째** | 고지 — 쌍체 지연이 실행 시각과 교란 |

**종전 관문은 이 런을 통과시켰고, 개정 관문(§11)은 SQL 관측 0건에서 막는다.**

### 0.5.2 축 신호 — 있었는가

- **pass 수가 arm 간 사실상 동일**하다: 62 arm 중 60 arm 이 baseline 과 **합격 시나리오 집합이
  글자 그대로 같다**. 다른 arm 은 `SCHEMA_CACHE_BACKEND-file` 하나뿐이고 차이는 2건(H-03·I-08)이다.
- `axis_verdicts.md` 62줄 중 61줄이 **「불일치 쌍 0건」**이다. 정확도 쌍은 존재했는데(`n_pairs>0`)
  **불일치가 0**이었다 — 설정이 결과를 한 건도 뒤집지 못했다.
- **SQL 생성률 신호는 상수 0 이었다.** `sweep.py` 가 `executed_sql` 만 읽는데 2단
  (`intent_orchestration`) 경로는 `done` 에 SQL 을 싣지 않는다. 판정표 62줄의
  「SQL 생성률 +0.0%p」는 **측정값이 아니라 고정값**이다.

### 0.5.3 「A군에는 축 신호가 남아 있을 수 있다」 — **반증됐다**

A군 12 시나리오 × 62 arm 실측:

| 결과 | 시나리오 |
|---|---|
| 전 arm `manual` (정확도 판정 불가) | A-01 · A-03 · A-07 · A-10 |
| 전 arm `pass` (천장) | A-02 · A-05 · A-06 · A-08 · A-12 |
| 전 arm `fail` (바닥) | A-04 |
| 전 arm `clarify`+`manual` | A-09 |
| **arm 간 결과가 갈림** | **A-11 단 1건** |

→ A군에서 불일치 쌍이 생길 수 있는 시나리오는 **arm 당 최대 1건**이다. 검정력 0.
「A군은 영향을 안 받았으니 신호가 남아 있을 수 있다」는 **성립하지 않는다**.

### 0.5.4 지연 — 재계산했고, 노이즈 바닥이 효과보다 크다

`axis_verdicts.md` 의 지연 수치를 승인하지 않고 `raw.jsonl` 에서 다시 쌍체 부트스트랩했다
(그래프에 진입한 턴만 · 시나리오 단위 합 · 2000회 · seed 93).

**노이즈 바닥은 추정하지 않고 실측했다** — 주입값이 기준선의 실효값과 **같은** arm 3개가 있다
(기동 로그 `investigation_metrics` 에코로 확인: `prior_targets_enabled=False` ·
`target_column_llm_enabled=False` · `fanout_concurrency=3` 이 baseline 과 동일):

| 대조군 arm (설정 = 기준선) | 측정된 「축 효과」 |
|---|---:|
| `COMPOSITE_FANOUT_CONCURRENCY-3` | **-2,072 ms** |
| `COMPOSITE_TARGET_COLUMN_LLM_ENABLED-false` | **+2,057 ms** |
| `COMPOSITE_PRIOR_TARGETS_ENABLED-false` | **+5,809 ms** |

→ **설정이 같은데 ±5.8초가 나온다. 이것이 이 런의 노이즈 바닥이다.**
58개 변이 arm 중 **신뢰구간이 0을 지나지 않는 것은 2개**이고, 그중 하나
(`COMPOSITE_AVAILABILITY_PRECHECK_ENABLED-true`, +18.4초)는 **코드 기본값이 `true` 라
네 번째 대조군일 가능성이 높다**(미확인 — 서버 `.env` 실제값 필요. 사실이면 노이즈 바닥이
±18초로 올라가고 **살아남는 축 효과는 0건**이 된다).

교란 원인도 특정됐다: **`scripts/scenario/runner.py:514` 가 프로파일을 알파벳 정렬**한다.
`"baseline"` 은 `"S2-..."` 보다 뒤라 **기준선이 언제나 마지막**이다. 이 런은
2026-09-14 18:55 → 09-18 16:18 **93.4시간 연속**이었고 기준선은 4일차에 돌았다.
실행 순서와 중앙 지연의 상관은 r = **-0.267**(전반 31 arm 61.1초 → 후반 31 arm 56.6초)로,
축 효과라고 보고된 델타(-8.6초 ~ +12.7초)와 **같은 크기대**다.

### 0.5.5 `failed_assertions` 4268건 — **실 결함 목록이 아니다**

단언 키별 전수 분류(중복 포함 10,898건 · 해당 턴 기준):

| 키 | 건수 | 성격 | 처분 |
|---|---:|---|---|
| `sql_must_match` | 5,509 | **전건 `actual=null`** — SQL 이 관측되지 않아 자동 실패 | 계측 부재 · 신호 아님 |
| `status` (기대 `completed` → 실제 `clarification`) | 2,311 | 역질문 자동응답 하네스 부재 | 하네스(`clarify.py`, `9bc94f0`로 해소) |
| `status` (→ `error`) | 390 | 그중 **314건이 HTTP 422 `body.query Field required`** | 하네스(`clarify.complete_payload`로 해소) |
| `has_file` | 560 | 대부분 위 `clarification` 턴의 2차 효과 | 2차 효과 |
| `row_count.min/eq/max` | 1,017 | 518건은 `actual=null`(계측 부재), 나머지는 **`plans/96` C-3**(팬아웃 합계 vs per-DB 기대) | 허위 불합격 확정분 |
| `clarification.options_len` (기대 1 → 실제 2) | 365 | **`plans/96` C-1** — 픽스처 미매핑 열이 2개인데 카탈로그가 1개를 기대 | 허위 불합격 확정분 |
| `db_ids` | 249 | **전건 `actual=[]`** — `db_scope` 미관측 | 계측 부재 · 신호 아님 |
| `response_must_contain` | 187 | `plans/96` C-6 그림자 | 허위 불합격 확정분 |
| `file.filled_rows.min` | 183 | **`plans/96` C-2** — 공란이 정답인 열을 실패로 센다 | 허위 불합격 확정분 |
| 기타(`file`·`sse_events`·`clarification`) | 126 | | |

턴 단위로 접으면 fail 4,268건은 이렇게 갈린다:

| 분류 | 턴 | 비고 |
|---|---:|---|
| 역질문에서 멈춤 | 2,311 | 하네스 부재 + 일부 과잉 역질문(분리 불가) |
| **계측 부재만으로 실패** | 816 | `sql_must_match`·`db_ids`·`row_count(null)` 뿐 |
| 산출 결함 | 739 | **그중 736건이 `plans/96` §3 C-1·C-2·C-3 의 재현** |
| 서버 오류 | 390 | 314 HTTP 422(하네스) + 76(주로 처리시간 초과) |
| 행(hang) | 12 | |

→ **신규 정보로 남는 것은 사실상 성능 계열뿐이다**: `처리 시간이 초과되었습니다` **128턴**
(E 42 · D 24 · A 22 · G 18 · B 14)과 하트비트 간격 초과 `hang` **140턴**(최대 306.8초).
이마저도 `plans/96` §4 J-2(전체 타임아웃 가드)가 이미 다루는 영역이다.
**「fail 4268건의 failed_assertions 가 이 런의 최대 유효 자산」이라는 서술은 반증됐다.**

### 0.5.6 설정 검증 트랙(`bench_validate/20260914-01`) — 유효하되 **처분거리가 없다**

이 트랙은 LLM 을 타지 않아 스위프 사고와 무관하게 유효하다. 다만 산출은 이렇다.

| 신호 | 값 | 93 §6.5.2 처분 |
|---|---:|---|
| 고아 키(0-b) | **0** | 대상 없음 |
| `.env.example` 누락(0-c) | **0** | 대상 없음 |
| 가림(0-d) | **0** | 대상 없음 |
| 기동 실패(0-a) | **0**(L2 시도 0건 — `--quick`) | 미측정 |
| L4 소비 실증 | 변함 0 · 불변 0 · 보류 0(미실행) | 규칙 1·2 입력 없음 |
| 설명 부재 | **69** | R1(문서) 대상 |
| `.encenv` 관리 후보 | **9** | 기록 |
| 등급 초안 | A 31 · B 144 · C 160 | R1·R2 입력 |
| 커버리지 | L1 335건 전수 · **L2·L3·L4 는 `--quick` 으로 148/335** | |

→ **R0(결함 수정) 대상이 0건**이다. 이 런이 지지하는 설정 작업은 R1(설명 69건 채우기)과
등급 재배치뿐이고, 그것도 스위프 산출물이 아니라 **별도 검증 트랙**에서 나온다.
**성능 근거(L5)가 필요한 처분 규칙 3~8은 입력이 없다 — 「근거 없음」으로 남긴다.**

### 0.5.7 반출 패키지 계약(§5) 대조 — 실물 점검

| 계약 | 실물 | 판정 |
|---|---|---|
| 크기 ≤ 50 MB | **56 MB** | 초과(경미) |
| `raw.jsonl` | 7.4 MB · 6567행 | 충족 |
| `run.json` | 240 KB · 62 프로파일 | 충족 |
| **`summary.json`** | **없음** | **결손** — 아래 참조 |
| 서버 로그 머리 | 전문 62개(6.3 MB) | 초과 충족 |
| `checkpoints-*.db` 원본 | 제외(14 GB) | 계약 준수 |
| `manifest.json` | **없음** | 결손(§7-5 축소 사실 기록 의무) |

**`summary.json` 결손은 반출 누락이 아니라 구조적 부재다.** `scripts/bench/__main__.py` 의
`--sweep` 경로는 `scripts.scenario.report.write_report` 를 **호출하지 않는다**(grep 실측:
`summary.json` 생성 지점은 `scripts/scenario/report.py:772` 와 `analyze.py:476` 뿐).
그래서 **스위프 산출물은 94 분석기(`analyze.py`)에 입력될 수 없다** — Tier A 계약이
스위프 경로가 만들지 않는 파일을 요구하고 있다. §5 계약을 고치거나 스위프가
`write_report` 를 부르게 해야 한다(잔여 · §11 R-5).

### 0.5.8 재현성 — **이 런의 코드 상태는 로컬에서 재현할 수 없다**

`run.json` 의 `meta.commit = 82e411e170045dbe0ac144c4278c44a4889a29ad` 이고 `dirty=true` 인데,
이 저장소에 **그 객체가 없다**(`git cat-file -t` 실패 · `git log --all` 346 커밋). 폐쇄망
서버의 미푸시 커밋 위에서, 게다가 dirty 트리로 돌았다. D-161 ①(어느 커밋으로 쟀는가)을
만족하지 못하므로 **이 런의 수치는 어떤 처분의 근거도 되지 못한다** — 위 분석은
"무엇이 잘못됐는가"의 근거일 뿐 "설정 X 가 좋다/나쁘다"의 근거가 아니다.

---

## 1. 실측 — 무엇이 용량을 차지하나

### 1.1 런 폴더 구성 (`20260915-131903` · 1.4GB)

| 파일 | 크기 | 비중 | config 처분 기여 |
|---|---:|---:|---|
| `checkpoints-baseline.db` | **1.38 GiB** | **98.6%** | **0** — 읽는 코드 없음(§1.4) |

> **단위 주의**: 위 `1.38 GiB`는 **1,486,233,600 B**이며 10진 표기로는 **1.49 GB**다 — `plans/96`이 같은 파일을 `1.49 GB`로 적은 것과 **같은 값**이다(불일치 아님).
| `logs/server-baseline.log` | 6.2 MB | 0.44% | 기동 에코 4줄 + `[ERROR]` 28줄뿐 |
| `artifacts/` (xlsx 29 · docx 2) | 2.1 MB | 0.15% | H·I군 산출물 검증(96 C-2) |
| **`raw.jsonl`** (383행) | **1.1 MB** | 0.08% | **L5 판정의 유일한 입력** |
| **`summary.json`** | 132 KB | 0.01% | 집계·`failures`·`meta`(커밋·프로바이더) |
| 분석 md 7종(`report`·`bottleneck` 등) | ~150 KB | 0.01% | 읽기용(재생성 가능) |
| `run.json` | 1.2 KB | — | provenance — **없으면 판정 무효** |

### 1.2 서버 로그 내부 (6,468,559 B · 17,530줄)

| 로거 | 바이트 | 비중 |
|---|---:|---:|
| `src.nodes.schema_analyzer` | 4.22 MB | **65.2%** |
| `src.nodes.multi_db_executor` | 244 KB | 3.8% |
| `src.nodes.query_generator` | 201 KB | 3.1% |
| 그 외 30여 로거 합계 | ~1.0 MB | 15% |

- 레벨별: INFO 1.92 MB · **WARNING 3.25 MB** · **ERROR 0.01 MB(28줄)**
- WARNING 상위 7종이 전부 `schema_analyzer: DEBUG[N] ...` — **최대 라인 12,074 B**(full_schema 테이블 전문 덤프)
- **즉 로그 용량의 3분의 2는 WARNING 레벨로 찍히는 디버그 덤프다.** 진단 가치가 있는 `[ERROR]` 28줄은 0.16%.

### 1.3 체크포인트 DB 내부 (3,261행 · 259 thread · 평균 324 KB/행)

| 구간 | 크기 |
|---|---:|
| `checkpoints` 테이블 | 1,036 MB |
| `writes` 테이블 | 376 MB |
| thread별 **최종** 스냅샷만 | 269 MB (26%) |

가장 큰 단일 체크포인트(19.7 MB · `scn-R1-10-2`)의 상태 키별 크기:

| 키 | 크기 | 판정 용도 |
|---|---:|---|
| `task_results` | 14.7 MB | 없음 — DB 조회 결과 행 |
| `query_results` | 7.3 MB | 없음 — 동일 |
| `final_response` | **4.2 KB** | **조용한 오답 재판정에 필요** |
| `task_plan`·`validation_result`·`parsed_requirements` 등 | 각 ≤0.7 KB | 원인 분류 |

→ **판정에 필요한 9개 키만 뽑으면 thread 당 2.7 KB · 원본의 0.222% · 259 thread 디코드 1초**(실측).
1.4 GB → **약 700 KB**. 500배 압축이 1초에 끝난다.

### 1.4 코드 실측 — 누가 무엇을 읽는가

| 소비자 | 읽는 것 | 읽지 않는 것 |
|---|---|---|
| `scripts/bench/sweep.py:303` `read_observations` | **`raw.jsonl` 단 하나** | 체크포인트 · 서버 로그 |
| `scripts/bench/optimize.py` | 카탈로그 · `.env`/`.encenv` · `git log -S` · `src` 워드 경계 grep | 로그 전부 |
| `scripts/scenario/analyze.py:356` `analyze(run_dir)` | `summary.json` + `raw.jsonl` | 체크포인트 · 서버 로그 |

---

## 2. config 개선에 필요한 파일 — 3등급

`plans/93` §6.5.1의 입력 신호 6종을 출처로 되짚으면 이렇게 갈린다.

| 신호 | 출처 | 7GB 안에 있나 |
|---|---|---|
| L1 정합 · L2 값역·기동 · L3 주입 실효 · L4 소비 실증 | `python -m scripts.bench`(트랙 T) 산출물 — `config_health.md`·`coverage_ledger.md` | **아니다** (별도 실행 · 총 120 KB) |
| **L5 성능 판정** | **`raw.jsonl`** | 그렇다 (arm 당 ~1 MB) |
| 정적(참조 수·`.env` 실제값·경과일) | `optimize.py`가 코드·git에서 수집 | **아니다** |

→ **§6.5.2의 처분 규칙 13행 중 7행(0-a~0-d·1·2·9)은 7GB를 한 줄도 읽지 않고 확정된다.**
L5가 필요한 것은 3~8행뿐이고, 그 입력은 `raw.jsonl`이다.

### Tier A — 반드시 확보 (arm 당 ~1.2 MB)

| 파일 | 왜 |
|---|---|
| `raw.jsonl` | L5의 유일 입력. 턴별 `func_verdict`·`wall_ms`·`node_elapsed_ms`·`node_path`·`executed_sqls`·`failed_assertions`(expected/actual 전문 포함)를 이미 담는다 |
| `summary.json` | 집계 · `failures` · `scenario_verdicts` · `silent_wrong_total` · `meta`(커밋·dirty·provider·platform) |
| `run.json` / `run_meta.json` | 어느 커밋·어느 설정으로 쟀는지. **없으면 어떤 판정도 근거가 되지 못한다**(D-161 ①) |
| 서버 로그 **머리 20줄** | `src.graph: 그래프 빌드 완료(orchestration=…)` · `src.observability.ladder: 사다리 확정 tier=…` — **arm 의 설정이 실제로 먹었는지의 유일한 증거**. 93 v8 사고("설정 에코 미확인"이 통과로 집계돼 62 arm 전부 `valid=true`)가 정확히 이 줄을 안 본 결과다 |

### Tier B — 원인 분류용 (grep 추출 · arm 당 수십 KB)

- 서버 로그 `[ERROR]` **전량**(동형 기준 28줄) — SQL 실행 실패·방언 오류·FabriX 상한 초과가 여기 다 있다
- `[WARNING]` 중 `SQL 검증 실패|폴백|강등|만료|타임아웃` 매칭 라인
- `artifacts/*.xlsx` — 폼필 H·I군 판정 (96 C-2: 미리보기 금지, 전 칼럼 판독)
- `axis_verdicts.md` — 스위프가 스스로 낸 축 판정(대조용)

### Tier C — 반출하지 않는다

- **`checkpoints-*.db` 원본** — 용량 98.6%. 판정 코드가 읽지 않고, `raw.jsonl`에 `thread_id`가 없어
  **직접 조인할 키조차 없다**(scenario_id 경유 매핑 필요). 필요한 건 §1.3의 9개 키뿐
- 서버 로그의 `schema_analyzer DEBUG[N]` 라인 — 65%

---

## 3. 단계 계획 — Phase 0~6

> **원칙 3가지**: ①**축소는 서버에서** 한다(반출량이 곧 절차 비용) ②각 Phase 는
> **조기 종료 조건**을 갖는다 ③**앞 Phase 의 산출물만** 입력으로 쓴다(뒤로 갈수록 대상이 줄어든다).

### Phase 0 — 판정 자격 선별 *(서버 · 로그 본문 미열람 · 10분)*

| # | 확인 | 방법 | 불합격 시 |
|---|---|---|---|
| 0-1 | arm 인벤토리 | 폴더별 파일 수·크기 집계 | — |
| 0-2 | provenance | `run.json`·`summary.json:meta`의 commit·dirty·provider·host | dirty면 재현 불가로 표기 |
| 0-3 | **401 무효율** | arm별 `raw.jsonl`의 `error`에 `401` 포함 턴 비율 + `run.json`의 `profiles[].valid`·`reasons`(`로그인 실패`·`http 401`) + **arm 당 연속 실행 시간**(`wall_ms` 누적 vs 6.4h) + `--token` 사용 여부 | **>5%면 조기 종료 A** |
| 0-4 | **설정 에코** | arm별 서버 로그 `head -20`의 `tier=`·`orchestration=` | 에코 없는 arm은 판정 대상 제외 |
| 0-5 | 완주율·SQL 생성률 | `raw.jsonl`의 `response_mode`·`executed_sql` (93 §4.3.1 2순위 신호) | 기준선 대비 급락이면 arm 무효 |

**조기 종료 A** — 무효율 5% 초과: **분석을 시작하지 않는다.** `plans/96` T-1(토큰 수명 관리)을
먼저 반영하고 재측정한다. 7GB 는 삭제하지 말고 서버에 보관한다(재측정 후 대조용).
**조기 종료 B** — 유효 arm 이 축당 2개 미만: 그 축은 「판정 불가」로 고정하고 §6.5.2 7행(보류)으로 보낸다.

> **이 Phase 가 계획의 핵심이다.** 무효 표본을 유효로 세면 **없는 신호를 해석하게 된다** —
> 96 §1 에서 리포트의 「과잉 거부 의심」 26건이 **전건 401 구간**이었던 것이 그 실물이다.
> 스위프의 만료 위험 자체는 §0 의 두 경로로 한정되지만, 이 10분은 *만료를 의심해서*가 아니라
> **판정 자격을 세지 않은 채 7GB 를 파지 않기 위해** 쓴다.

### Phase 1 — 축소본 추출 *(서버 · 10~20분 · 산출 ~30 MB)*

`scripts/bench/triage.py`(신규 · §6) 한 번 실행으로 반출 패키지를 만든다.

| 추출 | 방법 | 예상 크기(62 arm) |
|---|---|---:|
| `raw.jsonl` 전량 | 그대로 복사 | ~60 MB → **gzip ~8 MB** |
| `summary.json`·`run.json`·`axis_verdicts.md`·`report.md` | 그대로 | ~10 MB |
| 서버 로그 머리 40줄 + `[ERROR]` + 지정 WARNING | grep | ~2 MB |
| 체크포인트 9키 축소본 | thread별 최종 1건 디코드 → `state_digest.jsonl` | ~40 MB |
| `artifacts/` | H·I군 우선(전량이어도 130 MB) | 선별 시 ~10 MB |

**제외**: 체크포인트 원본 · `schema_analyzer DEBUG` 라인. **검증**: 패키지에 arm 수 × Tier A 4종이
빠짐없이 들어갔는지 매니페스트로 확인(누락은 침묵 폴백이 아니라 실패로 보고 — Known Mistakes).
**반출**: 기존 폐쇄망 반출 절차를 따른다. `raw.jsonl`에는 **질의문과 생성 SQL 전문**이 들어간다(93 A.6).

### Phase 2 — 건전성 관문 *(반출 후 · 30분)*

93 §4.3.2 관문 + 96 §1 방식으로 **arm별 유효 표본을 재산정**한다. 산출은 표 하나 —
「판정 자격 있는 arm 목록」. 이후 Phase 는 이 목록 밖을 보지 않는다.

### Phase 3 — L5 축 판정 *(1시간 · 입력은 `raw.jsonl`뿐)*

`read_observations` → 축별 `채택 권고`·`조건부`·`기각`·`차이 없음`·`판정 불가` 5값.
93 §5.3대로 **노이즈 상한과 검정력을 함께 적는다**(26건 골드셋으로는 10%p 미만이 안 잡힌다).
**여기까지가 config 최적값 결정에 필요한 전부다.** 로그 본문은 아직 한 줄도 열지 않았다.

### Phase 4 — 실패 원인 분류 *(2시간 · Tier B)*

**순서를 지킨다** — 96 §3의 허위 불합격 필터를 **먼저** 적용하고(I군 픽스처 미매핑 열·
`filled_rows`·`row_count` 합산·리터럴 단언 4종), 남은 불합격만 `[ERROR]` 라인과 대조한다.
거꾸로 하면 판정 계약 버그를 제품 결함으로 오분류한다.

### Phase 5 — 선택적 심층 *(조건부 · 체크포인트 축소본)*

조용한 오답 후보와 수동 검토 잔여 **한정**으로 `state_digest.jsonl`의 `final_response`를 읽는다.
**전량 판독은 하지 않는다** — 96이 이미 "수동 검토 66%"를 구조적 결함으로 확정했고,
그 해소는 러너 개선(D-218 ⑤)이지 로그 정독이 아니다.

### Phase 6 — 처분 결정과 롤아웃

93 §6.5.2 결정 절차에 Phase 3·4 결과를 넣고, §6.6 R0~R4 순서로 반영한다.
**R0(결함 수정)이 간소화보다 앞이다.** 이 Phase 의 정본은 93이며 이 계획은 입력만 공급한다.

---

## 4. "전체를 다 분석해야 하는가"에 대한 답

**아니다.** 근거 넷 — 전부 실측이다.

1. **판정 경로가 하나다** — 축 판정의 입력은 `raw.jsonl` 뿐이다(`sweep.py:303`).
2. **용량과 신호가 반비례한다** — 용량 98.6%인 체크포인트의 판정 기여는 0이고,
   0.08%인 `raw.jsonl`이 L5 전부를 담는다. 로그도 65%가 디버그 덤프이고 `[ERROR]`는 0.16%다.
3. **처분 13행 중 7행은 로그와 무관하다** — L1~L4·정적 신호는 트랙 T 와 코드 grep 에서 나온다.
4. **필요한 정보는 이미 구조화돼 있다** — `raw.jsonl`은 노드별 경과시간·노드 경로·실행 SQL·
   실패 단언의 expected/actual 을 행마다 담는다. 로그를 풀어 재구성할 것이 없다.

**다만 "다 분석하지 않는다"가 "일부만 본다"는 아니다** — `raw.jsonl`은 **전 arm 전량**을 본다.
버리는 것은 **동일 정보의 원시 중복본**(체크포인트·디버그 덤프)이지 표본이 아니다.

---

## 5. 반출 패키지 계약

| 항목 | 값 |
|---|---|
| 이름 | `sweep-<run_id>-triage.tar.gz` |
| 크기 목표 | **≤ 50 MB**(원본 7GB 대비 0.7%) |
| 필수 | arm 수 × {`raw.jsonl`, `summary.json`, `run.json`, `server-head.log`} + `manifest.json` |
| 선택 | `state_digest.jsonl` · `errors.log` · `artifacts/` 선별 |
| 금지 | `checkpoints-*.db` 원본 · `schema_analyzer DEBUG` 라인 |
| 보관 | **원본 7GB 는 재측정 종료까지 서버에서 삭제 금지**(축소본으로 답이 안 나오면 되돌아갈 곳) |

---

## 6. 추출 스크립트 사양 — `scripts/bench/triage.py` (신규 1파일)

- **경계**: `scripts/bench/` 안. `src/` 수정 0. 계층 규칙 대상 밖(scripts).
- **런타임**: 표준 라이브러리 + `langgraph.checkpoint.serde.jsonplus.JsonPlusSerializer`
  (체크포인트 msgpack 은 `ext_hook` 이 필요해 **서버의 같은 venv 에서** 풀어야 한다 — 실측)
- **Windows 1급**: 파일 쓰기 `newline="\n"` 명시 · 콘솔 ASCII (94 W군 규약)
- **CLI**: `python -m scripts.bench --triage <run_dir> [--out <pkg>] [--inventory-only] [--no-state]`
  - `--inventory-only` = Phase 0 (로그 본문 미열람 · 10분)
  - 기본 = Phase 1 (축소본 + 매니페스트)
- **불변식**: 입력 폴더에 **쓰지 않는다**(93 V5 와 동일 — 러너가 `.env` 를 쓰지 않는 것과 같은 이유)
- **테스트**: 픽스처 런 폴더로 ①매니페스트 누락 검출 ②9키 추출 등가성 ③401 집계 정확도

---

## 7. 리스크

| # | 리스크 | 완화 |
|---|---|---|
| 1 | Phase 0 에서 무효율이 높아 7GB 가 통째로 쓸모없어진다 | **그것이 이 Phase 의 목적이다.** 10분에 확인하고 96 T-1 로 넘긴다 |
| 2 | 축소본만 반출한 뒤 원본이 필요해진다 | 원본 보관 의무(§5) · `state_digest` 로 1차 대응 |
| 3 | serde 버전 차이로 반출 후 디코드 실패 | 디코드를 **서버에서** 끝낸다(Phase 1) |
| 4 | `raw.jsonl` 의 SQL·질의문 반출 | 기존 반출 절차 · 마스킹 확인(93 §4.4) |
| 5 | 축소가 신호를 지운다 | 매니페스트에 제외 규칙과 제외량을 적어 **무엇을 안 봤는지**를 남긴다 |

---

## 8. 완료 판정

| # | 기준 |
|---|---|
| 1 | Phase 0 표가 나왔다 — arm 수 · 무효율 · 설정 에코 확인율 · 유효 arm 목록 |
| 2 | 반출 패키지 ≤ 50 MB · 매니페스트 누락 0 |
| 3 | 축별 L5 판정 5값이 노이즈 상한과 함께 산출됐다 |
| 4 | 불합격이 **허위(96 §3) / 제품 결함 / 설정 효과** 셋으로 분류됐다 |
| 5 | 93 §6.5.2 처분표가 채워졌고, 그중 **로그 없이 확정된 7행이 명시**됐다 |
| 6 | 체크포인트 원본을 반출하지 않고 위 전부를 완료했다 |

---

## 9. 신규 결정 — D-219 *(2026-09-21 본문 등재 · ③·⑥ 이 §0.5 실측으로 개정됨)*

> **③ 개정 이유(2026-09-21)**: 예약 당시의 ③은 *"무효 표본률 5% 초과면 재측정"* 이었다.
> run `20260914-185540` 은 **무효 0% · 오류율 0.8%** 로 그 기준을 여유 있게 통과했는데도
> 62 arm 전부 「판정 불가」였다. **무효율은 판정 자격의 필요조건이지 충분조건이 아니다** —
> 워크로드가 축에 닿았는지(SQL 관측·그래프 진입)를 함께 봐야 한다. ⑥은 그 실측에서 새로 나왔다.

**벤치 스위프 산출물의 분석·반출 계약**:
①**판정 입력 단일화** — 축 판정의 입력은 `raw.jsonl`이며, 체크포인트 DB 와 노드 디버그 로그는
판정 경로가 아니다(실측: 용량 98.6%가 기여 0) ②**축소는 서버에서** — 폐쇄망 산출물은 원본이 아니라
축소본을 반출한다(≤50 MB · 체크포인트 원본 반출 금지 · 원본은 재측정까지 보관) ③**판정 자격 관문 선행** —
무효 표본률(401 등) 5% 초과 run 은 분석 대상이 아니라 재측정 대상이다(D-218 T-1 연계). 만료 위험은 arm 수가 아니라
**①토큰 주입(`--token`) ②단일 arm 연속 6.4시간 초과** 두 경로에 한정된다 — 러너는 프로파일마다 재로그인한다(실측) ④**설정 에코가
arm 유효성의 정본** — 기동 로그의 `tier=`·플래그 에코가 없는 arm 은 판정에서 제외한다(93 v8 재발 방지)
⑤**축소 사실의 기록 의무** — 제외 규칙과 제외량을 매니페스트에 남긴다(침묵 폴백 금지)
⑥**판정 자격은 워크로드 도달로도 묻는다**(2026-09-21 신설) — 오류율·무효율이 정상이어도
**SQL 관측 0건이면 판정하지 않고**, 그래프 진입률·역질문 종료율·기준선 실행 순서는 판정문에
함께 싣는다(차단 아님 — 임계가 워크로드 구성의 함수라서다). **판정표가 전건 「판정 불가」인 것도
사고다** — 표에 무효를 박고 `exit 1` 하며, 처분 규칙(93 §6.5.2 7행 「보류」)의 입력으로 보내지 않는다.
대안(기각): 전량 반출 후 분석 · 표본 추출(arm 일부만) · 체크포인트 상시 비활성화 ·
도달 지표에 고정 임계를 두고 차단(정상 런을 막는다 — 워크로드 구성이 바뀌면 임계가 무의미해진다).

---

## 10. 이 계획이 하지 않는 것

- **러너·판정 계약 수정** — `plans/96`(D-218)의 몫이다. 이 계획은 그 결과를 관문으로 쓸 뿐이다
- **축 선별 규칙·처분 규칙 변경** — `plans/93` §3·§6.5 가 정본이다
- **config 실제 반영** — R0~R4 는 93 §6.6 이 정본이다
- **`schema_analyzer` 로그 레벨 조정** — 65%가 WARNING 레벨 디버그 덤프인 것은 사실이지만,
  로그 정책 변경은 별건이다(관측 시 §7-5 매니페스트에만 기록)

---

## 11. 랜딩 — 관문 코드화 *(v2 신설 · 2026-09-21)*

§0.5 가 보여준 것은 하나다 — **관문이 오류율만 보면 사고를 사고로 판정하지 못한다.**
아래 5건을 `scripts/bench/` 에 넣었다. `src/` 수정 0 · 계층 규칙 대상 밖.

| # | 랜딩 | 위치 | 이 런에서 무엇을 막는가 |
|---|---|---|---|
| **R-1** | **SQL 관측 0건이면 판정을 막는다** | `sweep.py` `RunHealth.blocking_reason()` | 6567턴 전건 SQL 미관측을 통과시키던 경로. 임계값이 아니라 **0** 이다 — 질의 벤치마크에서 SQL 0건은 파이프라인 문제이거나 계측 문제이고, 어느 쪽이든 「SQL 생성률」은 신호가 아니라 상수가 된다 |
| **R-2** | **`executed_sqls`(감사 로그 수집분)도 SQL 관측으로 센다** | `sweep.py` `sql_observed()` | 94 가 D-217 로 적재하는 칸을 93 이 읽지 않아 **신호가 영구 0** 이던 것. 이것 없이 R-1만 넣으면 정상 런도 막힌다 |
| **R-3** | **무효 턴(`invalid`)을 분모에서 뺀다** | `sweep.py` `read_observations()` | D-218 이 94 쪽에 넣은 `invalid` 를 93 이 몰라 **러너 실패가 기능 불합격으로** 집계되던 것 |
| **R-4** | **도달 지표·기준선 실행 순서를 고지한다** | `sweep.py` `RunHealth.warnings()` · `__main__.py` | 그래프 진입 66% · 역질문 56% · 기준선 62/62번째가 **침묵되던 것**. 차단하지 않는다 — 임계가 워크로드 구성의 함수라서다 |
| **R-5** | **전 arm 「판정 불가」면 판정표에 무효를 박고 `exit 1`** | `__main__.py` `cmd_sweep()` | 62줄 전부 판정 불가인 표가 **형식상 정상**으로 나가 처분 파이프라인(§6.5.2 7행)에 「보류 62건」을 만들던 것 |

- 회귀 테스트 7건을 `tests/test_scripts/test_bench_sweep.py` 에 넣었다. 전부 **이 런의 모양**을
  픽스처로 재현한다(SQL 0건 · `executed_sqls` 수집분 · `invalid` 턴 · 도달 지표 저하 ·
  기준선 마지막 실행 · 전 arm 판정 불가 · 판정이 나오면 정상 종료).
- `test_정상_런은_관문을_통과한다` 의 픽스처에 `executed_sql`·`node_path` 를 더했다 —
  **SQL 도 노드 경로도 없는 턴만으로 된 런을 「정상」이라고 부르던 것이 이 사고의 축소판**이다.

### 11.2 2차 랜딩 — 레벨 간 비교 · 대조군 자동 판정 *(2026-09-21 · 사용자 지시)*

> 사용자 지시: *"이 설정이 되어 있는 경우와 되어 있지 않은 경우를 모두 검증하여 효과적인
> 설정을 선택하는 것이 이 벤치마크 테스트의 목적이다. … 왜 설정이 되어 있는지를 묻는거냐?"*

지적이 드러낸 결함은 「기준선 실효값을 모른다」가 아니라 **그보다 앞선 것**이었다.

**`scripts/bench/compare.py`의 `judge()` 는 모든 arm 을 기준선하고만 비교한다** — 불린 축이면
`-true`·`-false` 두 줄이 각각 기준선과 비교돼 나오고, **"켰을 때 vs 껐을 때"는 어디에도
계산되지 않는다.** §0.5.4의 대조군 3건은 그 구조의 산물이다(기준선 실효값과 같은 주입이라
그 줄은 순수 노이즈인데 판정표가 축 효과로 렌더링했다).

| # | 랜딩 | 위치 |
|---|---|---|
| **R-10** | **레벨 간 직접 비교** — `compare_levels()`·`optima()`. 축 1개당 판정 1줄(「최적 레벨 `X`」 / 「레벨 간 차이 없음」 / 「판정 불가」). 레벨 3개 이상이면 쌍마다 비교 후 **축 안에서 BH 보정**. 우세 레벨이 갈리면 최적을 만들어내지 않는다 | `compare.py` |
| **R-11** | **축 판정 전용 어휘 2종**(`BEST_LEVEL`·`LEVELS_TIED`) — arm 5어휘와 섞지 않는다(§5.5 폐쇄성 유지). arm↔기준선 판정은 **삭제하지 않고 유지**(기준선 대비 절대 위치는 §6.7 마이그레이션 판단의 입력) | `compare.py` |
| **R-12** | **대조군 자동 판정** — `ConfigSnapshot.control_arms()`. 실효 설정 지문(비결정 필드 제외)이 기준선과 같은 arm 을 식별해 판정표에 **(대조군)** 표기, 축 문장에 어느 레벨이 대조군인지 명시 | `sweep.py` |
| **R-13** | **노이즈 바닥 실측** — `latency_noise_floor()`(대조군 쌍체 지연 델타 최댓값) + `noise_floor()` 에 대조군을 기준선 반복으로 투입. **`--repeat 1` 런에서도 바닥을 잴 수 있는 유일한 경로**이고, 그 값이 `judge(noise_floor_pp=…)` 로 들어간다 | `sweep.py`·`__main__.py` |
| **R-14** | **arm 실효 설정 스냅샷**(§4.5 이행) — `capture_config_snapshot()` 이 서버 기동 **전에** arm 마다 자식 파이썬으로 `load_config()` 를 한 번 돌려 `config_snapshot.json` 으로 산출. 비결정 필드는 `probe.detect_nondeterministic_keys` 로 자동 제외, 민감값은 에코 단계에서 해시. **기준선 에코가 실패하면 대조군 판정을 아예 하지 않는다**(추정 금지) | `sweep.py`·`__main__.py` |

**판정표 형식 변경**: `axis_verdicts.md` 가 두 표가 된다 — 「## 축 최적 레벨」(먼저) + 「## arm 판정 — 기준선 대비」(근거).
대조군 목록과 실측 노이즈 바닥이 머리말에 붙는다.

#### 이번 런 데이터로 돌린 결과 (run `20260914-185540`)

| 항목 | 값 |
|---|---|
| 축 | **28개** |
| 판정 | **판정 불가 27 · 레벨 간 차이 없음 1**(`SCHEMA_CACHE_BACKEND`) |
| 불일치 쌍 합 | **`SCHEMA_CACHE_BACKEND` 2건을 빼면 전 축 0건** |
| 실측 노이즈 바닥 | 지연 **±4,009ms** · 정확도 **2.0%p** |

예상대로 전건 판정 불가다. **그게 R-10 이 옳게 도는 증거다** — 레벨을 바꿔도 결과가 한 건도
달라지지 않았고, 문장이 *"축이 무효인 것이 아니라 워크로드가 축에 닿지 않은 것일 수 있다"* 로
그 사실을 구분해 적는다.

**대조군 3건 대조** — 두 비교 단위가 실제로 다른 답을 낸다:

| arm | arm ↔ 기준선(종전) | 레벨 ↔ 레벨(신설) |
|---|---|---|
| `COMPOSITE_FANOUT_CONCURRENCY-3` | 판정 불가 · **지연 -1,430ms** | 판정 불가 · 쌍 3건 · **대조군 레벨 `3` 명시** |
| `COMPOSITE_TARGET_COLUMN_LLM_ENABLED-false` | 판정 불가 · **지연 +1,419ms** | 판정 불가 · **대조군 레벨 `false` 명시** |
| `COMPOSITE_PRIOR_TARGETS_ENABLED-false` | 판정 불가 · **지연 +4,009ms** | 판정 불가 · **대조군 레벨 `false` 명시** |

종전 표는 이 세 줄에 **축 효과처럼 보이는 지연 숫자**를 실어 보냈다(-1.4초 ~ +4.0초).
신설 표는 같은 값을 **노이즈 바닥으로 되돌려 쓰고**, 그 레벨이 기준선과 동일 설정임을 적는다.
※ 이 런에는 `config_snapshot.json` 이 없으므로(신설 이전 산출물) 위 검증은 §0.5.4의
기동 로그 에코로 확인된 대조군 3건을 손으로 지정해 돌렸다. **과거 런은 자동 판정 대상이 아니다.**

### 11.3 전수 감사 — 하네스 전 모듈 × 분석 결과 6기준 *(2026-09-21 · 사용자 지시 「모두 수정」)*

**감사 기준 ①~⑨**(이번 분석 + 병행 세션 실측이 확정한 사실):
① arm↔기준선만으로는 최적값에 답할 수 없다 ② 주입값 == 기준선 실효값인 arm 은 대조군이다
③ `executed_sql` 단독은 2단·멀티DB 에서 구조적으로 0이다 ④ `invalid` 를 기능 불합격으로 세면 안 된다
⑤ 「불일치 0」 ≠ 「차이 없음」 ⑥ 기준선 실행 위치·시간 교란이 축 효과와 같은 크기대다
⑦ 자동응답·기본값 주입이 판정 대상을 대체하지 않는가 ⑧ 산문에 박힌 수치가 실측과 일치하는가
⑨ 에러·타임아웃 판정이 서버 문구(한국어)를 실제로 잡는가
(⑦~⑨는 §11.4 · ①~⑥은 아래 표)

| 모듈 | 해당 기준 | 판정 | 근거 |
|---|---|---|---|
| `bench/sweep.py` | ②③④⑥ | **고침**(1차 랜딩 §11.1 + 2차 §11.2) | `sql_observed`·`INVALID_VERDICT`·`ConfigSnapshot`·`baseline_order` |
| `bench/compare.py` | ①②⑤⑥ | **고침**(§11.2) | `compare_levels`·`optima`·`latency_noise_floor` |
| `bench/__main__.py` | ①②⑤⑥ | **고침** | 축 표·대조군 표기·노이즈 바닥·후단 관문 + 아래 R-15·R-16 |
| **`bench/optimize.py`** | ①⑤ | **고침 — R-15** | `decide()`(`optimize.py:81-127`)가 **arm 5어휘만** 소비해 `AxisOptimum` 을 몰랐다. `axis_verdict_word`·`axis_disposition_inputs`·`render_recommended_env` 신설. `decide()` 자체는 **미수정**(§6.5.2 처분 규칙은 정본) |
| **`bench/report.py`** | ⑤ | **고침 — R-16** | `build_ledger`(`report.py:157-158`)가 L5 미측정 사유를 *"성능 스위프 미실행"* 하나로 적어, **돌렸는데 판정 불가**인 축도 미실행으로 기록됐다. `l5_unjudged` 추가 + `render_health`(`:267`)의 하드코딩 `L5 성능: 0건` 제거 |
| `bench/axes.py` | ①② | **불필요(근거)** | `expand_ofat`(`axes.py:172-186`)이 `arm_id·axis·level·env` 를 그대로 싣고 `sweep.build_arms`(`sweep.py:184-187`)가 `ArmSpec` 으로 옮긴다 — `optima()` 가 읽는 세 속성이 전부 채워져 계약이 맞는다. **레벨 전개 방식은 유지해야 한다** — 대조군이 거기서 나온다 |
| `bench/probe.py` | ② | **불필요(근거)** | `echo_config`(`probe.py:131`)·`detect_nondeterministic_keys`(`:220`)·`config_fingerprint`(`:244`)를 스냅샷이 **그대로 재사용**한다. 민감값 해시(`_safe`)·`ok=False` 를 데이터로 돌려주는 계약도 그대로 맞는다 |
| `bench/validate.py` | ②⑤ | **불필요(근거)** | L1~L4 전용이라 ③④⑥ 노출 0. `_dotted_of`(`validate.py:68-78`)의 ENV↔경로 규칙을 `ConfigSnapshot.baseline_env_values` 가 **같은 규칙으로** 따랐다(접두 깎기 금지 — `API_PORT`→`server.port`) |
| `bench/catalog.py` | ① | **불필요(근거)** | `KnobSpec.env_key` 가 곧 축 id 라 별도 매핑표가 필요 없다(테스트로 고정). 정합 검사는 L1 전용 |
| `scenario/runner.py` | ⑥ | **고침 — R-6** | `iter_executions:514` 알파벳 정렬 → `config.profiles` 순서 존중(지정 없으면 종전 유지) |
| `scenario/server.py` | ② | **고침 — R-9** | `verify_profile:349` 가 받은 실효 설정을 버리던 것 → `ProfileStatus.effective_settings` 보존 |
| `scenario/clarify.py`+`runner` 배선 | — | **불필요(근거) + 회귀 감시 추가** | 배선은 이미 있다(`runner.py:1583-1586`·`1569`) 그리고 `tests/test_scenario/test_auto_answer_runner.py` 9건이 러너를 통과해 `auto_answers` 적재까지 단언한다. 다만 *"배선이 끊기면 정말 깨지는가"* 를 못 박는 테스트가 없어 **추가**했다(무력화하면 `response_mode == "clarify"`) |
| `scenario/assertions.py`·`client.py` | ③④ | **불필요(근거)** | D-217·D-218 에서 이미 `executed_sqls`·`INVALID_VERDICT` 를 갖췄다. 이번 결함은 **93 이 그것을 안 읽은 것**이었고 sweep.py 에서 닫혔다 |
| `scenario/analyze.py`·`report.py` | — | **불필요(근거)** | 94 소유의 자체 분석 경로이고 이번 기준에 걸리는 곳이 없다. 다만 **스위프 산출물을 못 읽던 것**은 93 쪽 미호출이 원인이라 R-7 로 닫았다(아래) |

**잔여 참조 전수 확인**(침묵 누락 방지): `scripts/bench/` 전체에서 `executed_sql`·`func_verdict`·
`wall_ms` 를 grep 하면 **전부 `sweep.py`·`compare.py` 안**이다 — 나머지 6개 모듈은 ③④⑥ 노출이 0건이다.
원시 로그의 `executed_sql` 을 읽는 지점은 **`sweep.sql_observed()` 한 곳뿐**이고 이미 복수형 병행
판독이다(`sweep.py:46-53`). `compare.py` 는 `Observation.sql_generated` 속성만 본다(`compare.py:206`).

### 11.4 기준 ⑦~⑨ 감사 *(2026-09-21 2차 · 병행 세션 실측 편입)*

| 기준 | 대상 | 판정 | 근거 |
|---|---|---|---|
| **⑦ 자동응답이 판정 대상을 대체하는가** | `clarify.build_answer`(`clarify.py:112-140`) | **고침(고지) + 기록** | 아래 표 |
| **⑧ 산문 수치가 실측과 일치하는가** | `sweep.py:197`·`__main__.py:284`·`test_bench_sweep.py:626`·`plans/93` v9·v10 | **고침** | 아래 |
| **⑨ 에러·타임아웃이 서버 한국어 문구를 잡는가** | `scripts/bench/*` | **불필요(근거)** | 팀 리드 실측으로 종결 — bench 는 문자열 매칭을 하지 않고 구조화 필드(`response_mode`·`func_verdict`)만 본다. run 20260914-185540 의 타임아웃 128턴이 **전건 `response_mode=hang`·`forbidden_mode=hang`** 으로 잡혔고 `answer` 로 샌 것은 **0건**이다. `assertions.py` 도 무결 — 손대지 않았다 |

#### ⑦ — 자동응답 3종이 무엇을 대체하는가 (코드 감사)

| 자동응답 | 코드 | 대체되는 판정 |
|---|---|---|
| `zone` | `clarify.py:120-128` — 전건 선호 존(기본 김포 · `config/scenarios/auto_answer.yaml`) | **라우팅**. 존 선택을 제품이 아니라 하네스가 정한다 |
| `form_fill` | `clarify.py:129-137` — 전 필드 `blank` | **폼필 매핑**. *매핑이 옳았는가* 가 아니라 *공란 처리가 되는가* 를 잰다 |
| `approval` | `clarify.py:138-139` — 전건 `승인` | **HITL 거부 경로**. 거부 시 동작은 재지 않는다 |
| `complete_payload` | `clarify.py:155-180` — 빈 `query` 를 직전 역질문 원문으로 | **대체 아님** — 서버 계약(`QueryRequest.query` min_length=1) 충족이다 |
| `ISOLATION_ENV` | `runner.py:63-66` — `ALARM_ENABLED=false` | 알람 경로를 재지 않는다. **의도된 격리**이고 스위프가 화면에 고지한다 |
| 내장 테스트 계정 | `runner.py:71-72` | 그 계정의 `allowed_db_ids` 가 좁으면 **DB 범위 판정이 대체된다**(D-216 주의 ①에 기재) |

**실측(병행 세션 run `20260918-182507` · 380턴)**: `auto_answers.zone_select` 가 **221턴(58.2%)** 에서
발동해 **전건 `polestar_cm_gp`** 를 골랐다 — 그 런의 라우팅 계열 단언은 이 구성에서 성립하지 않는다.
**자동응답이 라우팅 판정을 대체한다 · `plans/99` 소관(「끄는 arm」) · 벤치 범위 밖.**

**조치**: 자동응답을 끄거나 바꾸지 않았다 — 「끄는 arm」은 `plans/99` 소관이고 중복 신설 금지다.
대신 **침묵을 없앴다**: `RunHealth.auto_answered_turns` 를 세어 건수·비율과 *"그 턴의 라우팅·매핑·
거부 판정은 제품이 아니라 하네스가 정한 값 위에서 내려진다"* 를 판정 전 고지로 싣는다.
**이번 런에서는 측정 불가**(`auto_answers` 0건 — `clarify.py` 이전 커밋). 다음 런부터 찍힌다.

#### ⑧ — 산문에 박힌 워크로드 수치가 낡았다

2026-09-21 실측: `closed` **103건 · 판정 가능 92/103(89%)** — 그룹별 D 6→8 · F 8→9 · K 10→3
(`testdata/scenarios/d_alarm.yaml` D-07·D-08 신설 포함 · 둘 다 `auto_answer: false`).
산문의 **`107건`** 이 낡았다. **`32건` 은 낡지 않았다** — 그룹 L 카탈로그 32건 중 SYN-F-05
(운영 절차 `action`)를 뺀 31건이 워크로드라 두 값은 기준이 다를 뿐 시점 드리프트가 아니다.
그래서 `__main__.py`·`compare.py`·`sweep.py`·`test_bench_sweep.py` 의 `32건` 표기는 **그대로 둔다**.

- **코드는 안 깨진다** — `judgeable_count`·`workload_summary` 가 동적 계산이고 건수를 단언하는
  하드코딩은 `scripts/bench/*`·`tests/test_scripts/*` 에 없다(grep 확인).
- 고친 것: `sweep.resolve_env` 독스트링·`__main__` 주석·테스트 주석에서 **건수를 제거**하고
  *"정본은 동적 계산이고 산문은 파생"* 을 못 박았다. `plans/93` 은 **v9·v10 항의 수치를 고쳐
  쓰지 않고**(그때의 실측 기록이다) v11.1 항으로 현재값과 원칙을 추가했다(기존 v11(2026-09-15)과 번호가 겹쳐 v12 작성 때 v11.1로 고쳤다).
- 같은 드리프트가 v9→v10(`89/100`→`87/98`)에서 이미 한 번 났다 — 세 번째를 막는 것이 목적이다.

### 11.5 기록만 — 구현하지 않은 것 *(2026-09-21)*

| # | 항목 | 왜 구현하지 않았나 |
|---|---|---|
| **G-1** | **판정 계약 변경 — 타임아웃을 기능 합격률 분모에서 뺄 것인가** | **사용자 게이트 대기.** 정본이 `plans/94` §15~§18 이고 임의로 바꾸면 우리 6,567턴과 병행 세션 380턴이 **서로 다른 규칙으로 집계**된다. 영향 범위만 아래에 조사해 둔다 |
| **G-2** | **teardown 미지원 오염을 `invalid` 로 표시**(CU-B3 · `plans/94` 소관) | 같은 게이트(새 사유 코드·판정 계약). **우리 런에서는 측정도 불가** — `teardown_unsupported` 124턴(62 arm × A-05·A-10)이 **pass 62 · manual 62 로 허위 불합격 0건**이고, I군은 I-01~I-08 이 균일 fail(각 61~62턴)이라 병행 세션이 본 순서 의존 패턴(I-03~06만)이 **역질문 결함에 가려 보이지 않는다**. 그쪽 4턴이 우리 데이터로 반증되지도 확증되지도 않는다. 등재는 `plans/108` §3.3·§5.2e — **중복 등재하지 않는다** |
| **G-3** | **최적화 표면의 사각지대** — `AppConfig` 밖 코드 상수는 축이 되지 않는다(`src/graph.py:63` `NON_SQL_RETRY_BUDGET`) | 검사기 신설은 「검토한 내용에 맞게 수정」의 범위를 넘는 **새 기능**이고, 설정 표면을 늘리는 것은 노브를 줄이려는 `plans/93` 방향과 별개 판단이다. **`axes.py` 독스트링 「한계 ②」로 기록만** 했다. 승격 여부는 재측정 후 |

#### G-1 영향 범위 조사 (코드 0건 — 승인 시 붙일 목록)

| 파일·함수 | 무엇이 바뀌나 |
|---|---|
| `scripts/scenario/assertions.py` `Verdict`·`evaluate_turn` | 새 사유 코드(`timeout` 등) 생성 지점. **정본 · 승인 필수** |
| `scripts/scenario/runner.py` `_row` | `raw.jsonl` 에 사유를 싣는 지점 |
| `scripts/scenario/report.py` `valid_rows`·`invalid_summary`·`scenario_verdicts` | 분모 재산정 · 사유별 분리 집계 |
| `scripts/bench/sweep.py` `read_observations`(`INVALID_VERDICT` 분기)·`RunHealth` | **이미 `invalid` 분모 제외가 들어가 있다** — 새 사유를 같은 자리에 넣으면 닫힌다 |
| `scripts/bench/compare.py` `_scored()` | 정확도 분모 필터(현재 `manual` 만 제외). 사유가 늘면 여기 |
| 테스트 | `tests/test_scenario/test_judgement_contract.py`·`test_measurement_trust.py`·`tests/test_scripts/test_bench_sweep.py` |

**우리 런 실측(참고 · 팀 리드 측정 확인)**: 타임아웃 128턴(`fail 76`·`error 52`)을 빼도
기능 합격률은 12.5% → 12.8%(+0.3%p)로 거의 안 움직인다. **우리 런의 지배적 원인은 역질문
3,705턴(56.4%)이고 타임아웃이 아니다** — 병행 세션 런(타임아웃 지배 · 25.0%→66.7%)과 다르다.
그래서 원칙은 타임아웃 하나를 넘어선다: `무효`(하네스 과실) · `timeout`(제품·성능 축) ·
역질문 차단(하네스 구성) 셋 다 **단언이 평가된 적 없다**는 점이 공통이고, 기능 합격률 분모에서
빼되 사유는 드러내야 한다. **결론은 승인 후에 낸다.**

#### 추가 랜딩

| # | 랜딩 | 위치 |
|---|---|---|
| **R-15** | **축 최적 레벨 → 처분 번역** — `axis_verdict_word()`. 핵심은 **최적 레벨이 대조군일 때**다: 대조군이 이겼다는 말은 *"현행이 최적이고 다른 레벨이 더 나쁘다"* 이므로 `채택 권고`(기본값 변경)가 아니라 **`기각`**(기본값 유지 + 권장하지 않음)으로 간다. 이 구분이 없으면 **현행 유지가 기본값 변경 제안으로 둔갑**한다. `recommended.env.diff` 도 신설 — 모듈 독스트링이 약속해 놓고 `write_proposals` 가 만들지 않던 파일이다 | `optimize.py` |
| **R-16** | **L5 「판정 불가」와 「미실행」 분리** — 장부 사유와 커버리지 요약이 실제 상태를 적는다 | `report.py` |
| **R-7** | **스위프가 94 리포트를 만든다** — `--sweep` 이 `scripts.scenario.report.write_report(out_dir)` 를 호출해 `summary.json`·`report.md` 를 낸다. 종전에는 없어서 스위프 산출물이 **94 분석기(`analyze.py:476` 이 `summary.json` 을 요구)에 입력 자체가 불가능**했다. §5 Tier A 계약을 고치는 대신 **스위프가 부르게** 했다 — 순수 후처리(입력 `raw.jsonl`)라 비용이 0이고, §5를 고치면 94 분석기가 영영 스위프를 못 읽는다 | `bench/__main__.py` |

### 11.1 남은 것 (코드 0건)

| # | 잔여 | 왜 여기서 안 했나 |
|---|---|---|
| ~~R-6~~ | ~~`runner.iter_executions:514` 알파벳 정렬로 기준선이 늘 마지막~~ | **2026-09-21 해소(§11.3)** — 사용자 지시로 94 범위 편입. `config.profiles` 순서를 존중하게 했다(지정 없으면 종전 알파벳순 유지 · 94 단독 재현성 보존). 「기준선 2회 실행」안은 R-13(대조군 기반 바닥)이 들어가 불필요해졌다 |
| ~~R-9~~ | ~~라이브 서버 실효 설정 전체를 버린다~~ | **2026-09-21 해소(§11.3)** — `ProfileStatus.effective_settings` 신설. `/admin/settings/schema` 가 `is_secret`→None·`is_sensitive`→`********` 로 내보내므로 `run.json` 에 평문이 남지 않는다(`src/api/settings_catalog.py:1040-1047`) |
| ~~R-7~~ | ~~스위프가 `summary.json`·`report.md` 를 안 만들어 94 분석기에 입력 불가~~ | **2026-09-21 해소(§11.3)** — `--sweep` 이 `scripts.scenario.report.write_report` 를 호출한다 |
| **R-8** | 반출 패키지 매니페스트(`triage.py` · §6) | 축소 자체는 이번에 수동으로 성립했다(56 MB). 스크립트화는 다음 스위프 전에 |
