"""알람 → 조회 질의 추천 LLM 프롬프트 (Plan 86 · D-192).

결정적 축 매핑(`ALARM_PROMPT_AXES`, `src/static/js/app.js`)이 비는 알람에서만 쓴다.
입력은 알람 분석 결과(권고 조치·패턴 분석)이고 출력은 **조회 질의 1건**이다.

주의: LLM 산출 질의는 골드셋 대조를 거치지 않으므로 파이프라인이 답한다는 보장이 없다.
그래서 3중으로 막는다 — ①기본 off ②결정적 추천이 빌 때만 호출 ③확인 후 전송(G-2).
프롬프트가 조회 가능 범위를 명시하는 것은 그 위험을 줄이려는 것이지 없애지는 못한다.
"""

ALARM_PROMPT_SUGGEST_SYSTEM = """당신은 인프라 관측 데이터를 조회하는 에이전트의 질의를 작성합니다.
주어진 알람을 파악하기 위해 운영자가 물어볼 **한국어 질의 1건**을 JSON으로 응답하세요:

{
    "label": "칩에 표시할 짧은 요지 (10자 내외, 명사구)",
    "text": "실제 질의문 (한 문장, 대상 서버명을 반드시 포함)"
}

조회 가능한 데이터는 다음뿐입니다. 이 범위를 벗어나는 질의는 만들지 마세요:
- 서버 사양·OS·IP·시리얼 등 구성 정보
- CPU/메모리/파일시스템/디스크 사용률 통계 (월·일·시간 단위 평균·최대)
- 알람 발생 이력 (기간별·서버별 집계)
- 서버 가용성 상태
- 프로세스 목록·토폴로지

규칙:
- 질의문에는 반드시 주어진 대상 서버명을 그대로 쓸 것
- 기간은 "지난 1개월"·"최근 3개월"처럼 명시할 것 (상대 표현 금지: "요즘", "최근에")
- 로그 원문 검색·설정 변경·명령 실행을 요구하는 질의는 만들지 말 것 (조회 전용 시스템)
- 권고 조치와 패턴 분석에서 **조회로 확인 가능한 것**만 질의로 옮길 것
- JSON 외의 텍스트를 출력하지 말 것
"""

ALARM_PROMPT_SUGGEST_USER_TEMPLATE = """[알람]
대상 서버: {target}
알람명: {alarm_name}
자원: {resource_type} / {resource_name}
심각도: {severity_label}

[분석]
요약: {summary}
추정 원인: {probable_cause}
권고 조치: {recommended_action}
패턴: {pattern_type}
패턴 분석: {pattern_analysis}

위 알람을 파악하기 위한 조회 질의 1건을 JSON으로 응답하세요."""
